# Summary

I am MacBeth, a category theorist working on containers, directed containers, and polynomial
functors, for the Kodamai AI Mathematician grant (PI: Neil Ghani; my human: Robin Langer).

**This file is current-state synthesis, organized by FRONT — not history.**
- Chronology → `dream-journal/YYYY-MM-DD.md` (one per cycle).
- Proof grades (proved / computed / lean-verified / in-progress) → `proofs/registry/*.json` — **the registry wins any disagreement with this file.**
- Crown claims + their citations → the auto-memory index `MEMORY.md` (`[[slug]]` links below point there).
- Source provenance (arXiv IDs, deep-read vs agent-summary) → `reading/sources.json` (validated by `trustcheck.py`).

*Compressed 2026-08-30 from ~1690 chronological lines. Nothing proved was demoted; the dated
session blocks now live only in `dream-journal/`. Two external citations that had no home outside
the old SUMMARY are preserved in the ledger below: Katharopoulos linear-attention `2006.16236`,
Kock "Polynomial functors and trees" `0807.2874`.*

---

## ★ ACTIVE FRONTS

### Front A — Fam(C^op) monoidal GENERALITY (Neil's #1 priority, UID 120)
**The question:** do the four monoidal structures on Poly generalize to `Fam(C^op)` for `C` with products, and what breaks?
- **★★ 2026-09-10 (WAKE) — RICK REFEREED THE M-CONTAINER PAPER (`70becd9`): ACCEPT WITH MINOR REVISIONS; and his own `O_{2,C₃}` test, run same-day, CLOSES the open middle of Problem 5.23.** Referee artifact `proofs/reviews/2026-09-10-m-containers-thm3-arc.md`. Rick endorses §5 by name (Prop 5.2, Prop 5.6/Cor 5.7 cardinality law, Lemma 5.9, Thm 5.13 D₄, Lemma 5.15, Thm 5.18 "airtight", **Thm 5.21 C₃ refutation = "the paper's most interesting content… the kind of negative result that reshapes a subfield"**). **Registry: 8 §5 THM-3-arc nodes promoted `proved → peer-reviewed`** (validator-green; `sufficiency-star-A1`, `cardinality-necessity-law-B1B2`, `lemma-R0-retract`, `theorem-P-plethysm-cancellation`, `theorem-S-bounded-degree`, `theorem-S-prime-wreath-depth`, `theorem-general-stabilizer-necessity`, `residual-necessity-fails-C3`; review pointer + minor-revision flags recorded on each). **The `O_{2,C₃}` test (Rick's §4, ~20 lines) decided Problem 5.23:** hybrid operad (binary S₂-symmetric μ = commutative + ternary C₃ ω + absorbed unit), fully symmetric in NEITHER generator, YET necessity HOLDS — witness `w=μ(μ(V₀,μ(V₂,E′)),μ(V₁,μ(V₃,E′)))` has `Stab(w)=⟨(01)(23)⟩=Δ_{C₂}` (order 2, brute `S₄`), an escape `∉𝒴` at `m=4` (`|T_{M∘M}|=6` vs `|T_{L∘M}|=5`, `onlyL=0`, S₃-control pattern). **So the THM 3 dividing line is "M has a commutative binary composite," STRICTLY WEAKER than "fully symmetric"; the headline is CORROBORATED (not re-hedged); Thm 5.24 becomes a genuine biconditional throughout the μ-commutative world; the C₃ reversal is confined to operads with NO commutative binary composite.** Rick's 15% `C₃≀S_m`-interference risk RESOLVED (escape robust, governed entirely by top node = commutative μ; `μ(ω,ω)` stress → `Stab` order 18 `=C₃≀C₂`, coexist; toggling μ non-comm collapses `Stab` to id). Grade `computed` (registry node `o2c3-commutative-mu-dividing-line` under `necessity-lemma-gap`; proof `proofs/2026-09-10-o2c3-commutative-mu-dividing-line.md`; scripts `scratch/2026-09-10-o2c3-*.py`; wip `7e85afc`). **Reply PDF sent to Rick** (`for-collaborator/2026-09-10-o2c3-result.tex`, 3pp, `7e85afc`, CC Robin) closing the referee loop same-day. **Seeded:** WRITE.md (5 minor flags + recast Problem 5.23 as μ-commutative-world theorem), PROVE.md (all-`m` proof = generalize `theorem-general-stabilizer-necessity` off full symmetry to "commutative binary composite"). `publishable-result` now openable AFTER the revisions land. Sharp residual: `a₀>0 ∧ 𝔥=∞ ∧ no commutative binary composite`, canonical inhabitant (pure C₃) already FAILS necessity ⟹ THM 3 picture essentially complete pending the all-`m` proof. [[o2c3-commutative-mu-dividing-line]], [[thm3-necessity-fails-C3-residual]].
- **★ 2026-09-29 (WAKE) — C_3 reversal node PROMOTED `computed → proved` (validator-green); the ENTIRE THM 3 arc is now `proved`.** `residual-necessity-fails-C3` was overlooked in the 09-28 sibling promotion batch: its two cites (Joyal ff/rigidity, Gambino–Kock polynomial↔flat) were deep-read on 09-28, and its internal argument is a genuine all-cases proof (recursive tree-Aut rigidity Lemma 1 + type-set=𝒴 Lemma 2 + ℵ_0-multiplicity Lemma 3 → molecule-multiset equality; the n≤4 escape-search is confirmatory control only, with an S_3 positive control proving the test non-vacuous). An audit sub-agent verified proof-generality + deep-read cites before promotion. So Thm P, Thm S′, U-corner, family 𝒰, and the C_3 reversal all stand at `proved`. **Consequence for framing:** the corrected (conditional-on-symmetry) biconditional is now a fully-`proved` result, not a computed claim — the m-container paper's §5 must be brought up to this (WRITE.md seeded this cycle: paper was a full cycle behind, still listing the U-corner as an open Problem; predated S′, 𝒰, and C_3). **UPDATE (WRITE done, wip `70becd9`/`4d9ec9d`):** the paper is now CURRENT — §5 restates the corrected conditional biconditional, U-corner is a resolved Prop/Thm (not `prob:corner`), the block-fixing meta-theorem is now `prop:meta`, wreath-depth machinery added, Joyal (LNM 1234) + BLL added to bib (both deep-read), 24pp, 0 undefined refs, all five THM-3 results stated as `proved`. RE-SEND to Rick (CC Robin) is the pending WAKE action; `publishable-result` gate stays CLOSED until Rick's referee reply. Definitional hinge for Neil still open (arbitrary analytic vs symmetric effect monads) — put to him in the 09-29 daily email.
- **★★ 2026-09-08 (PROVE-2) — REVERSAL: THM 3 necessity is FALSE in the residual; the biconditional is CONDITIONAL on full symmetry.** `proofs/2026-09-08-residual-necessity-fails-C3.md`; registry `m-containers.json` node `residual-necessity-fails-C3` (validator-green, `computed`); root note + `necessity-lemma-gap` note updated to record the reversal; → `for-collaborator/2026-09-08-thm3-necessity-fails-residual.md`, [[thm3-necessity-fails-C3-residual]]. **PROVE.md asked me to CLOSE the residual (`a_0>0 ∧ 𝔥=∞ ∧ ¬fully-symmetric`); it is a COUNTEREXAMPLE ZONE, not a theorem.** Witness `M` = free-algebra monad of `O=⟨ω ternary, H_ω=C_3 (cyclic, NOT S_3); e absorbed unit⟩`: analytic, **non-polynomial** (non-flat, `H_ω=C_3≠1`), `a_0=1`, unbounded degree/wreath depth, not fully symmetric — exactly the named canonical inhabitant. **YET** `⟦p⟧∘M∘⟦q⟧∘M ≅ ⟦p◁L⟧∘M` for all poly `p,q` (`L`=list container), so `𝓘_M={⟦r⟧∘M}` **is** composition-closed while `M` is non-poly ⟹ (compose ⟹ poly) is FALSE. **The clean dichotomy (real content), governed by ONE bit — is the binary unit-composite `μ(x,y)=ω(x,y,e…)` commutative:** fully-symmetric (`𝒰`) ⟹ `μ` COMMUTATIVE ⟹ peg rigidifies a block, outer `μ` SWAPS two rigid blocks ⟹ escape `Δ_{C_2}=⟨(01)(23)⟩∉𝓕` ⟹ necessity HOLDS; partial (`C_3`) ⟹ `μ` NON-COMMUTATIVE ⟹ no swap; Crux Lemma "only-three-identical" (sole automorphism source = ternary node w/ 3 identical children → `C_3`; all else Young product) ⟹ every `M∘M` stabilizer already a single-tree Aut ∈ 𝒴 ⟹ NO escape ⟹ closure holds ⟹ necessity FAILS. **Key invariant: functor iso ⟺ molecule-multiset equality** (Lemma-E + Joyal converse, holds for infinite components); both `M∘⟦q⟧∘M` and `L∘M` = `⊔_{τ∈𝒴} ℵ_0·τ`. **Test validated both ways:** S_3 control DETECTS the known `Δ_{C_2}` escape at n=4; C_3 finds none, type-sets of `M∘M`/`L∘M` coincide exactly (n≤4). **Corrected THM 3:** sufficiency (poly ⟹ compose) UNCONDITIONAL; converse HOLDS for `a_0=0` (Thm P), bounded wreath depth (Thm S′), fully-symmetric-unital (`𝒰`); FAILS for partially-symmetric unital operads. *Within the fully-symmetric world* (multiset/distribution/supply-chain effects — the applied monads) the biconditional stands; `C_3` marks the boundary showing the symmetry hypothesis is load-bearing. **SUPERSEDES** the hope in the 09-08 (PROVE) + 09-07 (PROVE-2) bullets below that the residual satisfies necessity — the residual `a_0>0 ∧ 𝔥=∞ ∧ ¬(fully-sym-unital)` REFUTES necessity, it does not satisfy it. Open definitional hinge for Neil: is "essential image composition-closed" the intended necessity hypothesis (read straight from `2026-09-05-thm3` §A0/§B)? [[necessity-trichotomy-is-symmetry-resolution]], [[symmetry-is-the-compositional-fingerprint]].
- **★★ 2026-09-28 (WAKE) — PROVENANCE LIFT COMPLETE: 6 THM-3-necessity nodes promoted `computed → proved` (validator-green).** The internal arguments were already proof-complete + code-verified; the sole cap was three background citations known from familiarity. This cycle a provenance sub-agent located + extracted them (`scratch/provenance/{gambino-kock,joyal-1986,bll_book,fghw}.txt`) and I DEEP-READ the exact passages: **GK** 2009 §1.18 (seven-fold TFAE: polynomial ⟺ familially-representable ⟺ preserves connected limits) + §1.20 (flat species ⟺ finitary polynomial functor) + Prop 1.12/1.16, all WITH proofs; **Joyal 1986 Théorème 1** (rigidity: species iso ⟺ natural iso of analytic functors; Prop 1 regular-transf. generalization), primary LNM scan + FGHW `1303.5638` Cor 5.14 corroboration; **BLL Theorem 2.15** (`Z_{F∘G}=Z_F∘Z_G` for **`G[∅]=∅`**), statement verbatim from the authors' free 2013 rewrite (Ch.4 PROOF paywalled/unread — recorded honestly; standard result, instances code-verified). Ledger entries upgraded agent-summary→deep-read. **Promoted:** `theorem-P-plethysm-cancellation`, `theorem-S-bounded-degree`, `theorem-S-prime-wreath-depth`, `U-corner-resolved-stabilizer`, `conjecture-V-refuted-unital-magma`, `theorem-general-stabilizer-necessity`. Parent `necessity-lemma-gap` stays `computed` (full residual `a_0>0 ∧ 𝔥=∞ ∧ ¬fully-sym-unital` still OPEN). **Sharpening:** BLL's `G[∅]=∅` hypothesis = the `a_0=0` P0 needs ⟹ the residual is exactly where the plethysm engine's hypothesis fails ⟹ next PROVE = Route A (unit-quotient to restore `a_0=0`, then P0). Scoop-check cleared: arXiv:2606.03649 (equivariant bispan composition=plethysm via character map) is orthogonal, a contrast cite, not a threat. [[provenance-lift-plethysm-partial]], [[reference_cornelius_bispans_plethysm_not_scoop]].
- **★★ 2026-09-08 (PROVE) — THM 3 necessity PROVED for the free commutative unital magma FAMILY; the peg-abstraction is CORRECTED.** `proofs/2026-09-08-general-stabilizer-necessity.md`; registry node `theorem-general-stabilizer-necessity` (`computed`, validator-green) under `necessity-lemma-gap`; → `for-collaborator/2026-09-08-general-stabilizer-necessity-and-peg-correction.md`. **(1) Theorem (family).** `M∘M≇⟦r⟧∘M` ∀ polynomial for the whole class `𝒰` = free-algebra monads of a **fully-symmetric unital operad** (every arity-≥2 op fully symmetric `H_g=S_arity`; universal absorbed unit `e`, `a_0≥1`; only unary op = id; some op arity ≥2) — the free commutative unital `n`-ary magmas `U_n` for **every** `n≥2` (`a_0=1, 𝔥=∞`), a NAMED slice of the residual, generalizing `U=U_2` from one witness to a family. The SAME `C_2` witness `w=μ(μ(V_0,μ(V_2,E')),μ(V_1,μ(V_3,E')))` works verbatim (`μ`=primitive or composite `ω(x,y,e,…,e)`), `Aut(w)=⟨(01)(23)⟩`. **Crux = the ESCAPE `Δ_{C_2}∉𝓕(A)`:** (R2) [full symmetry + absorbed unit ⟹ no rigid ≥2-label single tree — deepest-node arg]; Lemma 3 [full symmetry ⟹ `Aut(single tree)`=iterated **symmetric** wreath, classes uncorrelated]; a factor `Aut(s)≀S_m` is perm-iso to a fpf-involution-on-4 only if `m=2,|Aut(s)|=1,ℓ=2` = a rigid 2-block, contra (R2). Exhaustive `n=4` for `U_3`: 0 of 25 trees carry a `Δ_{C_2}` factor. **(2) CORRECTION.** The 09-08 `general-stabilizer-diagonal-evidence` (peg-without-unit, parametric-`H`) is **DEMOTED speculative/refuted**: its Task 3 tested only support-INDECOMPOSABILITY (`≥2`-part; the code skips 1-part), so it MISSED that `Δ_{C_3}=Aut(single tree)` EXACTLY for the free ternary operad with a non-absorbing peg ⟹ `Δ_{C_3}∈𝓕`, escape FAILS. And (R2) alone is insufficient — a `⟨(01)(23)⟩`-symmetry operation realizes `Δ_{C_2}` on single leaves; **full** symmetry (all transpositions) is what rules it out. **Correct hypothesis = "fully-symmetric operations + absorbed unit", NOT "rigid label-free peg."** Only the `S_2` control survived (= the genuine unital U-corner). The peg IS a unit; the mechanism is the two-level absorption asymmetry, not label-free rigidity. **Residual sharpens:** `a_0>0 ∧ 𝔥=∞ ∧ NOT a free comm. unital magma monad` (the non-fully-symmetric / non-absorbing part genuinely needs a different tool — §6). Verified `scratch/2026-09-08-{general-stabilizer-verify,escape-exhaustive-n4,correlated-swap-boundary}.py`. [[U-corner-closed-stabilizer-per-component]], [[conjecture-V-refuted-unital-magma]].
- **★ 2026-09-08 (WAKE) [SUPERSEDED/CORRECTED by the 09-08 PROVE bullet above] — The U-corner escape claimed to GENERALIZE from `S_2` to arbitrary `H` (`computed`→refuted).** `proofs/2026-09-08-general-stabilizer-diagonal-evidence.md`; registry node `general-stabilizer-diagonal-evidence` (`computed`, validator-green) under `necessity-lemma-gap`; artifact `scratch/2026-09-08-C3-diagonal-witness.py`. **Mechanism (parametric in `H`):** for an operad operation `ω` of arity `k≥2` with slot-symmetry `H≠1`, plus a **rigid label-free PEG**, the witness `w=ω(b_1,…,b_k)` of `k` isomorphic rigid inner blocks has `Stab(w)=Δ_H` (the correlated diagonal copy of `H`), which is never a block-fixing product when `H` moves the blocks ⟹ `∉𝓕(M)` ⟹ `M∘M≇⟦r⟧∘M`. **Four brute-force checks pass:** (1) CONTROL reproduces the published `C_2=⟨(01)(23)⟩` witness (model validated *before* trusting the extension); (2) cyclic-ternary `H=C_3` gives `Stab(w)=⟨(0 2 4)(1 3 5)⟩=C_3` exactly; (3) that diagonal is support-indecomposable / not a block-fixing product ⟹ escape fires; (4) NEGATIVE CONTROL flat non-commutative op ⟹ `Aut=1`, no escape ⟹ escape pinned on **non-flatness (`H≠1`)**, not the peg alone. **SHARPENING (honesty note):** the ternary `c` cannot carry binary absorption, so it was modeled as a rigid non-absorbing nullary peg — the argument only ever uses a *rigid label-free peg surviving at the symmetry-breaking level*, NOT a unit-with-absorption. So the theorem hypothesis is "`M` admits a rigid peg", which is broader than "`M` has a unit". PROVE.md reseeded to `theorem-general-stabilizer-necessity` (three lemmas: rigidification / constructive `Stab=Δ_H` / escape `Δ_H∉𝓕_ind(M)` — the last, the `𝓕`-self-control gap, is the crux). [[U-corner-closed-stabilizer-per-component]], [[necessity-trichotomy-is-symmetry-resolution]].
- **★★ 2026-09-07 (PROVE-2) — THEOREM S′ PROVED: bounded wreath depth closes THM 3-necessity; the residual is now SHARP (exactly `a_0>0 ∧ unbounded wreath depth`).** `proofs/2026-09-07-theorem-S-prime-wreath-depth.md`; registry node `theorem-S-prime-wreath-depth` promoted **speculative → computed** (validator-green) under `necessity-lemma-gap`. Supplies the "general wreath-depth invariant with conjugacy-invariance + max/additive laws" the old note demanded. **INVARIANT `h`:** `h_t(T)` (transitive) = max length of a strictly-refining chain of `T`-invariant **block systems** (= max subgroup chain from a point-stabilizer to `T`), conjugacy-invariant; `h(K)=max_{orbit O} h_t(K^O)`; `𝔥(A)=sup_l h(H_l)`. Primitive ⟹ `h_t=1`, `S_2≀S_2⟹2`, triple⟹3 (all code-verified `scratch/2026-09-07-wreath-depth-ht.py`). **TWO LAWS (proved):** (Lemma 2 / BOUND) block-fixing product on disjoint supports has `h=max` of factors ⟹ every `(⟦r⟧∘Ã)[m]` stabilizer (block-fixing product of single-constituent aut groups, by the Meta-theorem) has `h≤W:=𝔥(A)`; (Lemma 1 + WITNESS) wreath is **superadditive** `h_t(T_1≀T_2)≥h_t(T_1)+h_t(T_2)` (explicit concatenated chain), and the **uniform substitution** (outer non-flat `c`, all slots filled with one depth-`W` constituent `b`) has `Stab=H_b≀H_c` whose sub-orbit `O_c×O_b` induces a transitive sub-wreath of depth `≥W+1`. `W+1>W` ⟹ `Ã∘Ã≇⟦r⟧∘Ã` (Lemma E). **REACH:** subsumes Theorem S (bounded degree ⟹ `𝔥≤d−1<∞`, strictly larger — `𝕄`/symmetric-powers have unbounded degree but `𝔥=1`); re-proves `𝕄` (all `H_l=S_n` primitive, witness `D_4` at depth 2 — the 09-05 result on its correct axis) and every primitive-constituent monad; **any `a_0`** (no cardinality/cycle-index/finitariness). **RESIDUAL now exactly one row:** `a_0>0 ∧ unbounded wreath depth (𝔥=∞)`, whose one NAMED inhabitant `U` is already closed (below). So every analytic monad we can name has THM 3 as an unconditional biconditional. **Honest gap (§5):** `h` provably can't separate `𝔥=∞` (depth `=∞` both sides); unifying frame = escape must be a support-indecomposable factor of `Ã∘Ã` **outside `𝓕(A)`** (`h`=the greater-depth case; `U`=the same-degree-diagonal case via the peg). Wreath-closed `a_0>0` general case OPEN = `theorem-general-stabilizer-necessity`. → `for-collaborator/2026-09-07-theorem-S-prime-proved.md`. [[plethysm-cancellation-closes-lemmaN]], [[conjecture-V-refuted-unital-magma]], [[U-corner-closed-stabilizer-per-component]].
- **★★ 2026-09-07 (PROVE) — THM 3's LAST CORNER CLOSED: `U∘U ≇ ⟦r⟧∘U` for every polynomial `⟦r⟧` (answer to (★) is NO). Necessity holds at `U`; THM 3 is an unconditional biconditional for `U` (and every named analytic monad).** `proofs/2026-09-07-U-corner-resolved-stabilizer.md`; registry node `U-corner-resolved-stabilizer` (`computed`, validator-green) under `necessity-lemma-gap`. Both prior engines are DEAD at `U` (plethysm needs `Z_M` const-term 0, `Z_U∘Z_U` diverges; wreath-gap needs bounded depth, `U` has `W=∞`). **Tool = the `Aut(X)`-stabilizer invariant (09-05 Lemma E) read on SPECIES COMPONENTS — `a_0`-agnostic, convergence-free.** **META-THEOREM (elementary, any `M`):** on the each-label-once component `[m]`, every `(⟦r⟧∘M)[m]`-stabilizer is a **block-fixing product** `∏Stab(u_β)` of single-`M`-element stabilizers (polynomial outer = rigid positions, "function fixed ⟺ pointwise fixed", so NO swap of parts). **WITNESS (n=4, minimal):** `w=μ(μ(V_0,μ(V_2,E')),μ(V_1,μ(V_3,E')))∈(U∘U)[4]`, `Stab(w)=⟨(01)(23)⟩=C_2` — a lone fixed-point-free **correlated** swap, NOT a block-fixing product (any product ∋(01)(23) ∋(01),(23) → order≥4). So it's an orbit-type of `(U∘U)[4]` absent from every `(⟦r⟧∘U)[4]`. **THE UNIT IS THE WEAPON:** the inner unit `E'`, as an OUTER leaf (NOT absorbed there), builds a **rigid** block `μ(V_i,μ(V_j,E'))`; two equal copies swap with the swap as their ONLY symmetry. `a_0>0` does NOT save the corner — it SUPPLIES the counterexample (exact opposite of the vacuity intuition). **BONUS:** unit-free `n=8` witness `(S_2×S_2)≀S_2` (order 32, support-indecomposable ∉{8,128} ⟹ not a product of tree-groups) lives in `A∘A` (free magma, `a_0=0`) ⟹ **re-proves Theorem P by stabilizers, no plethysm.** CAVEAT (crucial): the raw all-content stabilizer sets COINCIDE (`⟨(01)(23)⟩` is an overlapping-tree intersection, `Hstar.py`) — those reuse labels ⟹ higher components; only the PER-COMPONENT reading separates. `C_2∉𝒫(4)` and `Stab(w)=C_2` brute-verified over `S_4`. Grade `computed` (load-bearing dep = Joyal ff, cited from familiarity, as siblings). General residual → new target `theorem-general-stabilizer-necessity` (uniform "swap escapes `𝒫_M`" for every non-flat analytic `M`). → `for-collaborator/2026-09-07-U-corner-closed-stabilizer.md`. [[conjecture-V-refuted-unital-magma]], [[plethysm-cancellation-closes-lemmaN]], [[neil-k-container-monad-lift-is-fam-kleisli]].
- **★★ 2026-09-07 (WAKE) — Neil UNBLOCKED on THM 1: the typing question is a real FORK, and my side is his change-of-base, not a new theory. Paper §5.6 vacuity claim CORRECTED to match the 09-06 refutation; corrected paper re-sent to Rick.** Neil sent 3 emails (UID 151/152/153) all PAUSING pending my clarification of THM 1. Resolution (`for-collaborator/2026-09-07-neil-thm1-clarification.{tex,pdf}`, 3pp, wip `842db92`): the extension is `⟦S,P⟧_M(X)=Σ_s(P_s→MX)=⟦S,P⟧∘M` — **M on VALUES** (side (i), lives in `Fam(Kl(M)^op)=M-Cont`). Neil's expected `Σ_s(MP_s→X)=⟦(S,M∘P)⟧` is **M on POSITIONS** (side (ii)) — a "fattened Set container", stays in ordinary `Cont`, NO new phenomena; his instinct "these are all just Set containers" is right *for (ii)*, which is exactly why I took (i). **Functor vs enriched:** BOTH — ordinary `Set→Set` (honest target `[Set,Set]`) AND underlying a `Kl(M)`-enriched functor; the gap between the targets IS the codensity monad `Ran_M M` = THM 2 (his `Lan_M` instinct → it's the RIGHT Kan ext / codensity, `(−)∘M⊣Ran_M`). **"Whole different theory?" NO** — `⟦−⟧_M` = his `Fam(C^op)` (priority #1) at `C=Kl(M)`; `Cont=Fam(Set^op)` is `M=Id`; codensity/polynomiality are what that instance contributes. Presheaf variant (UID 151, shapes=small cats, positions=functors to Set) = same frame at `C=Cat` (the UID-144 Cat-case). **Headline recommended:** change-of-base spine, codensity+polynomiality as results-inside ("Containers over a Kleisli base"). Asked Neil 3 confirmations (side (i)? spine? Cat-case now/later). **Paper fix:** §5.6 (`sec:cancel`, was `conj:vacuous`) rewritten — corner INHABITED by `U` (Prop `prop:corner`) + open Problem `prob:corner` (`U∘U≅⟦r⟧∘U`?); abstract/intro/outline/conclusion/provenance all de-vacuum'd; THM `thm:3final` no longer claims unconditional biconditional; 19pp clean. Re-sent to Rick (supersedes `b94bc32`). PROVE.md reseeded: converse-for-`U` / Theorem S′. [[neil-k-container-monad-lift-is-fam-kleisli]], [[conjecture-V-refuted-unital-magma]].
- **★★ 2026-09-06 (PROVE-2) — CONJECTURE V IS FALSE: the last corner of THM 3's converse is INHABITED, not vacuous.** `proofs/2026-09-06-conjecture-V-refuted.md`; registry `m-containers.json` node `conjecture-V-refuted-unital-magma` (`computed`, validator-green), parent `necessity-lemma-gap` note updated. The morning-09-06 dream had banked the §4 vacuity conjecture ("finitary analytic + unbounded wreath depth ⟹ `M∅∈{∅,∞}`", *strong evidence*) as the route to an UNCONDITIONAL converse. **That route is DEAD.** Witness = the **free commutative UNITAL (non-assoc) magma monad `U`**: `U[0]={c}` (`a_0=1`), `U[n]=(2n−3)!!` all finite, balanced `2^k`-leaf trees ⟹ stabilizer `S_2≀⋯≀S_2` depth `k` ⟹ unbounded degree AND unbounded wreath depth — it inhabits corner (a)∧(b)∧(c). Diagnosis of the §4 error: it assumed a substituted constant **BUILDS** new closed terms; a **unit ABSORBS** (`μ(x,c)=x`), so `a_0:0→1` while the entire positive-arity structure (hence unbounded degree + depth) is preserved — `U` = Theorem-P's free commutative magma (`a_0=0`) *with a unit adjoined*. Compensating gain: the corner is now **SHARP** — **wreath depth, not degree, is the true axis** (Theorem S′, `theorem-S-prime-wreath-depth`, conjecture: `A•A` gets a depth-`>W` constituent no flat `B•A` matches; would subsume Theorem S). Residual collapses to exactly `a_0>0 ∧ unbounded-wreath-depth`, **canonical test case `U`**, **converse for `U` OPEN** (if `U∘U≅⟦r⟧∘U` then THM 3 necessity is FALSE — the converse-breaker β couldn't be; handle = the constant-free sub-operad `A′` where P0 already applies). Elementary + code-verified (`|U[n]|=(2n−3)!!` n≤7; `|Aut(balanced 2^k tree)|=2^(2^k−1)`; rewrite `μ(t,c)→t` terminating+confluent, 1179 terms unique NF); the one un-deep-read dep (linear-regular theory ⟹ analytic monad = symmetric operad, Joyal/Gambino–Kock) caps the grade at `computed`. → `questions/lemmaN-beta-two-outcome.md`, [[conjecture-V-refuted-unital-magma]], [[plethysm-cancellation-closes-lemmaN]].
- **★★ 2026-09-06 (PROVE) — Lemma N (THM 3 converse) essentially CLOSED: the Plethysm residual is DISSOLVED by plethysm right-cancellation, covering even unbounded-wreath-depth monads.** `proofs/2026-09-06-lemmaN-plethysm-cancellation.md`; registry `m-containers.json` nodes `theorem-P-plethysm-cancellation`, `theorem-S-bounded-degree` (`computed`, validator-green). **Theorem P0 (plethysm right-cancellation, proved + code-verified):** in `R=ℚ[[p_1,p_2,…]]`, `H` with zero constant term & nonzero `p_1`-coefficient ⟹ `(−)∘H` injective (`a_1≠0` sharp — `a_1=0` control collapses rank). **Theorem P:** `Z_M∘Z_M=Z_{⟦r⟧}∘Z_M` cancels `Z_M` ⟹ `Z_M=Z_{⟦r⟧}`; flat RHS (p_1-powers only, C2) forces flat LHS ⟹ `M` polynomial — for **every analytic monad with `M∅=∅`, ANY degree / ANY wreath depth** (incl. free comm. magma `M[n]=(2n−3)!!`, free operad-algebras — the `S_2≀⋯≀S_2` monads no stabilizer method could reach). **Theorem S:** support-indecomposable-factor degree-multiset is a conjugacy invariant; block-linking (Lemma S2) gives `A•A` a degree-`2·e_max` factor no Young product of `B•A` matches ⟹ closes **all bounded-degree** analytic monads, any `M∅` (subsumes 09-05 `𝕄`/`D₄`). **Only open corner:** analytic, `M∅≠∅` finite, unbounded degree AND unbounded wreath depth — §4 argues VACUOUS (finitary + unbounded wreath depth ⟹ `M∅∈{∅,∞}`; conjecture, strong evidence). **The old "Plethysm Lemma" (decomposition-shaped) was NEVER needed** — cancellation bypasses constituent identification; the browse-triangulated literature gap ("no decomposition-from-composition theorem anywhere") was the wrong shape to want. Provenance cap: Theorems P/S invoke Joyal ff (J1) + plethysm-substitution `Z_{A•B}=Z_A∘Z_B` (C1) [Joyal 1986; Bergeron–Labelle–Leroux Ch.1–2] from memory (`agent-summary`) ⟹ nodes at `computed` until re-read; P0/S1/S2 self-contained + code-verified. WRITE (this cycle) folded it into `papers/mcontainers-codensity-polynomiality.tex` (now 18pp, §5.7 cancellation trichotomy + vacuity conjecture; wip `82e32d2`/`4675935`; local, routes through Rick). Crown restated: **polynomiality of the effect monad = FLATNESS of its species; composition survives self-plethysm iff flatness was there to begin with.** → `connections/decomposition-from-composition-is-the-wrong-shape.md`, `questions/lemmaN-beta-two-outcome.md`. [[plethysm-cancellation-closes-lemmaN]], [[neil-k-container-monad-lift-is-fam-kleisli]].
- **★★ 2026-09-05 (PROVE) — Lemma N (THM 3 converse) LARGELY SETTLED: R0 proved, BOTH counterexample suspects killed, a new stabilizer invariant found. THM 3 is a biconditional for every monad that arises.** `proofs/2026-09-05-lemmaN-retract-and-beta.md`; registry `m-containers.json` node `necessity-lemma-gap` `speculative→computed` (validator-green, 5 children, 4 `proved`). Target was **(★) `M∘M≅⟦r⟧∘M` (r poly) ⟹ M poly?** (1) **Lemma R0 PROVED (Lean-able):** monad ⟹ `M` split retract of `M∘M` (`μ∘ηM=1`), retracts inherit connected-limit preservation ⟹ `M` poly ⟺ `M∘M` preserves connected limits. (2) **The single instance is CIRCULAR — PROVED:** `M∘M≅⟦r⟧∘M` gives NO categorical info beyond R0 (comparison-map reflection through `⟦r⟧` is symmetric); corrects the scoping's hope, (★) needs the full family or a new invariant. (3) **β ELIMINATED — PROVED:** the scoping's prime suspect fails the cardinality law on infinite sets (`|ββX|=2^{2^κ}≠κ=Σ|βX|^{|B_i|}` fixed family); same kills ALL super-poly monads (P,P⁺,D,continuation,filter). (4) **𝕄 (free comm monoid) ELIMINATED — PROVED (CROWN):** the sharp B3 case (non-poly but cardinality all ℵ₀, so counting is blind). New **Aut(X)-stabilizer invariant**: nat iso ⟹ Aut(X)-equivariant bijections preserve point-stabilizers; every `⟦r⟧∘𝕄`-stabilizer is **Young** (within-block), but `{{a,b},{c,d}}∈𝕄𝕄X` has stabilizer `S₂≀S₂=D₄` (order 8 ∉ Young orders {1,2,4,6,24}) — the **block-swap** is a second-order symmetry no free outer layer makes. Python + independent referee sub-agent verified (referee converged on the same `D₄`). **DICHOTOMY:** every non-poly monad fails `M∘M≅⟦r⟧∘M` by (a) super-poly growth [cardinality] or (b) symmetric [wreath stabilizer]; proved for all (a) + the symmetric-power/𝕄 class of (b); residual = one **Plethysm Lemma** ("non-flat species `A` ⟹ `A•A` not `(flat)•A`"), uninhabited by any known monad. Naive "outer-symmetry" heuristic was WRONG (`⟦r⟧∘𝕄` is NOT outer-rigid — inner 𝕄 supplies within-block symmetry); correct line = Young(one layer) vs WREATH(non-free outer). → `for-collaborator/2026-09-05-lemmaN-stabilizer-invariant.md`. [[neil-k-container-monad-lift-is-fam-kleisli]].
- **★ 2026-09-05 (WAKE) — VCont deliverable drafted for Neil's external customer (UID-150); Lemma N SHARPENED into a two-outcome PROVE target; PROGRESSIVE_DISCLOSURE.md compressed 273KB→73KB.** (1) **VCont note** `papers/vcont-treatment-2026-09-05.{tex,pdf}` (8pp, compiles clean, honest grades, marked DRAFT) — Neil (UID-150) asked for a treatment of `VCont=(S:Set,P:S→Vec)` for an external requester. Headline = the **collapse**: over Set ⟦−⟧ is full+faithful (AAG), over Vec it is **faithful-but-not-full** (Thm D: `({∗},k²)≇({1,2},k)` both present `X↦X⊕X`, lean-verified `VecCollapse.lean`); mechanism = Σ-over-shapes ↝ biproduct collapse; ◁-comonoids=families of k-algebras; `Set×Vec_fd` ◁-admissible; **(Q) OPEN (conj. indep. of ZFC)** stated plainly. Extension def `⟦S,P⟧W=⊕_s Vec(P_s,W)`. Emailed to Neil as a draft; routes through Rick before publishable (`state/WRITE.md`). (2) **Lemma N (THM 3 converse) scoped** (`scratch/2026-09-05-lemmaN-scoping.md`): NOT off-the-shelf familial-representability — but `M` a **MONAD** is the lever (`M` = absolute split retract of `M∘M` via `μ∘Mη=id`), giving **Lemma R0: `M` polynomial ⟺ `M∘M` preserves connected limits**. Reduces Lemma N to **(★) `M∘M≅⟦r⟧∘M` (r poly) ⟹ M poly?** — genuinely open, and possibly **FALSE**: the **ultrafilter monad β** (codense ⟹ full by THM 2, but not polynomial) is the prime converse-breaker suspect. Two-outcome next PROVE (`state/PROVE.md`): biconditional, OR sufficiency-only with β witness (a codensity×polynomiality cross-term). Familial-rep theorem pinned: **F poly ⟺ preserves connected limits** [Gambino–Kock §1.18; Carboni–Johnstone MSCS 5 (1995) 441–459 **+2004 Corrigenda**; Weber TAC 18 (2007)]. (3) **Hygiene:** `PROGRESSIVE_DISCLOSURE.md` compressed (chronology→one pointer line; Level-1 entries→one line each; backup `PROGRESSIVE_DISCLOSURE-archive-2026-09-05.md`); now readable. [[vec-admissibility-yoneda-rigidity]], [[neil-k-container-monad-lift-is-fam-kleisli]].
- **★★ 2026-09-05 (PROVE + WRITE) — THM 3 PROMOTED `computed → proved` (SUFFICIENCY), and the m-container trilogy is written up.** `proofs/2026-09-05-thm3-composition-polynomiality.md`; registry `m-containers.json` root **`proved`, validator-green**; verifier `scratch/2026-09-05-thm3-verify.py`. **Sufficiency PROVED, line-audited:** `M` polynomial (`⟦⌈M⌉⟧=M`) ⟹ `p◁_M q := p◁⌈M⌉◁q`, `⟦p◁_M q⟧_M ≅ ⟦p⟧_M∘⟦q⟧_M` (one line from T1b + AAG closure of polynomials under `∘`: `⟦p⟧_M∘⟦q⟧_M=(⟦p⟧∘M∘⟦q⟧)∘M`, middle a composite of polynomials `=⟦r⟧`). **Associative, NON-unital unless `M=Id`** (unit forces `⟦e⟧∘M≅Id`, cardinality at `X=1,2` gives `M≅Id`). **Necessity — cardinality half PROVED, full converse OPEN:** closure forces `|M(Mn)|=R(|Mn|)` a fixed polynomial `R`, firing **already at `p=q=Id`** where `m∘m` is doubly-exponential for `P⁺,P,D` — **decisively refutes "compose ⟺ commutative+affine"** (`Maybe`/writer non-affine COMPOSE; `P⁺`/`D` affine+commutative FAIL). The one honest gap = **Lemma N** (closure ⟹ M polynomial); obstruction NAMED = **trailing-M** (`M^*=(−)∘M` not conservative, so `F∘M≅⟦r⟧∘M` cannot be cancelled); three attempts (limit-reflection, monad-unit retract, diagonal Id-retract-of-Sq) all die there; reduced to the classical familial-representability argument via Carboni–Johnstone (`agent-summary` — attributed in prose, not bibliography). **WRITE:** `papers/mcontainers-codensity-polynomiality.tex` (13pp, compiles clean, all 5 refs deep-read; THM 1/2/3 assembled with visible honesty guards: D-fullness conditional on `Nat(D,D)={id}`, Lemma N open, Kleisli-bifunctoriality open). Local, routes through Rick before `publishable-result` (`papers/` has no git remote). **Crown restated:** two Kan invariants of `M` diverge oppositely — **codensity governs fullness (THM 2), polynomiality governs composition (THM 3)**: *writer composes but isn't full; D is full but doesn't compose.* [[neil-k-container-monad-lift-is-fam-kleisli]], `connections/two-invariants-of-the-effect-monad-codensity-and-polynomiality.md`.
- **★★ 2026-09-05 (WAKE) — THM 3 (do M-containers COMPOSE?) computed, and it is a clean surprise: composition ⟺ M POLYNOMIAL, an invariant INDEPENDENT of fullness.** `scratch/2026-09-05-thm3-mcontainer-composition.md` + verifier `scratch/2026-09-05-thm3-compose-check.py` (ran clean); registry `m-containers.json` node `thm3-composition-polynomiality` (`computed`, +children), validates. **THM 3:** `M-Cont` carries `◁_M` with `⟦p◁_M q⟧_M ≅ ⟦p⟧_M∘⟦q⟧_M` **⟺ `M` is a polynomial functor** (familially representable / preserves connected limits). One line: `⟦p⟧_M∘⟦q⟧_M=⟦p⟧∘M∘⟦q⟧∘M` is an M-extension `⟦r⟧∘M` iff the middle is polynomial, true ∀p,q iff `M` polynomial; then `p◁_M q = p◁⌈M⌉◁q`. **Associative, NON-unital** (monoidal only at `M=Id`). **REFUTES** the parked "composes ⟺ commutative+affine" AND the ZS/H² distributive-law framing (retired — the obstruction is a REPRESENTABILITY dichotomy, not cohomology; the distributive-law route merges the two M's via μ = a different, lossy question). **Maybe/error COMPOSES** (polynomial, non-affine); **D does NOT** (affine+commutative but not polynomial — marginals `D(Y²)→(DY)²` not iso; finite shadow P⁺ has no polynomial fit, `⟦r⟧(3)=261121`). **Crown:** two independent invariants of the response monad — **CODENSITY governs fullness (THM 2), POLYNOMIALITY governs composition (THM 3)** — trading oppositely: *probability keeps faithfulness, loses composability; error keeps composability, loses faithfulness.* Answers the natural follow-up to Neil's UID 147/148. **[SUPERSEDED by the PROVE+WRITE bullet above: promoted to `proved` sufficiency 2026-09-05; Lemma N is the residual gap.]** [[neil-k-container-monad-lift-is-fam-kleisli]].
- **★★ 2026-09-04 (PROVE) — M-container theorems PROVED, and the sketch's fullness criterion CORRECTED: the invariant is the CODENSITY MONAD, not coproduct-preservation.** `proofs/2026-09-04-m-containers-codensity.md`, registry `m-containers.json` (validates, `proved`). **THM 1 `[proved]`:** `M-Cont = Fam(Kl(M)^op)` (equal on data) and `⟦S,P⟧_M = ⟦S,P⟧∘M = p∘M` (an M-container functor is a polynomial functor pre-composed with `M` — the one new identity). **THM 2 `[proved]`:** `⟦−⟧_M:M-Cont→[Set,Set]` is **ALWAYS FAITHFUL** (evaluate at `(s,η_{P_s})`; `η` is the Kleisli identity), and is **FULL ⟺ `M` is codense (canonical `M⇒Ran_M M` iso) AND every `(M−)^A` is a connected functor**; **affine (`M1=1`) ⟹ connected**. Engine: precomposition `(−)∘M ⊣ Ran_M` gives `Nat(Set(A,M−),H)=(Ran_M H)(A)`, so the whole question is governed by the **codensity monad `Ran_M M`** (`Φ(A)=Nat((M−)^A,M)`). **CORRECTION of the WAKE/brief claim "full-faithful ⟺ M preserves coproducts":** WRONG for the honest target `[Set,Set]`. (a) That is T1's *connected-unit* condition for the **coarser Kleisli-enriched target** `[Kl(M),Kl(M)]` (`⟦−⟧_M=R∘⟦−⟧^{Kl}`, `R=(−∘F)` restriction along the free `F:Set→Kl(M)`; `⟦−⟧^{Kl}` always f.f., all the action is in `R` = pure- vs Kleisli-naturality = codensity). (b) The coproduct-**preserving** writer `E×(−)` **FAILS** for `|E|≥2` (`Φ(1)=|E|^{|E|}≠|E|`) — a direct refutation. **Verdict REVERSED on `D`:** distributions are **affine + codense ⟹ FULL** (`Nat(D^A,D)=DA`, natural ops = affine combinations; `computed`, pending base lemma `Nat(D,D)={id}`), while **Maybe / exception / writer / reader / nondeterminism all FAIL** (exact AAG table). **Corrected slogan:** *probabilistic positions KEEP on-the-nose faithfulness; error / partiality / read-context / nondeterminism DESTROY it — the dividing invariant is codensity of the effect monad.* Crown connection: **container-extension fullness ↔ codensity monads** (Leinster-style), worth a WRITE. THM 3 (composition = distributive law, ZS/H²) parked `speculative`. Verification: `scratch/m-containers/{mcont,dcod,dnat}.py`. [[neil-k-container-monad-lift-is-fam-kleisli]].
- **★★ 2026-09-04 (WAKE, 2nd) — FIRST publishable-result push + Neil's new monad-lift idea REDUCED to my machinery.** (1) **Re-entrancy Lean note → `scotmacbeth/publishable-result@041e7c5`** (the repo's first entry). Rick refereed (email 09-04, artifact `proofs/reviews/2026-09-04-standalone-lean-note.md`; node `standalone-lean-note` in `orchestration-zs.json` stays `peer-reviewed`, finalized field added) and cleared it; folds applied this session (split Prop 3.1→3.1/3.2/3.3; **Gerstenhaber cite VERIFIED** Ann. of Math. (2) 79 (1964) 59–103 [[gerstenhaber-1964-cite-unverified]]; Z²=C²+.lean SHA already in). wip snapshot `a684270`. Emailed Robin per §4.3. (2) **Neil emailed 2 ideas** (probabilistic containers; uniform "K-container" monad-lift with distributions + error/Maybe monads, change of base Cont→M-Cont). A scoping sketch (`for-collaborator/2026-09-04-neil-K-container-monad-lift-sketch.md`, §2/§3 `computed`) reduced BOTH to Fam(C^op) with **C=Kl(M)**: **M-Cont ≃ Fam(Kl(M)^op)**, `[[-]]_M=[[-]]∘M`, and by flagship T1 **`[[-]]_M` full-faithful ⟺ M preserves coproducts** (FAILS for D and Maybe — M1 disconnected); composition = distributive law = the ZS/H² obstruction (predicted: composes ⟺ M comm+affine; Maybe non-affine breaks it — the grant-Impact "agent may decline" story). PROVE target reset to this (THM 1/2 primary, THM 3 stretch); (Q) frontier archived `state/PROVE.md.frontier-Q-2026-09-04`, stays the parallel deep track (flat-Mittag-Leffler next). [[neil-k-container-monad-lift-is-fam-kleisli]].
- **★ 2026-09-04 (WAKE) — NEIL STEER (UID 144): DRAW THE LINE on containers-over-a-base; commit the Vec-admissibility (Q) crux as the standalone deep result; SKETCH #3/#4 as future work.** Neil: "i think we wrap up ... but here are some things" — (1) does Cont preserve BIFIBRATIONS / fibred structure / comprehension categories (logic)? (2) VCont — anything left? (3) **Cat case**: shapes = categories, positions = indexed categories; (4) **Cont(Cont)** self-application. He wants #3 & #4 *sketched* — "noting them as examples for future work can be as important as an exhaustive trawl now." He endorsed my recommendation: line drawn on the trichotomy / logic-of-containers paper NOW; (Q)=Ext¹(Q,F_fin) in Add(Vec,Vec) as the one multi-session frontier; Front C (indexed-container branching) the self-contained retreat. Response: #1 already have it (Cont(cod)=Fam(cod^op) PROVED bifibration, A/E=∏/Σ liftings = comprehension anchor; sharper open Q = does (S,P)↦⟦S,P⟧ PRESERVE comprehension/fibred structure fibrewise); #2 VCont's live content IS (Q); #3/#4 drafted a future-work sketch note `for-collaborator/2026-09-04-cont-future-work-cat-and-cont-cont.md`. Triggers unchanged (PROVE=(Q), WRITE=Rick re-entrancy revisions + Xarez cite) — both align with the steer.
- **★★ 2026-09-04 (PROVE) — (Q) reduced to a SINGLE homological question, and BOTH classical decision routes provably CLOSED in ZFC.** `proofs/2026-09-04-Q-homological-reduction-and-target-dimension-dichotomy.md` (registry `conj-absorptive-dichotomy` +5 `proved` children `projective-presentation-thmD`/`homological-reduction-thmE`/`target-dimension-dichotomy-thmF`/`specker-cokernel-corG`/`no-cardinal-obstruction-thmH`, validates). **THM D:** explicit projective presentation `P₀=⊕_{η∈F(E)}h_E ↠ F` (𝔠 copies of `h_E`; `F` NOT cyclic). **THM E (the reduction):** applying `Nat(−,M)` to `0→F_fin→F→Q→0` (`F_fin` projective) gives `0→coker(ρ_M)→Ext¹(Q,M)→Ext¹(F,M)→0` ∀M ⟹ **`F` projective ⟺ `Ext¹(F,M)=0 ∀M` ⟺ `Ext¹(Q,M)=coker(ρ_M) ∀M`.** ★ CLARIFIES COR B: `[F]=∂(id_{F_fin})∈coker(ρ_{F_fin})`, so last session's Cor B is *only* `coker(ρ_{F_fin})≠0` — a bound on the LEFT term, **SILENT on `Ext¹(F,F_fin)` where projectivity actually lives** (the "close to done" reading was too optimistic). **THM F (target-dimension dichotomy):** `Nat(F,⊕_j h_{W_j})` has finite row support **⟺ every `W_j` finite-dim**; then `=⊕_n Nat(A,G)` (so `Nat(F,A)=⊕_n End(A)`, recovering THM 1). Infinite-dim FAILS — witness `r∈Nat(F,h_E)` (read column 0), `r∘in_n≠0 ∀n`. Turns §5's heuristic "wall" into a THEOREM. **COR G (Specker cokernel):** for finite-dim-rep `G`, `coker(ρ_G)=∏_n Nat(A,G)/⊕_n Nat(A,G)` a REDUCED PRODUCT ≠0 ⟹ **`Ext¹(Q,A)≠0`, `Ext¹(Q,id)≠0`** (sharper than Cor B); the Baer/Specker product-mod-coproduct object now a concrete subobject of `Ext¹(Q,−)`; (Q) YES ⟺ `Ext¹(Q,G)` is *nothing but* this ∀G. **THM H (cardinality NEUTRAL):** Lemma H0 `Nat(h_N,id)=N` (Yoneda, NO `2^{dim}` blow-up, vs ℤ `Hom(ℤ^{(κ)},ℤ)=ℤ^κ` size `2^κ` that powers Baer–Specker) ⟹ under the Cor-C1 free shape every dim invariant AGREES (all 𝔠); **no cardinal invariant separates `F` from free.** Cor H1: single-target (THM A) AND cardinality (THM H) routes BOTH provably closed ⟹ direction A must go through `Ext¹(F,−)` at an INFINITE-dim target (Whitehead territory) — **independence bet now theorem-backed, not heuristic.** Mechanics green `scratch/2026-09-04-nat-F-A-check.py`. NEXT: `Ext¹(F,M)=0?` at ∞-dim `M` via syzygy `K=ker(P₀→F)`; dir A = natural cocycle `K→M` not extending to `P₀`, dir B = Whitehead uniformisation under `¬(2^{ℵ0}<2^{ℵ1})`; THM H says only the uniformisation content of Eklof–Mekler Ch XII can bite. → `for-collaborator/2026-09-04-Q-homological-reduction.md`, [[vec-admissibility-yoneda-rigidity]].
- **★ 2026-09-20 (dream) — thin coverage cycle; two takeaways. (1) Lemma N's residual (Plethysm Lemma) confirmed a GENUINE LITERATURE GAP three ways → no citation shortcut, needs an original species proof.** Browse `reading/2026-09-20.md` ran a first-ever reverse-citation audit of the exact source theorem (Carboni–Johnstone MSCS 5 (1995), 121 cites; Weber TAC 18 (2007), 104 cites) + arXiv full-text + web: nobody 2023–2026 uses it for monad *decomposition* (only the easy closure `M,N poly ⟹ M∘N poly`). Reconciled with the 09-05 PROVE that already did the direct attack (R0 proved; β + 𝕄 eliminated; super-poly/wreath dichotomy; residual = Plethysm Lemma). **Stale `lemmaN-beta-two-outcome.md` rewritten** — the "β makes Lemma N FALSE" crown-witness outcome did NOT happen (β killed by cardinality). Weber TAC 18 formally RETIRED from citation-trail rotation (4th negative). **(2) "Codensity" went zero→active in PL/semantics, none touching handler *composability* — fresh territory adjacent to THM 2.** Kura arXiv:2602.03275 (deep-read, **Prop 5.3: free monads on polynomial endofunctors on Set = λeff-models** — my free-monad construction is a special case of a handler-completeness theorem, the THM-3/polynomial side); STACS26 "Demystifying Codensity via Duality" (LIPICS 364, citable if THM 2 written standalone); dicodensity arXiv:2510.06777. New target: recast THM 2 (codensity⟺fullness) as completeness in Kura's λeff-model class → `questions/codensity-effect-handler-composability.md`. Dormant-front leads noted (cstheory:56966 Plotkin–Power state-algebra for Workers; MO 511702 free-monoid-in-topos historical source, watch). HYGIENE still deferred: `PROGRESSIVE_DISCLOSURE.md` ~262 KB unreadable, `MEMORY.md` ~20 KB — WAKE tasks. → `connections/two-invariants-of-the-effect-monad-codensity-and-polynomiality.md`, [[neil-k-container-monad-lift-is-fam-kleisli]].
- **★ 2026-09-18 (dream) — thin cycle: one browse, mostly dedup; the flat-ML literature avenue is now CONFIRMED EXHAUSTED (four-point negative), and a new row-finiteness template/contrast lands on THM F.** `reading/2026-09-18.md` was almost entirely rediscovery (every substantial paper already `deep-read` in `sources.json`; title cross-check caught 100% of false "new find" claims). Two takeaways. **(1)** Trlifaj flat-ML survey (arXiv:2303.12549) has **one** Semantic Scholar citation (self-citation), zero independent uptake — the FOURTH negative on off-the-shelf functor-cat projectivity machinery ⟹ the flat-ML reframing of `F=∏_ℕ∘⊕_ℕ` must be done directly in PROVE, not sought in literature. **(2)** Genuine new find — **Brandenburg MO 139493** (2013): *"epis `ℤ^ℕ→ℤ^ℕ` split"* ⟺ countable Whitehead, via **row-finite infinite matrices + triangular splitting**. Same row-finiteness as my **THM F** (`Nat(F,G)` finite row support ⟺ every `W_j` fin-dim): in `ℤ` (slender) it holds → splitting; in `Vec` (anti-slender) THM F says it fails at ∞-dim targets = THE WALL. Brandenburg is both a **template** (triangular splitting = shape a projectivity proof of `F` needs) and a **sharp contrast** (works because `ℤ` slender). 10-min PROVE comparison flagged. Methodological: grep `sources.json` for a keyword BEFORE dispatching browse agents (rediscovery is now default at 113 logs); pivot next browse to Path 4/5/6. Hygiene: SEED Path 3 "Polynomial Comonoids 2021" = same as Functorial Aggregation 2111.10968 (no separate paper; cosmetic SEED staleness, flagged not edited); nLab "bicrossed product" revised 2025-10-10 (diff-check if wrap-up touches ZS). → `connections/browse-found-the-machinery-for-the-conj62-wall.md`, `questions/vec-admissibility-projectivity-crux.md`. [[vec-admissibility-yoneda-rigidity]].
- **★ 2026-09-17 (dream) — the browse loop closed on the (Q) wall a SECOND time, now matching the exact reference class THM H named; PIVOT to flat Mittag-Leffler.** Consolidates browses 09-16/09-17. **Crown association:** the 09-04 PROVE concluded *from inside* (THM H, cardinality-neutral) that (Q)'s direction A must run through `Ext¹(F,−)` at an ∞-dim target — **Eklof–Mekler Ch XII / Whitehead territory** — and the browse *independently* returned exactly that: (i) **flat Mittag-Leffler modules** (Trlifaj survey arXiv:2303.12549; Eklof–Mekler "1-projective") = the correct ∞-dim replacement for "projective" ⟹ **the recommended next attack on `F=∏_ℕ∘⊕_ℕ`, not tried yet**; (ii) **Nunke 1961** (slender groups, Bull. AMS): `Ext¹(∏_ℕℤ,ℤ)≅(⊕_{2^𝔠}ℚ)⊕(⊕_{2^𝔠}ℚ/ℤ)` — abelian shadow of `Ext¹(Q,F_fin)`, **upgrades THM C's independence conjecture from "consistent with" to "historically corroborated"** (cite + Fuchs Vol II §99; caveat: `ℤ` slender / `k` anti-slender, corroborates GENRE not transport). **Three-point negative pattern now named:** off-the-shelf functor-cat projectivity machinery stops short of the non-Hom-finite ∞-index regime (2407.04012 triangularity fails on Vec; refs all Hom-finite; sole citer Ma–Yang 2026 arXiv:2608.29267 silent) ⟹ shelf bare, do the reframing directly. **Cat-case raw material found** for Neil UID-144 wrap-up: **Spivak "Functorial Aggregation" arXiv:2111.10968** (categories as positions; frontier confirmed EMPTY at every level). Xarez prior-art RESOLVED + shipped (09-04 WRITE, `b8462d1`). Minor citable corroborations: MO "zero divisors in connected distributive categories" (2025) + Fujino–Matsumoto 2207.05702 (TAC 2022) for the π₀/connectedness line; nLab "connected object" hypothesis-list = a shortcut for re-checking trichotomy bases. → refreshed `connections/browse-found-the-machinery-for-the-conj62-wall.md`, `questions/vec-admissibility-projectivity-crux.md`, `for-collaborator/2026-09-17-Q-pivot-and-cat-case-raw-material.md`.
- **★ 2026-09-15 (dream) — the 09-14 browse handed me TWO consequential inputs.** (1) **Technique leads for the (Q) wall:** arXiv:**2407.04012** (stalk functors → intrinsic projective/injective criterion in `(Add(Vec,Vec);E)` — the exact setting) + **Kaplansky, Ann. Math. 68 (1958)** (via Schröer Baer–Specker note — classical `∏∘⊕` non-projectivity technique). The browse loop closed on its own wall: the 09-03 WAKE set the frontier keyword FROM the PROVE's wall, and the browse returned Ext¹/projectivity machinery matching the *mechanism* the wall named. READ 2407.04012 before next PROVE; open worry = does it reach the non-Hom-finite, infinite-index case or die at Hom-finiteness. → `connections/browse-found-the-machinery-for-the-conj62-wall.md`, `questions/vec-admissibility-projectivity-crux.md`. (2) **Possible 15-yr prior echo of π₀-multiplicativity — Xarez arXiv:1112.4277** (`agent-summary`): "stable units ⟺ finite products of connected components are connected" (categorical Galois theory, 2011) reads verbatim as my Cor 3.1, under a *different* sense of "admissible." Crown meta-pattern in REVERSE (vocabulary COLLISION, not fused seam). If literal on deep-read, Cor 3.1 cites Xarez for the connected-components lemma and claims only the ◁-bridge — **must settle before the admissibility paper is `publishable-result`.** → `connections/xarez-stable-units-vs-pi0-multiplicativity.md`, `questions/xarez-stable-units-prior-art.md`.
- **★ 2026-09-03 (WAKE, post-09-15-dream) — the two (Q) technique leads BOTH ASSESSED and set aside; Xarez prior-art RESOLVED (not a demotion); Rick referee event registered.** (1) **arXiv:2407.04012** (Estrada–Cortés-Izurdiaga–Odabaşí, stalk functor `s_A`, Thm 6.5 intrinsic projectivity in `Add(A,R-Mod)`) **DOES NOT APPLY** to `Add(Vec,Vec)`: needs shape cat `A` **triangular** (Hyp 5.1, zero trace ideals `Hom(A,B)⊗Hom(B,A)→Hom(B,B)=0` for `A≠B`) AND **chain-bounded** (Cond 6.1/6.2). `Vec` fails ALL — nonzero trace (`g:k²↠k, f:k↪k², g∘f=id`), infinite chains both ways; `s_A` literally undefined for `A=Vec`. Break is STRUCTURAL, strictly EARLIER than Hom-finiteness. Salvage: confirms representables are the projectives + `E-Proj⊆⊥s(∏R_A-Mod)` unconditional; the reverse needs the triangularity `Vec` lacks. (2) **Kaplansky 1958 / Baer–Specker** ∏∘⊕ non-projectivity = SLENDERNESS of `ℤ` (Specker `Hom(∏ℤ,ℤ)=⊕ℤ`), **NOT field-transportable** — over `k` every space free, `∏_ℕV` IS projective, fields ANTI-slender (`Hom(k^ℕ,k)` blows up ≠ `⊕`). My worry confirmed. **(Q) REFRAMED: a categorical-Specker / slenderness problem INSIDE `Add(Vec,Vec)`** — projective = retract of `⊕ ev_N`; the only obstruction is whether every `F ⟹ ⊕ᵢ ev_{Nᵢ}` factors through FINITELY-supported data; THM 1 (`Nat(F,id)=⊕_ℕ k^ℕ`) is exactly the fin-dim-target instance, the ∞-dim target `ev_W` is the wall. Neither imported paper reaches it; direct ∞-dim-target computation stays the attack (`state/PROVE.md` refreshed). → `questions/vec-admissibility-projectivity-crux.md` (leads corrected). (3) **Xarez arXiv:1112.4277 "Admissibility, Stable Units and Connected Components" (2011): deep-read, prior-art RESOLVED, verdict (b) cite-as-related.** His "admissible" = semi-left-exact = Janelidze admissibility = **Thm 4.1** (single connected components) — WEAKER, NOT the products condition; the products condition = his **STABLE UNITS Thm 5.1** ("finite products of connected components connected"), strictly stronger. RHS coincides with my π₀-multiplicativity but the LEMMAS differ (my ◁-admissibility of `Fam(C^op)` vs his stable units of a reflection); for infinitary-lextensive `C` they are **CO-EXTENSIVE** — same watershed via Lemma S / external distributive law vs Galois descent; **the bridge is my novelty.** My FAILURE examples (`Set×Set`, `Gl((−)²)`) are NEW — both Xarez examples SATISFY the condition, so no prior art for the failure; Weichsel stays the anchor. Admissibility paper needs a short Xarez [Thm 5.1] paragraph + refs (Cassidy–Hébert–Kelly 1985; Carboni–Janelidze–Kelly–Paré 1997) + a phrasing guard (don't conflate his own two notions) BEFORE `publishable-result`; NO demotion, headline stays novel. `questions/xarez-stable-units-prior-art.md` RESOLVED; `state/WRITE.md` set. (4) **Rick referee report (email 2026-09-03) CLEARS the publishability bar** on the standalone Lean-verified re-entrancy note (`papers/reentrancy-machine-checked-obstruction.tex`, `[ω]=ε∈H²≅𝔽₂`) with minor revisions (split Prop 3.1→3.1a/b/c; C³=0 boundary sentence; `.lean` SHA; Gerstenhaber page-check). Registered as a SCOPED **`peer-reviewed`** child node `standalone-lean-note` in `orchestration-zs.json` (root stays `proved`, `lean-omega-equals-epsilon` stays `lean-verified` — the review is of the WRITEUP, not the whole program); artifact `proofs/reviews/2026-09-03-reentrancy-standalone-note.md`. Revisions queued in `state/WRITE.md` before it moves to `publishable-result`.
- **★★ 2026-09-03 (PROVE, 2nd session) — THE WALL WAS FALSE. Row-killing rigidity `Nat(Q,h_W)=0 ∀W` (THM A); free⟹countable summands in ZFC (THM C, GCH removed); obstruction relocated to `Ext¹(Q,F_fin)`; (Q)/Conj 6.2 conjectured INDEPENDENT of ZFC.** `proofs/2026-09-03-row-killing-rigidity-and-zfc-index-bound.md` (registry `conj-absorptive-dichotomy` +5 children incl. `row-killing-rigidity-thmA`/`zfc-index-bound-thmC`/`reduced-sequence-nonsplit-corB`, all `proved`, validates; `infinite-dim-target-rigidity-wall`→dead-end). Set `F_fin=⊕_ℕ h_k` (finitely many nonzero rows), `Q=F/F_fin`. **THM A:** the ∞-dim-target rigidity the AM session declared FALSE is TRUE for **every** `W` — kill a whole ROW `φ_{n0}` of the tautological `U=k^{(Σ)}` (a single-row kill preserves `[η]∈Q`), and `⋂_{n0}Fix(φ_{n0})=0` **independent of `dim W`**, so `v=β_U([η])=0`. AM attacked columns/finite projections (wrong axis). Generalises THM 1 (=`W=k`). Cor: `Nat(Q,⊕_j h_{W_j})=0`, `Nat(Q,F)=0`. **COR B (unconditional):** `0→F_fin→F→Q→0` NON-SPLIT (split ⟹ `Q↪F∈Nat(Q,F)=0`); `Ext¹(Q,F_fin)≠0` — **the obstruction to (Q) lives in an extension class, in NO Nat-group**, so every single-target rigidity route is provably dead (all consistent with `F` free). Contrast: telescope `0→A→F→F→0` splits. **THM C (ZFC):** free `F≅⊕_J h_{N_j}` ⟹ `J` countable (`F_fin` ctbly generated, compact `h_k` ⟹ finite j-support ⟹ `F_fin⊆⊕_{J'}`; uncountable `J`⟹ summand-proj kills `F_fin`= nonzero `Q→h_{N_j0}`, contra THM A) — **removes AM's `2^{ℵ0}<2^{ℵ1}`.** With THM 2(c): free `F≅⊕_ℕ h_{N_n}`, ∞-many ∞-dim, `dim N_n≤𝔠`. **REFRAME:** `F=Hom(E,E⊗−)=h_E∘A`=monad of `E⊗−⊣h_E`; the ℤ-Baer–Specker proof dies STRUCTURALLY because the honest B–S object `h_E=∏_ℕ id` **is free** (representable), Specker's thm = THM 1. **Residual crux = Specker-realisation:** can `∏_n N_n≅Nat(F,id)=⊕_n k^ℕ` compatibly with finite-row-support `Θ`? = does `Ext¹(Q,F_fin)` split as a fn of set theory. **CONJECTURE (spec): (Q)/Conj 6.2 INDEPENDENT of ZFC** (Whitehead-type) — not-free under `2^{ℵ0}<2^{ℵ1}` (dir A), maybe free under its failure. Mechanics green `scratch/2026-09-03-row-killing-check.py`. → `for-collaborator/2026-09-03-row-killing-rigidity-and-the-independence-conjecture.md`, [[vec-admissibility-yoneda-rigidity]].
- **★★ 2026-09-03 (PROVE, 1st session) — Conj-6.2 crux (Q): `Nat(F,id)=⊕_ℕ k^ℕ` PROVED, free case pinned, wall located (later REFUTED — see 2nd session above); (Q) still OPEN.** `proofs/2026-09-03-nat-F-id-specker-and-free-structure.md` (registry `conj-absorptive-dichotomy` new children, all validate). **THM 1 (guaranteed deliverable, DONE):** `Nat(F,id) ≅ ⊕_{n∈ℕ} k^ℕ` exactly — the *no-exotic* half (`α∘in_n=0 ∀n ⟹ α=0`) by a **field-native** *tautological-spread + finite-projection* argument (no ℤ-divisibility, no base-slenderness; any `ξ=F(w)(η)` with `U=k^{(supp ξ)}`, `v=α_U(η)` a single finite-support vector killed by a projection). **THM 2:** free `F≅⊕_j h_{N_j}` ⟹ **[ZFC] ∞-many summands are infinite-dim** (via F not preserving ∞-⊕ — kills old Case A outright; all the wall uses), `dim N_j,|J|≤𝔠`; **under `2^{ℵ₀}<2^{ℵ₁}` (GCH, indep. of ZFC): countably many countable-dim summands** (honest: countable refinement is NOT a ZFC theorem). **Self-similarity** `F≅A⊕F≅h_E⊕F`; telescope `0→A→F→F→0` **splits** (`lim¹=0`) — a tempting approach documented dead. **THE WALL, located:** Thm 1's engine works for target `id=h_k` *only because `k` is finite-dim* (`v` a single vector); for target `h_W`, `W` ∞-dim, it's FALSE (`v∈Hom(W,U)` ∞-support). Thm 2(c) forces a free `F` to carry an ∞-dim summand — and `h_E` **IS** a genuine retract of `F` (column-0 split; consistent, not a contradiction). Direction A needs an **∞-dim-target rigidity** / `Ext¹(F,K)≠0` the field engine can't supply; 3 finish attempts hit this one edge. Mechanics green `scratch/2026-09-03-mechanics-check.py`. → `for-collaborator/2026-09-03-nat-F-id-specker-and-the-wall.md`, [[vec-admissibility-yoneda-rigidity]].
- **★ 2026-09-03 (WAKE) — Neil back in touch (UID 141): wants a STATUS SUMMARY of "containers over different categories" + open questions to catch up; he is clearing his inbox and will reply in detail to what I send.** Shipped a 6pp status memo `papers/status-containers-over-a-base-2026-09-03.{tex,pdf}` (well-formed, verified §5/§6; corresponds to wip `53db0f7`) — synthesises Front-D weakening tower + the proved ledger (T1/T2/T4-left/Cont(cod)) + the admissibility trichotomy & π₀ characterization + Gap 1 + DJN gate + the Conj-6.2 frontier + δ≟Φ correction. Emailed to Neil end-of-session, CC Robin. **Also: the ∞-copower "obstruction" sketch (old PROVE.md tractable-first-move) is DEAD — independently stress-tested (`scratch/2026-09-03-vec-inadmissibility-stress-test.md`, `computed`): EVERY naive separator (coproduct-/product-preservation, dim, accessibility, exactness, Lemma-S-vacuous) is a non-obstruction, confirming the 09-02 rigidity state.** PROVE.md REDIRECTED to the real crux: (Q) is F=∏_ℕ∘⊕_ℕ projective in Add(Vec,Vec)? — attack = free presentation `0→K→P₀→F→0` + does infinite-row-support ζ∈F(k) lift naturally (Ext¹≠0 ⟹ F not projective ⟹ Vec inadmissible ⟹ Conj 6.2 holds, direction A); guaranteed fallback = Specker lemma `Nat(F,id)=⊕_n k^ℕ`; caveat carried = free (coproduct-of-representables) ⊋? projective (retract), coincide only under an unestablished Krull–Schmidt over Add(Vec,Vec) so "not projective" settles A but "projective" alone does NOT settle B. **(Q) is MULTI-SESSION** (functor-cat homological algebra over a field; Baer–Specker engine does NOT transport — every k-space free); Front C is the self-contained retreat. [[vec-admissibility-yoneda-rigidity]].
- **★★ 2026-09-02 (PROVE + WRITE) — THE EXTENSIVE-CCC POLE IS COMPLETELY CHARACTERIZED: admissible ⟺ `π₀` multiplicative; the two inadmissible bases UNIFY.** `proofs/2026-09-02-lemmaS-sufficiency.md` (`proved`, registry `lemmaS-sufficiency-extensive-pole`, verify `scratch/verify_distributive.py` 0 fail). On a nontrivial **infinitary-lextensive cartesian-closed** base, `Fam(C^op)` is `◁`-admissible **⟺ the connected-components functor `π₀` preserves finite products** (Cor 3.1). **Thm 1:** extensive closed-monoidal + external distributive law (D) ⟹ admissible (constructs `◁` explicitly; (D): shape = hom-**SET** `C(P,T·1)`, positions = external `P≅∐_t P^c_t`). **Thm 2:** extensive **CCC** ⟹ (D) ⟺ S-weak (`[P,T·1]` a copower of `1`); universality fails in additive cats ⟹ flexible pole (`Vec`) out of scope. **Cor 3.2 — THE UNIFICATION:** the census's two inadmissible extensive bases are ONE criterion — `Set×Set` (disconnected unit; `π₀`=coproduct, `π₀(X×Y)≠π₀X×π₀Y`) and `Gl((−)²)` (CONNECTED unit; `π₀`=graph-components, `π₀(K×K)=2` by Weichsel) **both fail `π₀`-multiplicativity**. **KEY TENSION honoured:** `Gl` is a topos so the *internal* Gambino–Kock law holds and is useless — admissibility needs the **external** (hom-SET) law, external=internal ⟺ S-weak. **Corrects** Cor 5.1 prose overreach in [[gl-inadmissible-pi0-multiplicativity]] ("Lemma S can fail to give admissibility" FALSE — `Gl` *fails* Lemma S). **Front-A state after this:** extensive-CCC pole SOLVED (`π₀` mult, decidable single check); flexible pole admissible via copower-tiny; **only Gap 1 (mixed middle — admissible ∧ non-collapse ∧ non-cartesian) still open** (π₀ result is extensive-CCC-only). **WRITE shipped 2026-09-02:** admissibility paper upgraded to the complete characterization (21pp clean pdflatex, new §5, `π₀` map-column; two errors fixed — false "connected ⟺ admissible" in intro/Cor, and §6 Lemma-S-sufficiency overreach `Gl` refutes). NOT pushed (email/browse off; routes via Rick per protocol); handoff `papers/HANDOFF-admissibility-2026-09-02.md`. Robin flag: `memory/code/citation_check.py` not at documented path. Memory [[lemmas-sufficient-extensive-pole-pi0]]; SCOPE: Conj 6.2 (absorptive dichotomy) STILL OPEN.
- **★ 2026-09-02 (WAKE) — admissibility paper OUT FOR REVIEW + Gap-1 target COLLAPSED to one question (`computed`/sketch, not verified).** (1) `papers/admissibility-composition-product.{tex,pdf}` (π₀ characterization) pushed to `work-in-progress` commit **`53db0f7`** and **sent to Rick** (review PDF `papers/admissibility-for-rick-2026-09-02.pdf`, stamped w/ hash per PROTOCOL §2.3) — the gate before publishable. (2) A candidate-scoping pass reframed **Conjecture 6.2**: CONFIDENT lemma — additive/semiadditive ⟹ **not extensive** (coproduct universality fails) ⟹ **no rigid factor**, so 6.2's "reducible = rigid × flexible" framing is wrong for additive bases: **if full ∞-dim `Vec` is admissible it is IRREDUCIBLE** (a counterexample to 6.2), not a product. ⟹ **the whole conjecture reduces to ONE question: is full (∞-dim) `Vec` ◁-admissible?** — every additive family (R-Mod non-Noeth, dg, comodules, Banach, `[D^op,Vec]`, graded, Sup/CMon/JSL) funnels to this door (non-extensive ⟹ all-tiny=collapse OR a 3rd ∞-⊕ mechanism), and the other door (non-additive non-extensive) is empty but for dead `Set_*`. TRACTABLE FIRST MOVE (queued `state/PROVE.md`): prove `Vec` **inadmissible** via the ∞-copower obstruction `X↦[k^(ℕ),⊕_ℕ X]=∏(⊕)` not a coproduct of representables ⟹ dichotomy HOLDS, no irreducible inhabitant. WILDCARD: nuclear/topological VS (tiny ≠ fd) — the one corner unscreened.
- **★★ Path-6 ↔ Front-A BRIDGE (2026-09-12 dream; `demonstrated` (pattern) 2026-09-02 LEAN) — QPF `abs`/`repr` is the formalisation tool for the collapse/identification loci.** **[LEAN 2026-09-02, sorry-free]** `lean/Containers/Containers/QpfCollapse.lean` (full build 60 jobs / 0 warnings / 0 sorry) machine-checks the abs/repr pattern on the smallest genuine collapse locus: two-leaf bag `Bag₂ X=(X×X)/swap=Sym²X`. `abs=Quotient.mk`, `repr`=`Classical.choose` section (**core Lean has NO `Quotient.out` — this project has ZERO Mathlib**), `abs_repr`=split-surjection law (`Classical.choose_spec`); `abs_map` naturality; `MinimalQPF`/`bag₂QPF`; container `pairC` with `Ext pairC X≅X×X`. The quotiented `swap` IS the proved leaf-symmetry obstruction to `Arr_M` [[effect-coeffect-arrows-first-strength]]. `#print axioms`=`[Classical.choice]`/`[Quot.sound]` only. → `for-collaborator/2026-09-02-lean-qpf-collapse.md`, [[lean-qpf-collapse-abs-repr]]. **DONE 2026-09-03 (LEAN, sorry-free)** the specific `Vec_fd` `({∗},k²)`/`({1,2},k)` collapse: `lean/Containers/Containers/VecCollapse.lean` (full build **61 jobs / 0 warnings / 0 sorry**). As the target note predicted, the clean statement is **NOT** an ACH `Wequiv`/`Mcongr` quotient but a **natural iso of the two extension functors** (`collapseIso : LinNatIso p p'`) — because here `abs` is an **iso** (`pIso`/`p'Iso` to the common cover `Pow₂ X=Bool→X`), the *degenerate* QPF, contrast `Bag₂`'s proper leaf-swap quotient. The linear extension is the **Π-over-shapes** `LExt(S,Pos)X=(s:S)→(Pos s→X)`; the ONLY change from set `Ext=Σ`-over-shapes is `Σ↝Π` — **that shift IS the biproduct collapse** (positions carried by basis types: `k²↦Bool`, `k↦Unit`). `shapes_not_equiv` (**axiom-free**) certifies `p≇p'` as containers (Lemma 1.3) ⟹ machine-checked **Theorem D(ii)** non-injectivity of `⟦−⟧`. `#print axioms collapseIso=[Quot.sound]` (funext only). Registry `linear-containers-vec.json`→`part1-finite-collapse`→`lean-theorem-D-collapse-witness` (`lean-verified`). → `for-collaborator/2026-09-03-lean-vec-collapse.md`. The collapse-pole objects where `⟦−⟧` is non-injective (Theorem D: `({∗},k²)`,`({1,2},k)` both present `X↦X⊕X`; "`◁:=⊗`" a CHOICE) are **exactly QPFs** — quotients/retracts of a polynomial functor, not literal polynomial functors. Avigad–Carneiro–Hudon "Data Types as Quotients of Polynomial Functors" ITP 2019 (`10.4230/LIPIcs.ITP.2019.6`, **deep-read pp.1–10/19; the 09-11 "correction" to `.17` was itself wrong — `.17` is an unrelated Forster–Kunze paper — fixed back to `.6` on 09-12**): `abs:P⟹F` split surjective = the type-theoretic form of `⟦−⟧`-non-injectivity; `Wequiv`/`Mcongr` = the (co)inductive quotients. Live Lean 4: `github.com/alexkeizer/QpfTypes` (WIP). Turns the standing IDENTIFICATION caveat into a constructive Lean pattern; audit target = restate Vec-comonoid/linear-attention results as QPFs/extensions not containers. → `connections/qpf-is-the-tool-for-collapse-loci.md`. Unchecked: does a *specific* collapse container quotient cleanly (a `/lean` pass)? Adjacent flag (outside my territory, → Neil): QPF ≈ Girard **dilator/ptyx** (leanprover Zulip, unanswered).
- **★★ 2026-09-01 (PROVE) — GAP 1 INHABITANT `Set×Vec_fd` PROMOTED TO `proved`; DUAL-NUMBERS PROBE REFUTED; ADMISSIBILITY ⟺ ABSORPTIVE.** `proofs/2026-09-01-gap1-setxvec-proved.md`, verified `scratch/2026-09-01-gap1-verify.py`. **(1)** On the **finite-vec-support locus** `𝒫` (`{s:V_s≠0}` finite; `S`, `A_s` arbitrary), the full `◁` is built — `r=p◁q` with `D=∐_s T^{A_s}`, `C_{(s,f)}=∐_a B_{f(a)}`, `U_{d_0}=⊕_{s,t}V_s⊗W_t` at ONE slot — and an **explicit natural iso** `Θ=(Θ_set,Θ_vec)`: `Θ_set` a bijection with inverse (Set-container composition), `Θ_vec` a coordinate **permutation** matrix (built, verified — *not* a `4=4` collision), naturality checked on both components. Locus is exact (outside: `⟦p⟧` leaves `Vec_fd`; full `Vec`: non-fd positions not tiny ⟹ predecessor Gap 1, OPEN). **Coherence:** no *canonical* monoidal structure (slot choice not natural); a non-canonical one exists; obstruction = non-faithfulness of `⟦−⟧` on Vec = **Thm D**. **(2) Irreducibility — dual numbers REFUTED, and the whole family with them.** **Lemma (fg ⟹ copower-tiny, any ring):** so `fg-R-Mod` (`R` comm. Noetherian) is a **COLLAPSE base** (all objects tiny, `◁=⊗_R`), = `Vec_fd`; **never Gap 1**. `k[ε]/ε²` killed (its `k=R/ε` IS copower-tiny — `Hom_R(k,−)=ker ε` preserves `⊕` — yet non-dualizable): **copower-tiny = finitely generated `⊊` dualizable**; the brief conflated the two. Kills `k[x]`, `ℤ`, glued/pullback module cats too. **(3) Structural payoff: admissibility ⟺ every object ABSORPTIVE** (`X↦[P,⟦q⟧X]` an extension ∀`q`) — per-object localisation of Neil's #1. Gap 1 = the **seam** of extensive(rigid)+additive(flexible); realized by the PRODUCT `Set×Vec_fd`. **Irreducible inhabitant pinned between two OPEN doors:** (i) additive non-fg = predecessor Gap 1; (ii) non-additive disconnected = `γ`-non-injective (only `Set_*`, inadmissible Thm A). **Conjecture 6.2 (decomposition, speculative):** every nice Gap-1 inhabitant = rigid-extensive × flexible-additive ⟹ no irreducible inhabitant among nice bases. Registry `open-middle-region` = **`proved`** (+ children `setxvec-admissible-natiso`, `irreducible-gap1-hunt`, `admissibility-per-object-absorptive`, `conj-absorptive-dichotomy`). [[gap1-inhabited-setxvec-rigid-flexible]].
- **★★ 2026-09-01 (WAKE) — GAP 1 IS INHABITED; THEOREM B IS NOT SHARP (`computed`, now superseded by the PROVE bullet above).** A scoping agent (`scratch/2026-09-01-gap1-hunt.py`) + MacBeth hand-verification found the middle region (admissible ∧ non-collapse ∧ non-cartesian) is **NOT empty**: **`Set × Vec_fd`** is `◁`-admissible, non-collapse (positions `(A,V)` with `|A|≥2` not tiny), non-cartesian (`⊗_k≠⊕`), with a **DISCONNECTED** unit `(1,k)` — exactly what the extensive pole forbids. **So the extensive pole is a SUFFICIENT condition for the one-bit rigidity, not a characterisation.** **Mechanism (hand-verified via hom-tensor adjunction + biproduct collapse, natural isos not cardinality):** the composite `⟦p⟧∘⟦q⟧` splits over the *same* external shape set; the **rigid** (fully-faithful) Set factor *forces* `∐_s T^{A_s}`, and the **flexible** (collapse, non-full — Thm D) Vec factor *absorbs* it because `⊕_i[U_i,Y]=[⊕_iU_i,Y]` collapses finite coproducts of representables to one. `Set×Set` is inadmissible (Prop 9.1) precisely because **both** factors are rigid and force incompatible shape sets. **The operative axis is RIGID vs FLEXIBLE factors — NOT cartesianness, and NOT the idempotent-splitting `I_i⊗I_j≅0` lead (now DEAD:** Theorem B also needs a nonzero terminal, Lemma E2, which a collapse factor lacks — independent of `I_i⊗I_j`). `Rel` reclassified from [OPEN] to **COLLAPSE** (compact closed, all tiny). File `proofs/2026-09-01-gap1-inhabited-setxvec.md`; registry `open-middle-region` promoted `speculative→computed` with `Set×Vec_fd` witness. **Grade `computed`** — PROVE owes full `◁`-construction/coherence + precise summability locus. **Sharper OPEN Q (the live PROVE target):** is there an **IRREDUCIBLE** (non-product) Gap-1 inhabitant? (`k[ε]/ε²`-Mod is the cleanest first probe.) → `state/PROVE.md` 2026-09-01.
- **★★ 2026-08-31 (WAKE) — NOVELTY GATE ON THEOREMS A/B/D: DISCHARGED against DJN, and the verdict is better than "clear".** The standing worry — that Dorta–Jarvis–Niu `2305.05655` use an *indexed* formulation (one index set per component) which would dodge Thm B and scope it to my external-shape construction — is **UNFOUNDED**: their Def 2.6 (p. 87) has a single external **set `I` of positions**, exactly my `S`. The true relation is **`ΣΠC = Fam(ΠC)`** — DJN is *my* construction over a **free product completion**, a base with all small products and essentially **no coproducts**. So **Thm B's hypotheses never hold there, while its conclusion always does** (one-summand object ⟹ connected unit): **DJN is a CONFIRMING INSTANCE, not prior art, and not a counterexample.** Their only base hypothesis is *"let `(C,e,·)` be a monoidal category"* (§3 p. 88); the words *admissible / connected / indecomposable / extensive / lextensive / cartesian closed / coclosure / left adjoint / Vec / pointed* appear **nowhere** in the paper (grep-verified); there is **no** full/faithfulness result for any extension functor and **no** determined-vs-chosen discussion. **★ §6 (p. 95) explicitly poses the base-condition question as future work** — *"it would be instructive to examine which of these properties and structures carry over to `ΣΠC`, **perhaps requiring various conditions on `C`**"* — so **Theorem B answers a question they asked and left open.** That belongs in the introduction, not a footnote. **Citation correction: their comonoid result is Theorem 4.3, NOT 4.2** (4.2 is the *definition* of enriched cofunctor). ⚠ **Two live cautions.** (i) Their `◁` is **STIPULATED**, not derived from `⟦p◁q⟧ ≅ ⟦p⟧∘⟦q⟧` — no extension functor into endofunctors exists for general `C` (their objects are presheaves on `ΠC`, so composition is not type-correct); they justify the name only by reduction at `C=1`. **This is Theorem D's phenomenon observed in the wild.** (ii) **Possible NAME COLLISION on `◁`:** their predicate at `(i,j,a,b)` is `u_{i,a}·v_{ja,b}` — the **outer** predicate multiplied into the inner — whereas mine may carry no outer `P` factor. **Do not write "same `◁`, more general base" until this is settled** (cf. `connections/fusion-versus-identification.md`, COLLISION mode). ⚠ Still OPEN: **Carboni–Lack–Walters** for E1/E2 (likely folklore). Memory [[reference_dorta_jarvis_niu_generalized_poly]]. **2026-09-02 UPDATE — gate re-verified against the primary source now ON DISK** (`/home/agent/papers/djn_2305.05655.pdf`, text `/home/agent/papers/djn.txt`): **CLOSED FAVOURABLY.** Def 2.6 object data = a single **set `I`** of positions (= my shape set `S`); C-objects enter only as fibre predicates, NOT a dependent family over an index object. `ΣΠC = Fam(ΠC)` (their line 132), `ΠC` = free PRODUCT completion (all small products, essentially NO coproducts) ⟹ not lextensive, not assumed CCC ⟹ **Thm B's hypotheses NEVER hold over `ΠC`**; DJN neither constrains nor challenges Thm B — **not prior art, not a counterexample.** Grep-confirmed: they prove **no full/faithfulness for any extension functor** (only the standard `C↪ΠC`/`C↪ΣC` embeddings); §6 p.95 poses the base-condition question as future work. The ◁-weighting/outer-factoring caveat (Def 3.5, `u_{i,a}·v_{ja,b}`) is representation-level and does **NOT** affect the gate — but must be settled before any "same `◁`, more general base" claim.
- **★★ 2026-08-31 (WAKE) — GAP 1 HAS A LIVE CANDIDATE, and it was in my own store the whole time.** Gap 1 (admissible ∧ non-collapse ∧ non-cartesian) was recorded as having *no example and no proof of emptiness*, with the idempotent-splitting lead marked **"Not attempted"** verbatim in seven places. A store-search found **Artin gluing `Gl(T) = (Set ↓ (−)²)`** in `proofs/2026-08-26-copowers-gap-writer-monad.md` §4(v) — written **four days before Theorem B existed** and never connected to Gap 1: *"a CCC with coproducts, **non-extensive** … `C(I,κ·I)=κ` — **(A) holds** … `C(I,X⊔Y)=C(I,X)⊔C(I,Y)` — **(C) also holds**"*, with a working script `scratch/2026-08-26-gluing-copower-check.py`. So it is **ccc + cocomplete + NON-extensive + unit CONNECTED**, and its **`◁`-admissibility has never been tested**. The same §4 census already kills every other candidate through **(A)**: `Vec`, `R-Mod`, `CMon`, `Set_*`, `Rel` (`C(1,∐_κ1)=2^κ ⊋ 2κ`), quantales/frames — **do not re-derive it**. **★ Its value is the OPPOSITE of the first guess:** its unit is *connected*, so it can never separate Thm 1's converse; it is a candidate for **admissible-but-NOT-lextensive** — a base where Thm B's *conclusion* holds but its *hypotheses* fail, which would show **Theorem B is not sharp** and demote the extensive pole from a characterisation to a *sufficient condition*. A strengthening, not a defeat. Registry node `open-middle-region` (`speculative`, `file: null`, approach ends *"NOT ATTEMPTED."*) is the target — **promote or refute, do not duplicate**. → `state/PROVE.md` 2026-08-31; memory [[gap1-artin-gluing-candidate]].
- **★★ 2026-08-31 (PROVE) — THE GAP-1 CANDIDATE IS DISQUALIFIED ON BOTH COUNTS, and the by-product is bigger than the target.** `proofs/2026-08-31-gluing-inadmissible-pi0.md` (`proved`). **(1) `Gl((−)²)` is a TOPOS, not non-extensive.** `(−)²=Set(2,−)` is right adjoint to `2×(−)`, hence lex; Artin gluing of a lex functor between toposes is a topos (MacLane–Moerdijk A2.1.12), so `Gl((−)²)` is a **Grothendieck topos — infinitary extensive, `⊗=×`**. The "non-extensive" tag in `2026-08-26` §4v was an ERROR (it conflated "`T` doesn't preserve coproducts" with "`Gl(T)` not extensive"); the file's `(A)∧(C)` computation stands and is an instance of its own Thm 3.1. **So `Gl` is NOT in Gap 1 — it's on the extensive pole. Gap 1 stays empty of known inhabitants.** (extensivity verified: 0 failures / 200+377 random diagrams.) **(2) `Gl((−)²)` is `◁`-INADMISSIBLE** (Theorem G): Lemma S fails at `P=K` (single edge), `T=2`. Objects of `Gl((−)²)` are **graphs** (`B` vertices, `A` edges, `β:A→B²`); the product is the **graph tensor product**; `π₀ ⊣ (−)·1_C` is connected-components. Lemma S ⟺ **`π₀` preserves finite products** (Prop 3.1), and by **Weichsel's theorem** the tensor of two connected bipartite graphs has 2 components: `π₀(K×K)=2≠1`. Computed explicitly: **`[K,2·1_C] ≅ 2·1_C ⊔ K`** — two looped vertices (the 2 points) plus a **bald edge** (a component with no loop), the exact summand that is not a copower of `1_C`. **(3) THE COROLLARY — connectedness is NECESSARY but NOT SUFFICIENT for admissibility.** `Gl` has a **connected** unit (its POINTS functor `C(I,−)=β⁻¹(Δ)` preserves coproducts) yet is inadmissible. **First base separating connected from admissible, and it sits on the extensive pole.** The separation is two different Set-functors: POINTS (preserve coproducts = connected) vs COMPONENTS `π₀` (must preserve products = Lemma S) — and `π₀` fails. Answers predecessor **Gap 2** ("is Lemma S sufficient?") in the strong negative. **Third inadmissible base** (with `Set×Set`, `Set_*`), via a **third mechanism**. Registry `open-middle-region` subtree (`gl-is-topos-extensive`, `lemmaS-iff-pi0-multiplicative`, `gl-inadmissible-weichsel`, `connected-not-sufficient-for-admissible`); collaborator note `for-collaborator/2026-08-31-gluing-inadmissible.md`; memory [[gl-inadmissible-pi0-multiplicativity]].
- **★★ 2026-09-09 (dream) — THE QUESTION HAS THE WRONG SHAPE FOR `◁`, and this cycle's three PROVE results are the proof.** For `⊗` (T2) and for the fibrational logic the graded question is real. For `◁` nothing breaks *gradually*, because `◁` mostly **does not exist**: on an infinitary-lextensive ccc base, admissibility **already implies** unit-connectedness (Thm B) ⟹ fullness of `⟦−⟧` (T1) ⟹ left adjoint for every `q` (Thm 1) — **one bit, and the pole is rigid: every base there behaves exactly like `Set`, or `◁` isn't there** (`Set×Set`, `Set_*` both inadmissible). Off that pole `◁` must be **stipulated** (`◁ := ⊗`) and by Thm D `⟦−⟧` stops being injective on objects, so left-adjointness is a property of **the choice**, not of `C`. This *explains* the anti-diagonal rather than recording it: a forced structure is rigid in one variable, a chosen one in the other. **The only place real generality could live is Gap 1 (admissible ∧ non-collapse ∧ non-cartesian) — named, and so far empty.** → `connections/admissibility-is-the-real-generality-question.md`. **Novelty gate — DJN half now CLOSED FAVOURABLY (2026-09-02, verified against the PDF on disk `/home/agent/papers/djn_2305.05655.pdf`): `ΣΠC = Fam(ΠC)` over the coproduct-free product completion `ΠC` (Def 2.6 = single position SET, not a dependent family), so Thm B's lextensive-CCC hypotheses never hold there — DJN dodges Thm B but is thereby neither prior art nor a counterexample; no full/faithfulness for any extension functor; §6 poses the base-condition question as future work. ◁-weighting caveat (Def 3.5) carried, representation-level only. STILL OPEN: Carboni–Lack–Walters for E1/E2 (likely folklore).**
- **T1 (flagship) — DONE, `proved`.** `⟦−⟧ : Fam(C^op) → C-[C,C]`, `⟦S,P⟧=∐_s[P_s,−]`, is **fully faithful ⟺ the monoidal unit `I` is CONNECTED** (`C(I,−)` preserves small coproducts) — and this is **NOT extensivity** (`Set×Set` is (l)extensive yet not full; unit `(1,1)` disconnected: 2 container morphisms vs 4 nat transfs). "Faithful always" is also FALSE (Vec zero-collapse). Registry `fullness-unit-connectedness`; topic `topics/fullness-unit-connectedness.md`; memory [[fullness-unit-connectedness]]. Method: enriched co-Yoneda, set-level.
- **T1 copowers gap — DISCHARGED, `proved`.** Does `C(I,−)` preserving copowers `κ·I` upgrade to preserving ALL coproducts? (A) copower test ⟺ induced Set-monad `C(I,(−)·I)` is the **writer monad** `(−)×End(I)`; (C) ⟺ EM-comparison `K:C→End(I)-Set` preserves coproducts; **EXTENSIVE C ⟹ (A)⟺(C)⟺ I indecomposable & non-initial** → the one-parameter copower test **decides fullness over Set / all toposes / Set×Set**. Additive/thin bases (Vec, R-Mod, Rel) fail (A) already. Registry `copowers-gap-writer-monad`; memory [[copowers-gap-writer-monad-extensive]]. Residual (Q′): a non-extensive closed base with (A) but not (C) — `in-progress`, ruled out for all standard bases, not constructed.
- **T3 (change of base = change of enrichment) — PROVED.** Lax `F:Set→C` moves extension into `C`-enriched functors; free-vec `k` not connected ⟹ linearizing loses `∐⊊⊕`. Vec-enrichment = the invariant view (confirms Neil).
- **T2 (closedness) — DONE, `proved` (2026-08-26, PROVE session; answers Neil UID 124 "do T2 generally").** Registry `t2-day-closedness-famcop`; proof `proofs/2026-08-26-t2-day-closedness-famcop.md`; memory [[t2-day-closedness-famcop]]. **Reduction (Thm 1.1):** `⊗` on `Fam(C^op)` closed ⟺ `Φ(Z)=∏_t∐_r C(M_r,Z⊗Q_t):C→Set` is **familially representable** for all families (equivalently `Θ(Z)=∏_t C(A_t,Z⊗Q_t)`). Two conjuncts: **(A)** single-factor `Z↦C(A,Z⊗Q)` fam-rep; **(B)** product-closure (`∐_t N_t` exists in `C`). **Two sufficient regimes w/ explicit internal homs:** (I) `C` cartesian closed ⟹ closed unconditionally (recovers Set/Poly Dirichlet hom, Niu–Spivak Ex 4.78); (II) all `Q_t` dualizable + coproducts ⟹ closed, hom `=(R^T,(∐_t M_{ρ(t)}⊗Q_t^*)_ρ)`. **Linear dichotomy (Thm 3.2, THE DELIVERABLE):** over Vec single-factor fam-rep ⟺ `Q` fd (Lemma 3.1); so `⊗` closed on `Fam_fin(Vec_fd^op)` but **NOT on `Fam(Vec_fd^op)`** (infinite shapes break (B): `Φ=|Z|^ℕ=Vec(k^{(ℕ)},−)` needs ∞-dim rep) **NOR on `Fam(Vec^op)`** (∞-dim positions break (A)). **HONEST CORRECTION:** the pre-registered prediction ("holds over full Vec, fails over Vec_fd") was WRONG — fails over BOTH by *dual* mechanisms (cocompleteness vs dualizability); load-bearing conjunct NAMED = simultaneous dualizable-and-summable, only on the finite/fd corner. Day LNM 137 Thm 3.3 CONSISTENT but not directly applicable (enriched domain `A=C` LARGE ⟹ representability refinement = the T2 content DJN omit). T2 closedness ⊥ T1 fullness (independent axes: `Set×Set` closes `⊗` yet `⟦−⟧` not full). Verified 2000/2000 (Vec_fd) + 2000/2000 (Set) random families.
- **T4-left (`◁` LEFT-closedness) — DONE, `proved` (2026-08-27, PROVE; answers Neil UID 125 "left closedness too, if possible").** Registry `t4-left-closedness-lhd-famcop`; proof `proofs/2026-08-27-t4-left-closedness-lhd-famcop.md`; collaborator note `for-collaborator/2026-08-27-t4-left-closedness-lhd.md`. **Disambiguated first:** target = `◁`-**closure** (right adjoint to `(−)◁q`, obstructed over `Set` by Workers Thm 2), NOT the known `◁`-**coclosure** (Meyers; Niu–Spivak Prop 6.57: `⌜q/−⌝ ⊣ (−)◁q`, a **LEFT** adjoint to the SAME variable, a `Lan`) — *variance corrected 2026-08-30; this line previously said "right adjoint to `q◁(−)`", wrong on both counts*. **Mechanism = COLLAPSE (Prop 2.1):** positions **tiny** (`[Z,−]` preserves ∐; = dualizable; = fd in `Vec`) ⟹ `⟦p◁q⟧=∐_{s,t}[P_s⊗Q_t,X]=⟦p⊗q⟧`, so **`◁=⊗`** (my proved Prop 4.1's Vec_fd instance, generalized) — `◁` becomes SYMMETRIC, left hom = right hom = the T2 `⊗`-hom. **Thm 3.1:** `◁`-left-closed on **`Fam_fin(Vec_fd^op)`** with `[(T,Q)◁(R,M)]=(R^T,(⊕_t M_{ρ(t)}⊗Q_t^*)_ρ)`; FAILS on `Fam(Vec_fd^op)` (∞ shapes, conjunct B) and `Fam(Vec^op)` (∞-dim positions break tininess = conjunct A). **THE CROWN (inverts T1/T2):** over extensive `Set` the distributive law `[Z,∐_t B]=∐_{τ:Z→T}∏B` branches into `T^Z` ⟹ `◁≠⊗`, `◁`-closure OBSTRUCTED; over linear `Vec_fd` the hom is additive ⟹ collapse ⟹ closed. **Non-extensivity — the OBSTRUCTION of T1(fullness)/T2(⊗) — is here the REPAIR; extensivity ⊥ `◁`-left-closedness.** Neil's hint (left-Kan preserves representability ⟹ coproducts) = T2's rigid dualization `C(M_r,Z⊗Q_t)=C(M_r⊗Q_t^*,Z)` then `∏_t⤳⊕_t`. Synthesis of 3 proved results (Prop 4.1 + T2 + Workers Thm 2); positive verdict holds ONLY where `◁` degenerates (honest). Verified 20000/20000 collapse + 3000/3000 closure adjunction (F_2). Set escape: `◁`-closure exists over Set iff `q` monomial. Memory [[t4-left-closedness-lhd-famcop]].
- **Prior-art guardrail (MUST cite, DON'T re-claim) — ⚠ RESCOPED 2026-08-31:** Dorta–Jarvis–Niu `2305.05655` "Monoidal Structures on Generalized Polynomial Categories" (EPTCS 397, 2023) already builds **⊗** over a general base — mine matches theirs *exactly* under `ΣΠC = Fam(D^op)`, `D=(ΠC)^op` — and a composition product **`◁_DJN` which is NOT my `◁`**. ⚠ **2026-08-31 scope correction:** their composition product `◁_DJN` is **weighted** — direction object at `(i,j:A_i→J)` is `∏_{a∈A_i}∏_{b∈B_{ja}}(u_{i,a}·v_{ja,b})` (Def 3.5 / Lemma 3.6, p. 89) whereas mine carries **no outer `u` factor** — so `◁_DJN = ◁` **only at `C = 1`**. Witness: `C=2`, `·=∧`, `e=⊤`, `p=∑_{i∈1}∏_{a∈1}⊥`, `q=∑_{j∈1}∏_{b∈1}⊤` ⟹ DJN `⊥`, mine `⊤`; over `C=[0,∞]` (their §5) DJN `5+3=8`, mine `3`. My defining property `⟦p◁q⟧≅⟦p⟧∘⟦q⟧` is *unavailable* on `ΣΠC` for `C≠1` (`E(p):(ΠC)^op→Set` is not an endofunctor). Their `⊗` **does** match mine exactly, so only `◁` breaks. Their **Theorem 4.3** (comonoids in `◁_DJN` ≃ small `(ΣC^op,e,⊙)`-enriched categories + enriched cofunctors; their Def 4.2 is the *definition* of enriched cofunctor) therefore no longer straightforwardly subsumes my Vec-comonoid/algebroid. ⚠ **OPEN QUESTION, `speculative` (2026-08-31).** Whether the `Vec`-comonoid classification is subsumed by DJN **Theorem 4.3** is now OPEN: their `◁` is weighted and coincides with mine only at `C = 1`, and the two comonoid classifications visibly differ (families of `k`-algebras vs all enriched categories). The concession was made on the assumption that the operations agreed. **This is not a restored novelty claim — it is an unresolved question, and it must be settled before anything is claimed either way.** ⚠ **And a counter-consideration is on file against my own finding** (registry node `djn-subsumption-reopened` in `proofs/registry/linear-containers-vec.json`): wherever my `◁` is *defined* off `Set` — the tiny/collapse locus — its direction object **is** `P_s⊗Q_t`, so the outer position **is** multiplied in there (Prop 4.1 over `Vec_fd`); "no outer factor" is the *`Set`* reading, where `P_s` enters as an index. That does not restore agreement (DJN's outer factor is matched **diagonally**, `u_{i,a}·v_{ja,b}`, which is what makes their `µ_{b,c}` a weight-composition *along a path*; a `⊗`-style outer factor would run over all pairs and give a **third** formula), but the transport of my `◁` into `ΣΠC` is a **choice**, not forced, and settling that is part of the question. My genuine delta = exactly what they omit: fullness (T1), closedness (T2), Set→Vec change-of-enrichment (T3). Boundary: Gambino–Kock `0906.4931` — Vec not LCCC ⟹ Fam(Vec^op) outside classical machinery; theirs is fully-internal poly (holds over Set×Set), mine is mixed `Fam(C^op)` (external ∐). Memory [[reference_dorta_jarvis_niu_generalized_poly]], [[feedback_neil_fam_cop_generality_priority]].

- **T4-left successor / Q2 falsifier — DONE, `proved` (2026-08-30, PROVE).** Registry `pra-vs-probe-method`; proof `proofs/2026-08-30-pra-vs-probe-method.md`. **`1 ◁ q ≅ 1` for every `q`** (`y^0` terminal because `∅` is terminal in `Set^op`), so Weber's p.r.a. slice `Cont/T1` collapses to `Cont` and **p.r.a. ⟺ has an honest LEFT adjoint**. It does, for every `q`, unconditionally: `F_q(R,U)=(R, ρ↦⟦q⟧(U_ρ))=Fam(⟦q⟧^op)`. **PRIOR ART — CITE, DO NOT CLAIM:** this is Josh Meyers' `◁`-coclosure, Niu–Spivak **Prop 6.57** (`⌜q/p⌝=Σ_i y^{q(p[i])}`), also SGF Prop 2.16 / Spivak `2202.00534` §5; my own `equivalence-chain` node `seq-right-coclosure` already said "NOVELTY: NONE … DO NOT CLAIM". **The result that IS mine: the slice-collapse observation + the consequent REFUTATION of the crown re-filing** (see below). `Set`-distributivity is used exactly once, at `(†)`; that is the successor.
- **★ CROWN RE-FILING REFUTED (2026-08-30).** The conjecture that my "one functional, many probes" method IS Weber-p.r.a.-failure across a slice is **dead**, by the falsifier named in advance. Diagnosis: **wrong adjoint side** — p.r.a. is *left*-adjoint existence (unconditional); all three probe instances test *right*-adjoint/closure existence (`|T|=1` over `Set`, summability over `Vec`). A thrice-published unconditional adjunction cannot discriminate between them. **The method survives as mine, `speculative`, three instances, NOT underwritten by Weber; the fusion mechanism remains EXTENSIVITY.** See `connections/one-representability-functional-two-probes.md` §"Falsifier: RUN".
- **★ LEFT-ADJOINT SUCCESSOR over `Vec` — DONE, `proved` (2026-08-30, second PROVE of the day).** Registry `left-adjoint-over-vec`; proof `proofs/2026-08-30-left-adjoint-over-vec.md`; code `scratch/left-adjoint-vec/verify.py`. **Three verdicts.**
  **(1) The predicted summability gate is REFUTED.** On the collapse locus (`◁=⊗`) the left adjoint to `(−)◁q` exists **iff `|T|=1`**, and the comparison map already fails at `dim P_s = 1` with `|T|=2` and `T` **finite** — where the two sides even have *equal cardinality* (4=4) but the canonical map double-counts `0` and misses `e_0+e_1`. **A cardinality-only check would have missed it. NO fourth occurrence of the summability pattern.**
  **(2) The `Set` proof's load-bearing step was MISIDENTIFIED (by me, in the predecessor, and by the brief).** `(†)` is Set-distributivity *as written*, but the adjunction never needs it. **Theorem 1 (base-general, presentation-independent):** if the monoidal unit `I` is **CONNECTED** (`C(I,−)` preserves ∐) then `F_q = Fam(⟦q⟧^op) ⊣ (−)◁q` for **every** `q` over **any** closed symmetric monoidal cocomplete base — four lines, `γ` used twice, no distributivity, no choice of presentation of `◁`. Distributivity of `Set` is used in *constructing* `◁` over `Set` (it makes `Σ_s T^{P_s}` the shape set), not in the adjunction. **`γ` is verbatim T1's comparison map ⟹ T1 (fullness of `⟦−⟧`) and the `◁`-coclosure are THE SAME LEMMA APPLIED TWICE.** So the fusing agent on the LEFT side is **unit-connectedness, not extensivity** — and `Set×Set` (lextensive, unit disconnected) already separates them for T1.
  **(3) p.r.a. and left-adjointness COME APART off `Set` — the brief's (C) fires.** `1 = ⟨0_C⟩` and `1◁q = (T,0) ≇ 1` for `|T|≥2`, so Weber's slice does **not** collapse. It does not need to: `Fam/(T,0) ≅ ∏_{t∈T}Fam(C^op)`, `(L_q)_1 p = (p⊗Q_t)_t`, left adjoint `(r_t)_t ↦ ∐_t F_{Q_t}(r_t)`. **`L_q` is p.r.a. for every `q` over `Vec` too**, while having an honest left adjoint only at `|T|=1`. The predecessor's "p.r.a. ⟺ left adjoint" was **base-specific**; the responsible feature is that **`Set` has no zero object**, and the exact measure of the gap is the object `1◁q`.
  **THE ANTI-DIAGONAL (the actual crown).** Left adjoint: `Set` always / `Vec_fd` iff `|T|=1`. Right adjoint (`◁`-closure): `Set` iff `|T|=1` / `Vec_fd` iff `#{t:Q_t≠0}<∞`. **The conditions swap sides.** And inside `(V)⊊(C)⊊(F)` (`fibredness-vs-left-closure`, over `Vec_fd`), **left-adjointness ⟺ verticality (V)** — the *bottom* rung — whereas over `Set` `(V)=(C)=(F)={|T|=1}` and left-adjointness is unconditional at the top. **The chain inverts end to end.**
  **Two necessity proofs, and they differ:** (a) terminal object (`1◁q=(T,0)`); (b) binary products (`|T|=|T|²`), free of any zero object. For `q=(ℕ,k)` over `Vec_fd` **(b) is inconclusive AND the obstruction genuinely vanishes** — `L_q` preserves all *binary* products but not the terminal object. **The EMPTY product is the binding probe** (cf. [[containers-preserve-connected-not-empty]]).
  **By-product (Prop 9.1, found probing the converse):** `Fam((Set×Set)^op)` is **not closed under `◁` at all** — `⟦E,N⟧(1_C) = E·1_C` is a copower of the terminal object hence *diagonal*, but `⟦p⟧(T·1_C)=(T^A,T^B)` is not, for `p=⟨(1,2)⟩`, `|T|=2`. So the intended separator base is unavailable; `Set` and `Vec` pass the criterion for *opposite degenerate* reasons (`1_C` a generator vs `1_C=0`). **Scope caveat: this is about MY external `Fam(C^op)`. DJN `2305.05655` — CHECKED 2026-09-02 against the PDF on disk — is `Fam(ΠC)` over the coproduct-free product completion `ΠC`, where this cannot arise as a challenge to Thm B (no lextensive base, and no full/faithfulness result for any extension functor); DJN not prior art, not a counterexample.**
  **Standing caveat carried:** `◁ := ⊗` on the collapse locus is a **definition** (over `Vec`, `⟦−⟧` is not faithful, so `(T,0)` and `({∗},0)` present the same constant-`0` functor). Necessity proof (b) is independent of it for finite `T`. **Novelty gate OPEN on Theorem 1's general-base form** (the `Set` instance is Niu–Spivak Prop 6.57/Meyers, gate closed).
- **★★ ADMISSIBILITY + the CONNECTEDNESS CONVERSE — DONE, `proved` (2026-08-30, THIRD PROVE of the day; closes Gap 3 of the bullet above).** Registry `left-adjoint-over-vec` subtree `gap3-converse`; proof `proofs/2026-08-30-admissibility-and-the-connectedness-converse.md`; code `scratch/connectedness-converse/verify.py`; collaborator note `for-collaborator/2026-08-30-admissibility-and-connectedness-converse.md`. **Question: is CONNECTEDNESS of `I` NECESSARY for `(−)◁q` to have a left adjoint? Four results.**
  **(A) The §9bis dichotomy is REFUTED by `Set_*`.** Pointed sets under smash have a **zero object** (`1_C ≅ 0_C = ∗`), so the copower criterion is vacuous — yet `Fam(Set_*^op)` is **not** closed under `◁`: for `p=⟨S^0∨S^0⟩`, `q=({1,2},(S^0,S^0))`, `⟦p⟧⟦q⟧X=(X∨X)^2`, and matching `|⋁_d[N_d,X]| = 1+Σ_d((m+1)^{n_d}−1)` forces `b=4` summands with `n_d=2` and `a=−4` with `n_d=1`. **A zero object does NOT buy admissibility.** `Set_*` is on **neither** pole: not extensive (wedge identifies basepoints; `γ` fails by NON-injectivity), not additive, not collapse (`S^0` tiny but `S^0∨S^0` not). It is the base where both known mechanisms switch off.
  **(B) THEOREM B — on the extensive pole, admissibility FORCES connectedness.** `C` nontrivial, **infinitary lextensive + cartesian closed**, `Fam(C^op)` closed under `◁` ⟹ `1 = I` is **connected**, hence (Thm 1) `(−)◁q` has a left adjoint for **every** `q`. Mechanism: `1` disconnected ⟹ `1 ≅ A⊔B` nontrivially ⟹ `C ≃ C/A × C/B`, and `[A,T·1] = (T·1_1, 1_2)` while copowers of `1` are `(E·1_1,E·1_2)` with **ONE external `E`** — forcing `E=1` then `|T|=1`. Ingredient: **Lemma E1** (lextensive, `1 ≅ 1⊔Z` ⟹ `Z ≅ 0`; `θι_1` is a map to the terminal hence `id`, then disjointness vs. pullback-along-identity). **So on that whole pole a counterexample to Thm 1's converse CANNOT BE POSED**, and `I` connected ⟺ `◁` exists ⟺ left adjoint for every `q`. Subsumes and relocates Prop 9.1 (`Set×Set`): visible already at `p=⟨A⟩` for `A` any nontrivial summand of `1`.
  **(C) LEMMA D — on the collapse pole the unit is ALWAYS disconnected.** Every object copower-tiny (the T4-left Prop 2.1 hypothesis) ⟹ `I` disconnected, by a diagram chase with **no cardinality argument**: connectedness would make `(d,f)↦ι_d f` a bijection `C(I⊔I,I)⊔C(I⊔I,I) → C(I⊔I,I⊔I)`, giving `id=ι_1 f`, hence `ι_2 = ι_1(fι_2)`, hence two tagged elements with one image. (`D′`: a zero object kills connectedness directly.) With Thm 2 this closes Gap 3 on the collapse pole — the left adjoint fails at the smallest witness `|T|=2`. **Bonus:** `κ_{B,Z}` on the collapse locus **IS `γ` with probe `B`**; connectedness is `B=I`, and the **fatal probe is `B=0_C`** (LHS `|T|`, RHS `1`) — a two-line base-general reproof of Thm 2's necessity, and the house *one-functional-many-probes* method with the probe ranging over `C` itself.
  **(D) THEOREM D — why the converse cannot be a statement about `C` alone.** `I` connected ⟹ `⟦−⟧` full and faithful (T1) ⟹ injective on objects up to iso ⟹ `p◁q` is **determined** and left-adjointness is a property of `C`. `I` disconnected: over `Vec_fd`, `({∗},k^2)` and `({1,2},k)` both present `X↦X⊕X` and are **not** isomorphic — so `◁` is a **CHOICE**. **Theorem 1's hypothesis is simultaneously what makes its converse true and what makes its converse MEANINGFUL.** Explains why the predecessor's caveat (D) can never be discharged, and identifies §5.2(b) (binary products) as the re-choice-robust necessity argument, §5.2(a) (terminal) as the convention-sensitive one.
  **TRICHOTOMY replacing §9bis:** extensive pole (admissible ⟹ connected ⟹ left adjoint always) / collapse pole (admissible, disconnected, left adjoint iff `|T|=1`) / **inadmissible** (`Set×Set`, `Set_*`). **Gaps:** the middle region (admissible, non-collapse, non-cartesian) is uncharted — lead: `I ≅ I_1⊔I_2` gives `X ≅ (X⊗I_1)⊔(X⊗I_2)`, an idempotent-splitting shape; is Lemma S *sufficient*?; **novelty gate for Thms A/B/D — DJN half CLOSED FAVOURABLY (2026-09-02, verified vs the PDF on disk): `ΣΠC=Fam(ΠC)` over the coproduct-free product completion, so Thm B's lextensive-CCC hypotheses never hold there ⟹ DJN not prior art and not a counterexample; ◁-weighting caveat (Def 3.5) carried, representation-level only.** E1/E2 still ungated (likely folklore — Carboni–Lack–Walters).
- **Pradic–Price `2601.15420` attribution — CLOSED 2026-08-30, and it closed against me.** Their `shape` fibration IS my `π` ("exactly the fibrewise opposite of the codomain fibration", §2.2 pp. 8–9); Def 13.1 = my (F) with strict equality ⟹ PP-fibred ⟹ my (F), so Thm A holds a fortiori. **My "bonus" (`◁` right-variable fibred, base functor `⟦q⟧`) = their Lemma 15** (p. 14, proof p. 31); **both-variables cartesian-preservation = Niu–Spivak Prop 6.88**. Neither is mine. **What IS mine: Cor A′ — Theorem A supplies a PROOF of their Remark 16 (p. 14), which they assert with no proof anywhere**; and Theorem B, which is **outside their scope** (standing hypothesis p. 7: "all categories in sight shall be lextensive"; `Vec_fd` is not). **★ That hypothesis is direct corroboration of the extensivity thesis: prior work assumes the very condition under which the seams fuse, so it structurally cannot see the separation.** Notation: PP `Q ⋆ P` = my `P ◁ Q`; the flip cancels, "left variable" agrees across PP/BHM/me.
- **Weber Q4 — ANSWERED NEGATIVE 2026-08-30: DIFFERENT AXES.** `Fam` familial-but-not-opfamilial (Weber TAC 18(22), prose between Cor 5.14 and Prop 5.15) is **not** my one-sided joint BC seam. His obstruction is a failure of the **diagonal** `S→S×S` over a non-singleton fibre (`x≠y`), with **no coproduct anywhere**; mine is `∐∏≠∏∐` interchange. Def 5.2 fn 15 confirms opfamiliality is a pure **2-cell orientation** axis. Fourth consecutive "surely these are the same" → **no**. Residue (INFERRED, not load-bearing): Weber's next sentence names `X ↦ Fam(X^op)^op` as opfamilial-not-familial — the **fibrewise-op move**, possibly placing `Cont` on his opfamilial side. `questions/weber-pra-boundary.md` Q4.

### Front B — Vec-attention five-lineage unification note (WRITE; greenlit under Neil's hard depth bar, UID 121)
Five independent categorical accounts of a single attention layer, none using container language, none citing another: **Vertechi** (parametric spans, agent-summary) · **O'Neill** `2501.02931` (Para(Vect) free monad, deep-read) · **Hedges** (applicative transformers, cybercat 2025-02-12) · **Maruyama** `2511.18417` (Vect-valued presheaves) · **Mahadevan-KET** `2605.27259` (weighted Kan extension; its §10.6 caveat — coend is a genuine Kan extension only in the *representable* case — independently rediscovers my linear/nonlinear boundary). Memory [[vec-attention-precedents-need-unification]].
- **The uncontested delta = the DEPTH-composition law all five omit:** stacking = free ◁-monoid = tensor algebra `T(AttP)=⊕_L AttP^{⊗L}` (proved: [[oneill-free-monad-tensor-algebra-linear-container]], corrects O'Neill Thm 3.2 to Adámek partial-sums `⊕_n Fⁿ`; F pointable not well-pointed; residual = unit η). The quantitative form = **degree exactly `3^L`** for a live depth-L stack ([[linear-attention-odot-degree-3L]]), which is why the tower cannot collapse to one Vec-matrix. Neil's acceptance criterion: must "tell a practitioner something new" — single-layer restatement fails; only the depth law clears it.
- WRITE shipped (three lineages deep-read/cited, two footnoted as agent-summary; unification stated as *proposal*, only O'Neill proved-inside; DJN Thm 4.3 credited). Companion `for-robin/2026-08-25-attention-unification-note.md`.

### Front C — Indexed containers (Neil's next-direction pointer for "states that let prompts vary") — SPECIALIZATION RESOLVED 2026-08-27
De Pascalis–Uustalu–Veltrì `2509.25879` "Monoid Structures on Indexed Containers" (LSFA 2025): `Set^I`-monads with container-extent underlying functors ≃ **Indexed Container Monoid Structures** (**Def 3.3 + Lemma 3.2**, NOT a "Thm 3.5" — old cite corrected) — a *combinatorial* recipe (unit shape `e`, shape-mult `•`, index-tracking `↑/↖/↗`, coherence `Pe_≡` with no non-indexed analogue). More general than Gambino–Kock **precisely by dropping cartesianness** (η,μ need not be cartesian nat transfs). **RESOLVED (2026-08-27 WAKE):** at trivial `I=1` the two genuinely-indexed data `Pe_≡` and `↑` **trivialize**, leaving exactly `(S,P,e,•,↖,↗)` = Uustalu's non-indexed characterization = **a ◁-monoid acting by `⟦S◁P⟧∘(−)`** = my `T^Σ_M=M◁−` [[lifting-general-M-and-monoid-comonoid-unification]]. So **my classification IS the I=1 fibre of their ICMS** — recognition, NOT new maths, do not re-claim. Note `connections/my-Mtriangle-is-I1-fibre-of-indexed-icms.md`; memory [[my-Mtriangle-is-I1-fibre-of-indexed-icms]]. **The genuine DELTA candidate:** their "cartesian(GK) vs general" one-step is the coarse version of my **strict 4-level chain** `λ-inv⊊non-branch⊊cartesian⊊∏-Mendler` [[crown-tfae-strict-chain]] — they do NOT stratify inside "general", I do. **Open (real Front C):** does my branching stratification **lift index-wise** to `IC_I`, and do `↑/Pe_≡` interact with leaf-symmetry? = Neil's "prompts vary" generality. `/prove` or `/expository` target.

### Rick front — Ext-support ↔ Schur-rank strata correspondence
My proved Mackey/Shapiro Ext-support dictionary maps stratum-for-stratum onto Rick's Schur-rank statistic `r(Ψ(f))`: **rank-1 / per-Schur / c-invariant ↔ single transverse coset ↔ degree-0 concentration ↔ 0-dim support** ("restrict to a subvariety"); **multi-Schur / Pieri / grows-in-c ↔ coincident intersections ↔ surviving tower `h·Hⁿ(A∩B)` ↔ positive-dim support `V_r(M)∩V_r(N)` ↔ Benson–Carlson–Rickard locus.** WRITE shipped: `expository/support-variety-schur-rank-correspondence` + strata table + D₈ `A=B=⟨s⟩` witness (`[3,2,2,…]`, deg0=3>tower=2, one transverse coset) + the **honesty caveat**: "cancellation" names a *different invariant* on each side (his = top-weight support loss; mine = `deg0−Ext¹=#transverse cosets`) — tabulated separately, NO bijection claimed. **Reply SENT 2026-08-26** (`support-variety-schur-rank.pdf` attached, CC Robin) answering his requests 1–3. **Rick's side is now a proved theorem** (emails UID 122/123): his factorizing EGF `F=A·B` (Day 131) + density theorem giving support `⌊(b+2)²/4⌋ = A002620(b+2)` (Day 133), sign `(−1)^{x₁+x₃}`; he confirms my direction-of-cancellation caveat is right and registers the D₈ `[3,2,2,…]` as his negative test. NO RUSH (FPSAC 2027 deadline Nov 15); next thread = a candidate `(A,B)` family realising his e₁ vs e₃ sweeps + the heuristic `c ↦ B(c)` subgroup-move (my next prove target if he supplies it). NB Rick CC's Clio (not on my allowed-contacts list) — reply to Rick + CC Robin only, never reply-all. Memories [[emergent-holonomy-is-ext-degree0]], [[d8-nonabelian-ext-tower-computed]].

### Front D — "Three approaches to containers over a base" survey (Neil UID 125, 2026-08-27) — ORIENTING
Neil greenlit T2 + reframed AWAY from grants: "make you the most knowledgeable agent in the world on containers and their applications." Wants a **few days contemplating three approaches** to *containers in a category*, with **Fam(Vec^op) as the running core example**: (1) **external `Fam(C^op)`** (my front; external Set-∐; reaches Vec directly); (2) **indexed/dependent-polynomial `I←P→S→O` with Σ-Π-Δ over an LCCC** (Gambino–Hyland/Kock; `I=O=1`+Set⇝LCCC; Neil expects extensive); (3) **fibrational** (positions as a fibration over shapes; comprehension; "logic feels more fibred"). **My discriminator thesis (orientation note done):** extensivity + local-cartesian-closure of the base is the separating axis; **Vec has NEITHER** (`∐⊊⊕` non-extensive AND no internal `Π` ⟹ non-LCCC), so approach (2) *cannot even form its Σ-Π-Δ semantics over Vec* and only (1) reaches Fam(Vec^op) — the honest reason Neil says Fam(Vec^op) "needs the first approach." (3) is the referee: (1)=family fibration, (2)=codomain fibration; non-extensive/non-LCCC bases are where they diverge. Orientation `scratch/2026-08-27-three-approaches-containers-in-category.md`. Enriched-Yoneda dependency flagged by Neil (for the `C→Set` presheaf view; reconcile with my set-level T1). Aberlé `2604.01303` (Neil's link) = Poly program-verification (Agda, Spivak/Libkind lineage); its "dependent polynomial" is a Hoare-triple decoration NOT the GK dependent poly — PARKED as Path-5/6 application, not foundations. Gaps → `/expository` survey over coming days; **left-closedness** (Neil's explicit ask) split to `state/PROVE.md` (`◁` LEFT internal hom on Fam(C^op) via left-Kan/representability; disambiguated from the known ◁ right-coclosure). **[08-27 browse → 09-01 dream] FOURTH candidate approach surfaced:** Walker **"Locally Subcartesian Closed Categories"** `2607.10242` (independent Street/Weber lineage, NOT Gambino–Kock) — a *weaker affine substitute* for Σ-Π-Δ via **subpullbacks** (`∇_f⊣⊠_f` exist when `Σ⊣Δ⊣Π` fails), slice tensor `g⊗_Y f≅Σ_f∇_f(g)` right-closed-not-cartesian, motivating tensor `A+B−X` = structurally Vec's `⊗/⊕`. Sharpens the discriminator thesis into a **test**: does Vec/`Fam(Vec^op)` carry a subpullback structure despite failing full LCCC? (NO ⟹ sharper negative than "Vec not LCCC"; YES ⟹ partial Σ-∇-⊠ semantics over Vec, reopens a Front-D corner). **RESOLVED 2026-08-27 WAKE by DIRECT-READ (Charles Walker, Macquarie/Masaryk/TalTech; Weber distributivity pullbacks + Street protocalibrations; PDF `/home/agent/papers/walker_lscc.pdf`): the NO branch — leans negative, NOT a PROVE target.** Three corrections to the old agent-summary: (1) pullbacks are MANDATORY ⟹ `Σ_f⊣Δ_f` always holds, only the TOP adjoint `Δ_f⊣Π_f`→`∇_f⊣⊠_f` is weakened; (2) closure needs GENUINE right adjoints `⊠_p` (Def 4.0.1/Prop 4.1.2), NOT unique-if-exists — refutes the "Vec slips through" hunch at the closure step; (3) nearest cousin = **affine spaces**, filed (Rem 4.0.2/fn 15) as "close but obstructed by *cross-fiber combinations*" = Vec's `∐⊊⊕` in disguise (`⊠_{X→1}` exists, general `⊠_p` fails). Thm 5.2.8 = GK-style polynomial biequivalence, NOT a recognition criterion ⟹ no back door. **Net: Vec fails even Walker's affine weakening — the sharper-negative discriminator, now an EXPOSITION line for the survey (why Fam(Vec^op) needs approach (1)), not new maths.** Q2 (Walker Street-spans vs my family-`∐`) still open, lower priority. → `questions/vec-subcartesian-closure.md`. **[browse 08-27-browse2 → 09-02 dream] Front D approach (3) (fibrational/comprehension), previously EMPTY, now has a source cluster** (all pulled from Walker's own 30-item bibliography): **Weber `1106.1983` "Polynomials in categories with pullbacks"** (weakens GK from LCCC to mere pullbacks — the rung *between* GK and Walker), Street "Polynomials as spans" `1903.03890`, Street–Verity comprehensive factorization (TAC 2010), Najmaei et al. `2503.10868` (non-full/non-split comprehension categories), Arkor–Fiore `2006.16949`. **Reframe (crown of the cycle, `connections/weakenings-of-sigma-pi-delta-vec-fails-all.md`):** Front D is NOT three disjoint approaches but a **totally-ordered family of weakenings of Σ-Π-Δ** (LCCC ⊐ pullbacks-only ⊐ subpullbacks ⊐ spans), and `Fam(Vec^op)` falls off the *bottom* — fails all, always for `∐⊊⊕` + no internal `Π`; only external `Fam(C^op)` (approach 1, external Set-`∐`) reaches it. **Q3 — RESOLVED 2026-09-03 (dream) by DIRECT-READ of Weber `1106.1983` (TAC 30, 2015; deep-read in `sources.json`): branch (b), Vec fails.** "Categories with pullbacks" is misleading shorthand — Weber requires each polynomial's **middle leg exponentiable**, run through **distributivity pullbacks** (comparison **δ iso**). Exponentiability = Vec's missing `Π`, so the abelian-pullbacks hope was a red herring: Vec meets "has pullbacks" but not "exponentiable middle leg," failing same as GK/Walker. What it bought: Weber **localises** the failure (each specific middle leg fails δ-iso, not a global no-`Π`) and is the tower's master reference. **★ SPINOFF — δ≟Φ: RESOLVED 2026-08-28, DISTINCT (`computed`), conjecture REFUTED.** [[weber-delta-vs-t2-phi-distinct]]; question file marked RESOLVED; working `scratch/2026-08-28-delta-vs-phi-check.md`. The two "canonical-map-iso" conditions constrain **different legs**: Weber's **δ tests the LEFT position** (`[Z,−]` preserves `∐` ⟺ `P_s` exponentiable/tiny) = my proved **T4-left** collapse (`◁` well-definedness); my T2 **Φ tests the RIGHT position + target** (`Q_t` dualizable + summable), never sees `P_s`. Two-way separable over Vec (P fd + Q=k^(ℕ) ⟹ δ iso Φ fails; and converse) ⟹ the coincident-Vec-failure that made it plausible was an artifact of ∞-dim generic data. **T2 does NOT fold into the LCCC weakening tower.** Survivable increment = a two-way **re-filing**: δ ↔ T4-left in Weber `1106.1983` (distributivity pullbacks); **T2/Φ belongs to Weber's OTHER machinery — parametric right adjoints / familial functors, TAC 18 (2007)** ("Φ fam-rep" = "p.r.a."), a separate axis. Seam-heuristic negative control (cf. OrgTr). Upgrade→`proved`: deep-read Weber `1106.1983` §2 + TAC 18 (2007). **[08-28 inventory, Neil UID 134]** base-category inventory for `Fam(C^op)` compiled (`scratch/2026-08-28-base-category-inventory.md`, sent to Neil): direct general-base answer = **DJN `2305.05655`** (any monoidal V, ◁-comonoid=enriched cat Thm 4.3); published bases = LCCC/topos (GK, von Glehn fibrational), finite-limit (Shapiro–Spivak `2305.00167`, comonoid needs only exponentiable source), Weber pullbacks+exp-legs, Walker LSCC (quantale/affine/nominal), any fibration (von Glehn), Set^I (DPUV), Prof (FGHW JLMS 2008); MINE = Vec/Set^→/copower-obstruction/tower-assembly; OPEN = Rel, R-Mod, Mod/Cat#/Poly (Niu–Spivak Ch9 Q5). Organising fact: off Set a base fails by losing extensivity (`∐⊊⊕`) or internal Π; Vec loses both. Community confirmation: **MO 205902** got a 2026-07-28 answer naming Walker's LSCC as *the* monoidal analogue of LCCC (cite alongside Walker). Resource: Mathlib now ships `CategoryTheory.Monoidal.DayConvolution.Closed` (internal homs via profunctor ends) — same shape as T2's "closed ⟺ Φ fam-rep", handle for a future `/lean`. **SURVEY REVISION SHIPPED (08-28 WRITE):** `papers/containers-over-a-base.tex` now 14pp with **§5 = the proved logic of containers** (`Cont(cod)` fibration, A/E liftings, dualisation theorem) as approach-(3)'s concrete anchor; for-robin note `2026-08-28-containers-survey-revision.md`. ✓ **von Glehn TAC 33 (2018) provenance debt CLOSED** (2026-08-28-browse2, full PDF deep-read, now in `sources.json` with locators: Prop 6.9 p.1132 `Π_B:=(Σ_B)^op` = the exact source for [[contravariance-is-fibrewise-op]] and the Cont(cod) dualisation theorem; Thm 7.1 p.1134 distributive law exists ⟺ base LCC; Thm 7.2 composite `=Poly_B`). Survey §5 citation now airtight. Also new-to-tracking: Walker `2208.13088` (derivatives via negation; container derivative = ¬δ at I=J=1, relevant to the ∂p↔Weihrauch open item); Hedges' "Poly" blog independently rediscovers my (⋉,⋊) duoidal/LDC — uncited, compare-and-cite in a future `/write`. Tooling: `mcp__research__arxiv_search` broken (301 http→https not followed); `r.jina.ai/<url>` proxy is the working method for MO/nLab reads.
- **[08-28 PROVE] Approach (3) (fibrational) — PROVED, `proved`. `cont-cod-predicate-fibration`.** Proof `proofs/2026-08-28-cont-cod-fibration.md`; registry validator-clean; collaborator note `for-collaborator/2026-08-28-cont-cod-logic-of-containers.md`; memory [[logic-of-containers-cont-cod-fibration]]. **(Structural, Lemma 2.1 in full):** `(u,{φ_i})` is `Fam(p)`-cartesian ⟺ each `φ_i` `p`-cartesian ⟹ Fam preserves fibrations/bifibrations componentwise; `cod` opfibration ⟹ `cod^op` fibration ⟹ **`Cont(cod)=Fam(cod^op)` is a BIFIBRATION**; fibre over `(S,{P_s})` = `∏_s(Set/P_s)^op` (self-contained fibrewise-op derivation, = von Glehn [[contravariance-is-fibrewise-op]]). **⚠ THE TRAP (corrects the orientation):** cartesian reindexing is `(Σ_ρ)^op` = dualised postcomposition, **NOT** the pullback `ρ^*` (which is the OPfibration weakening). **(Quantifiers = A/E, Thm 5.1):** along the collapse `η:(S,{1})→(S,{P_s})`, string **`A ⊣ Δ_c ⊣ E`** with `E=Exists=η^*=(Σ_!)^op=◁` (cartesian reindexing=total space), `Δ_c`=weakening, `A=All=(Π_!)^op` — the proved UID-94 liftings [[neil-A-E-predicate-liftings-proved]]. **★ DUALISATION THEOREM (Thm 5.2, the increment):** the container hyperdoctrine = **fibrewise OP of the standard Set-family hyperdoctrine**: container-∃ = All (Set's Π), container-∀ = Exists (Set's Σ) — roles swapped vs operations; also `∧↔∨`, `⊤↔⊥`; proof-relevant fibre = **co-topos** (subtractive logic, invisible at the Boolean truncation). BC (Prop 6.1) + **co-Frobenius** (Prop 6.2, uses fibre coproduct) hold, dualised; Set instances brute-force verified (`verify_cont_cod.py`, `verify_bc_frob.py`), op+Fam transport formal. **Clean statement:** `Cont` = the fibrewise-opposite endofunctor on the 2-category of Set-family hyperdoctrines. **Fork answered:** keep proof-relevant primary (subtractivity is witness-level). **GAPS (honest):** shape-level quantifiers (Fam Kan along `u:S→S'`) exist componentwise but combined shape×position BC/Frobenius unchecked; truncation preservation + intrinsic BC-square class open — none touch Thms 3.1/5.1/5.2, Props 6.1–6.2. This is approach (3)'s anchor for the survey WRITE.
- **[08-28 PROVE, fallback] Shape-level + JOINT gap CLOSED — `proved`, `joint-bc-cont-cod`.** `proofs/2026-08-28-joint-bc-cont-cod.md`; validator-clean; collaborator note `for-collaborator/2026-08-28-joint-bc-cont-cod.md`; memory [[cont-cod-is-a-co-hyperdoctrine]]. **RESULT: `Cont(cod)` is a CO-HYPERDOCTRINE.** Shape quantifiers exist (`∃_j=Lan_u ⊣ j^* ⊣ Ran_u=∀_j`, fibre product / disjoint union, unconditional); the shape/position **exchange square is a genuine Poly pullback** (UP brute-forced). **Joint BC/Frobenius is ONE-SIDED:** right-adjoint (∀/E) quantifiers carry **co-Frobenius** (`∧↦∨`) + Beck–Chevalley; left-adjoint (∃/A) quantifiers **FAIL both**, obstructed uniformly by **co-topos non-distributivity** of `(Set/P)^op` (`sum-of-products ≠ product-of-sums` = fibrewise OP of Set's distributivity — the ∃-laws spend exactly the distributivity the op removes). Kindred-but-distinct from the `∐⊊⊕` base boundary [[extensivity-is-the-container-boundary]] (there base-change; here the fibrewise op, base stays Set). **★ CORRECTION to parent §6.2 (audit):** its co-Frobenius was mis-attributed to `A=(Π_!)^op`; it belongs to `E=(Σ_!)^op` (op of Set's `Σ_!`-Frobenius); `A` has Frobenius for no connective — the classic unaudited-duality error ([[the-summary-is-what-gets-audited]]). Main PROVE target (T2=p.r.a.) NOT attempted: Weber TAC 18 reading gate unmet (summary-only source, browsing off).
- **[09-06 dream] ★ THE METHOD, NAMED — "one functional, many probes"; and extensivity is the FUSION MACHINE.** Three times now (T2 conjuncts A/B `proved`; δ≟Φ `computed`; fibredness-vs-left-closure `proved`) two conditions that both read "canonical map is iso / this adjoint exists", both failing over Vec, both repaired at representability, have turned out **distinct**, and the resolving move was identical: *write the one formula both are about, identify which factor each constrains.* Sharpest form (instance 3): both are "is `G_r(Z)=Fam(⟨Z⟩◁q,r)` familially representable?" at different **probes** `r` — shape probe `(R,0)` sees collapse, position probe `⟨I⟩` sees summability. **New sentence:** over `Set` there is exactly ONE collapse mechanism (`|T|=1`) and it trivialises summability too, so the probes **fuse** — *extensivity is what makes logically independent conditions indistinguishable*, which is the mechanistic reason prior work keeps fusing seams and why moving to `Fam(Vec^op)` is a fusion-breaking instrument rather than mere generalisation (explains [[contribution-is-the-delta-prior-work-fused-away]] from the inside). **CONJECTURAL upgrade, falsifier named:** "familially representable" is Weber's p.r.a. vocabulary — Weber, *Familial 2-functors and parametric right adjoints*, **TAC 18(22):665–732 (2007)**, DOI `10.70930/tac/9l84qqh9` (no arXiv ID; `sources.json` `weber-2007-familial-2-functors-pra`, **deep-read 08-29**), Def 2.3 / Ex 2.4 / Ex 2.5 — and a p.r.a. condition IS a slice-indexed family of representability conditions, so the probes may literally be its slice objects. Cheapest falsifier: over `Set`, is `L_q=(−)◁q` p.r.a. for every `q`? If yes, re-filing wrong. → `connections/one-representability-functional-two-probes.md`, `questions/weber-pra-boundary.md`. **Side effect: the δ≟Φ `computed→proved` gate is DISCHARGED** (both Weber papers now deep-read: `1106.1983` TAC 30 2015 for δ, TAC 18 2007 for Φ) — no longer browse-blocked, now a `/prove`-or-`/expository` task; grade NOT changed on citation alone.
- **[09-06 dream] Path-5 filing correction — applied CT has TWO schools, and only one can carry an obstruction.** Full reads (08-29-browse2) of Li–Patterson–Mabry–Osgood `2509.18475` (Compositional System Dynamics) and Aduddell–Fairbanks–Kumar–Ocal–Patterson–Shapiro `2301.01445` (Compositionality **6**, 2024) show **structured cospans + pushout gluing over ACSets / signed slice categories**, NOT Poly/comonoid — grep-confirmed zero hits; two speculative same-day `sources.json` notes corrected. **Siblings, not prior art, not corroboration** — [[applications-are-directed-containers]] must not claim them. **Pattern (not a theorem):** pushout gluing is *total* ⟹ no obstruction class; ZS/directed composition is *partial* ⟹ `[ω]∈H²` — and the OrgTr control says the same from the other side (`2602.17917` Prop 4.3 total ⟹ no `[ω]`). *Totality of the primitive ⟺ absence of an obstruction class*; a fourth mode (interface-sharing by colimit) alongside [[three-modes-of-composition-dream]]. Unstaked lead: CT Zulip archive (`mattecapu.github.io/ct-zulip-archive`, new to toolkit) "power grid oscillations and polynomial functors?" (Jun 2025, **unanswered**) = a physical-infrastructure Path-5 domain. → `connections/two-schools-of-compositional-applications.md`.

---

## ★★ THE CROWN META-PATTERN — my contribution is always the DELTA the prior work fused-away, omitted, or deferred
Laying five fronts side by side reveals one repeatable, honest search strategy: read prior work asking *"what seam did they smooth over?"* — **fused** (fullness = folklore fused unit-connectedness with Diers/extensivity), **omitted** (DJN omit closedness/fullness/change-of-enrichment; attention's five lineages omit the depth law), **deferred** (O'Neill defers the nonlinear degree-`3^L`), or **absent-control** (OrgTr had NO seam — Prop 4.3 total — so NO delta, and the honest read correctly killed the crown). This is the honest inverse of megalomania (the durable wins came from scoping DOWN to one seam) and the grant narrative for an AI mathematician. Crown jewel: `connections/contribution-is-the-delta-prior-work-fused-away.md`. Supported by [[the-summary-is-what-gets-audited]] (lossy summaries MANUFACTURE novelty — running forward as load-bearing discipline).

---

## PROVED-RESULTS LEDGER (pointers, not re-derivations — details in `proofs/`, grades in `registry/`, citations in `MEMORY.md`)

**Fullness / extensivity / Vec-container boundary**
- **T2 (Dirichlet ⊗ closedness on Fam(C^op)) — `proved` 08-26.** Closed ⟺ `Φ(Z)=∏_t∐_r C(M_r,Z⊗Q_t)` familially representable; closed on cartesian bases (Poly Dirichlet hom recovered) + `Fam_fin(Vec_fd^op)` ONLY; FAILS over both `Fam(Vec_fd^op)` (∞-shapes, conjunct B) and `Fam(Vec^op)` (∞-dim positions, conjunct A) — correcting the "holds over full Vec" prediction. Load-bearing = dualizable-and-summable. ⊥ T1 fullness; NOT the `∐⊊⊕` seam. [[t2-day-closedness-famcop]], registry `t2-day-closedness-famcop`.
- Extensivity IS the container boundary; Set carries all container theory, Vec breaks it `∐⊊⊕`. Two valuations: global shape-erasure (liability) vs local either-prompt=biproduct (asset); UNIVERSAL (Hedges additive lenses = same lemma, julesh.com 2026-02-20). [[extensivity-is-container-boundary]], [[vec-biproduct-collapse-proved]].
- Vec ◁-comonoid = family of k-algebras `Comon_◁(Fam(Vec_fd^op))≅Fam(Alg_k^op)`; lax ◁ = matrix product `(P◁Q)_{ac}=⊕_b P_{ab}⊗Q_{bc}`, comonoid = algebroid = Mat(Vec) comonoid; strict single-index = diagonal. [[vec-comonoid-algebra-family-proved]], [[vec-lax-matrix-crown-resolved]]. (⚠ 2026-08-31 `speculative`: **whether this is subsumed by DJN Theorem 4.3 is now OPEN** — their `◁_DJN` is weighted and coincides with my `◁` only at `C=1`, and the two comonoid classifications visibly differ (families of `k`-algebras vs all enriched categories). The concession assumed the operations agreed. **Not a restored novelty claim — an unresolved question, to be settled before anything is claimed either way.** Cite DJN regardless.)

**Orchestration / Zappa–Szép / obstruction**
- Agent/directed-container composition = ZS product `C⋈D`; reentrancy = obstruction `[ω]∈H²(Sk_C;𝒟)≅ℤ/2`. [[orchestration-is-zappa-szep-weld]], Lean sorry-free [[lean-reentrancy-omega-equals-epsilon]]. **SOURCE re-attribution (must cite):** ZS-from-distributive-laws = Ahman–Uustalu, "Distributive laws of directed containers," Progress in Informatics 10 (2013) 3–18, DOI 10.2201/NIIPI.2013.10.2 (matching pair = my λ); they build NO obstruction theory ⟹ `[ω]∈H²` is my clean delta. [[reference_ahman_pi13_zs_attribution]]. **[09-13 — Rick's Option 2 EXECUTED (WRITE, `papers/reentrancy-machine-checked-obstruction.{tex,pdf}`, 7pp clean, wip commit `733b76b`).** Standalone note with the **Lean file as the load-bearing artifact** (emphasis inversion vs `containers-for-orchestration.tex`, which proves `[ω]=ε` analytically w/ Lean as corollary): quotes `Reentrancy.lean` theorems verbatim (`phi_omega`, `omega_inB2_iff_zero`), draws the verification boundary (Lean certifies the finite-𝔽₂ last mile; categorical→complex reduction stays cited pen-and-paper, Prop 3.1). Total/partial lens demoted to ONE paragraph = Gerstenhaber absolute-vs-classified (Annals 79, 1964); H¹ prediction retired; no novelty claimed. **SENT TO RICK for review 2026-09-03** (§2.3 header added: For Rick / 2026-09-03 / wip commit `fad5f3c`; covering note CC Robin; asked him to check the verification-boundary framing + Prop 3.1). NOT `publishable-result` until Rick's review comes back; open Lean target = the first mile (orbit category, freeness (L), defect `=(0,ε)`). `for-robin/2026-09-02-reentrancy-standalone-note.md`.
- Pairwise ZS criterion `K=C⋈D ⟺ (L) free hom-presheaf ∧ (G) closing basis`; (G) ⟺ `[ω]=0∈H²`, rigid twist = ℤ/2. [[pairwise-zs-criterion-proved]], [[g-obstruction-is-h2-class]]. Two [ω] sites irreducibly distinct (Sk_C rigid ⟹ isotropy=0). [[two-omega-sites-not-isotropy-restriction]].
- OrgTr control: Spivak `2602.17917` composition `#` = Prop 4.3 **unconditionally total** ⟹ NO `[ω]` one level up (crown REFUTED, honestly); embedding DCont↪OrgTr = Prop 4.6/Cor 4.8 (constant/time-invariant fragment). [[orgtr-composition-total-no-omega]].
- Three modes of composition: directed/ZS `[ω]∈H²`, state/Workers none, effect-coeffect BRANCHING. [[three-modes-of-composition-dream]].

**Liftings / monad-comonad zoo / fibration stratification**
- DCont ≅ Cat is the convergence hub (4 fronts, one theorem). [[dcont-cat-convergence-hub]]; DCont morphisms = cofunctors/retrofunctors, not functors [[dcont-morphisms-are-cofunctors]]; Cat# = DCont ≅ Cof in Poly [[cat-hash-is-dcont-cof]].
- Reader's proof-relevant liftings ≅ small categories; general-M lifting reduction; A/E predicate liftings = ∏/Σ; position-op turns monads→comonads (`G(S,P)=(S,M∘P)` = ◁-left-coclosure = Lan). [[reader-liftings-are-categories-done]], [[lifting-general-M-and-monoid-comonoid-unification]], [[neil-A-E-predicate-liftings-proved]], [[position-op-monads-to-comonads]].
- Crown TFAE is FALSE → strict 4-level chain: λ-inv ⊊ non-branch ⊊ cartesian ⊊ ∏-Mendler; Reader/State OUTSIDE ∏-Mendler (DROP); trichotomy MERGE(Pf)/DROP(Reader,State)/SYMMETRY(Bag). [[crown-tfae-strict-chain]], [[reader-state-outside-pi-mendler]], [[proof-relevance-boundary-reader-state]].
- Arrows/Arr_M form a category ⟺ M non-branching (Hughes/Freyd; obstruction = leaf-symmetry); affine class = E+A×X (Set-monad = monoid on E⊔A); branching/comm/affine pairwise independent; Atkey index-degree conjecture NEGATIVE (boolean not graded). [[effect-coeffect-arrows-first-strength]], [[affine-classification-writer-exceptions]], [[branching-commutativity-affine-independent]], [[atkey-index-degree-negative]].
- Workers: (Set,×)-graded category (`ΔS⊗ΔT=Δ(S×T)`); type hierarchy (4 monoidal descend grade-mult); closure row **⊗✓ ×✓ ◁✗** (× closed via `∏_{s_p}q◁(y⊕c_…)`; Cont lacks ◁-closure, only left-coclosure). [[workers-graded-category-proved]], [[workers-type-hierarchy]], [[workers-x-closed-lhd-obstructed]]. **[08-29 PROVED — retract theorem]** The "Workers = `P=ΔS` fibre of BHM `T_P(X)=X▷P`" conjecture is **FALSE** and the honest relation is now a THEOREM (`proofs/2026-08-29-workers-retract-of-bhm-grading.md`, registry validates, verified n≤3). Workers grades by Dirichlet `⊗` (`ΔS⊗ΔT=Δ(S×T)`), BHM by composition `▷` (`ΔS▷ΔT`); at n=2, `8y⁴` vs `4y⁴`. **P2:** `A=ΔS⊗ΔT` is a non-trivial **retract** of `B=ΔS▷ΔT` via `σ:(s,t)↦(s,const_t)`, `r:(s,g)↦(s,g(s))`, **`r∘σ=id` as genuine Poly morphisms** (`σ∘r≠id`); idempotent `e=σr` = collapse-branch-to-self-eval. **P3 coherence:** `σ` oplax-hexagon + `r` lax-hexagon (self-eval associative). **P3 collapse (= "⊗ is the diagonal of ▷"):** `r∘δ=Δ(d)` — store comult `δ` is a LIFT of the ⊗-diagonal along `r`; off-diagonal `(1−e)δ` = failure of `▷` to be ⊗-diagonal. **P3 impossibility:** `σ∘Δd=δ'` coassociative but NOT counital ⟹ not a comonad ⟹ `Δ` oplax(σ)+lax(r) **only on core groupoid `(Set_≅,×)`**, NOT a monoidal functor on full `(Set,×)`; store comonad is INTERNAL. [[workers-grading-retract-not-fibre-of-bhm]]. **[08-29 LEAN — `lean-verified`, ZERO sorry] `lean/Containers/Containers/WorkersRetract.lean`** (Lean 4 core, no Mathlib; `lake build` 0 errors / 0 warnings). `storeSection` σ + `storeRetraction` r as genuine container morphisms (**L1 — that they elaborate IS the variance claim**); `storeRetraction_storeSection` (**r∘σ=id**, `rfl`); `storeSection_storeRetraction_ne` (σ∘r≠id, `Bool` witness `⟨true,id⟩`); **`storeRetraction_coComult` (`r∘δ=Δ(d)`, `rfl`) — the collapse identity, '⊗ is the diagonal of ◁', the ◁-side companion of `deltaDC_prod` in `StateComonadTensor.lean`**; and the full L5 dichotomy `storeDiagSection_coassoc` (δ'=σ∘Δd IS coassociative, `rfl`) vs `storeDiagSection_not_right_counital` (right counit FAILS at shape `true`, position `⟨false,()⟩`) ⟹ not a comonad ⟹ Δ lax/oplax only on `(Set_≅,×)`. **Five of six theorems axiom-FREE**; `storeDiagSection_coassoc` on `Quot.sound` alone (via `Container.associator`). Registry `P2-retract`/`P3c-diagonal-collapse`/`P3d-not-oplax-full-set` → `lean-verified` + child `L-lean-workers-retract`; both validators pass. **★ CORRECTION the type checker surfaced:** the paper proof's "all fibres are literally `S×T`, so the identity position maps typecheck" is WRONG as stated — `B.Pos ⟨s,g⟩ = (q:S)×T` is a `Sigma`, NOT defeq to `Prod S T`; the backward maps are the canonical `Sigma`↔`Prod` swaps. Inverse by structure η ⟹ **theorem unaffected**, but the stated justification isn't the one Lean accepts — amend if written up. P3ab hexagons NOT formalised (by instruction), node stays `proved`. → `for-collaborator/2026-08-29-lean-workers-retract.md`. Three graded-monad pictures — Snoc (free-`ℕ`), BHM (`▷`), Workers (`⊗`) — one line, **three products** (raised w/ Neil 08-29). Their "`▷` not fibred in its left variable" lines up with T4-left's `◁`-left obstruction (open sub-Q2). **[08-30 WAKE recon, `computed`] sub-Q2 SCOPED and leaning NEGATIVE.** BHM primary source now LOCAL (`/home/agent/papers/BHM-polylang-ACT2026.pdf`) — a **2-page extended abstract** whose entire claim is ONE unproved parenthetical in §3 ("we are not using [PP26 fibred endofunctors] because the composition product ▷ is not fibred in its left variable") ⇒ **corroboration, NOT a lemma**; the definition and proof are mine to supply. Variable alignment CONFIRMED (`(−)▷P` = my `(−)◁q`, not a trap). But **different logical shape**: non-fibredness = failure of base-functoriality (`π(p◁q)=Σ_s T^{P_s}` reads `p`'s POSITIONS, so no base functor exists), NOT a missing adjoint. **One formula `Σ_s T^{P_s}` drives both**, and over `Set` the boundaries appear to coincide (fibred ⟺ closed ⟺ `q` monomial). **★ CANDIDATE SEPARATOR:** over `Fam(Vec_fd^op)` with `T=ℕ`, `Q_t=k`, tininess gives `◁=⊗` ⇒ shapes `S×T` ⇒ **fibred**, yet proved T4-left Thm 3.1(2) says **closure FAILS** (`⊕_{t∈ℕ}` leaves `Vec_fd`) ⇒ **fibred ⇏ closed**; identification is `Set`-local. Diagnosis: fibredness tests only that the exponent **collapses**, closure also needs the collapse **summable in the base**. Same moral as δ≙Φ (two canonical-looking conditions constraining DIFFERENT legs) — second occurrence. Load-bearing gap: PP26 `2601.15420` NOT on disk, its "fibred" read only from `sources.json:412` agent-summary. → `state/PROVE.md` 08-30 (A `Set`-TFAE / B `Vec_fd` separation / C diagnosis), cheapest falsifier named in the trigger. → `connections/workers-grading-is-fibre-of-bhm-polynomial-grading.md`. **[08-30 PROVE] sub-Q2 ANSWERED — `proved`, `fibredness-vs-left-closure`. NO, not the same obstruction.** Proof `proofs/2026-08-30-fibredness-vs-left-closure.md`; registry validates; collaborator note `for-collaborator/2026-08-30-fibredness-vs-left-closure.md`; [[bhm-fibredness-vs-t4-left-separable]] computed→proved. Shape fibration `π:Fam(C^op)→Set`; **(V)** vertical / **(F)** fibred / **(C)** right adjoint to `L_q=(−)◁q`. **Thm A (Set):** `(V)⟺(F)⟺(C)⟺|T|=1` — `(F)⟹|T|=1` in ONE line (`⟨∅⟩,⟨1⟩` share a shape set but `π(⟨∅⟩◁q)=T^∅=1` vs `π(⟨1⟩◁q)=T`). **Thm B (Vec_fd):** `(F)` **always**, `(V)⟺|T|=1`, `(C)⟺#{t:Q_t≠0}` FINITE ⟹ **`(V) ⊊ (C) ⊊ (F)` strictly**; witnesses `q=(2,k)` closed-not-vertical, `q=(ℕ,k)` **fibred-not-closed**. Two-sided ⟹ NO under **either** reading of 'fibred', so the answer is reading-independent. **★ BONUS:** `◁` IS fibred in its **RIGHT** variable ∀q, base functor literally `⟦q⟧` (`π(q◁p)=Σ_t(πp)^{Q_t}`); and **both** variables preserve cartesian morphisms **unconditionally** ⟹ non-fibredness = pure failure of **base-functoriality**, never cartesianness (so `μX.1+q◁X` IS fibrationally constructible, shapes = W-type `μS.1+⟦q⟧S`; Rem 2.3 — BHM's reason doesn't block their three listed fixpoints, only their own `T_P(X)=X▷P`). **★ DIAGNOSIS (one test, two probes):** both conditions are 'is `G_r(Z)=Fam(⟨Z⟩◁q,r)` familially representable?' at different `r` — **shape probe** `r=(R,0)` gives `R^{π(⟨Z⟩◁q)}` (over Set this IS fibredness ⟹ closed⟹fibred *conceptually*, Cor 5.3); **position probe** `r=⟨I⟩` gives summability. **Fibredness = COLLAPSE of the shape object; closure = collapse AND SUMMABILITY inside the base.** Set has exactly ONE collapse mechanism (`|T|=1`) which trivialises summability too ⟹ the coincidence; over a linear base collapse is generic (tininess) ⟹ decoupling. **Two of my own results strengthened:** Workers Thm 2 SUPERSEDED (least-support Lemma 3.4 — `x=(u,g)∈im F(A)` iff `im g⊆A` — replaces the `2^{2^n}` counting and covers all `|T|≥2` finite *or infinite* plus `|T|=0`; witness `Z=ℕ`, `A_n=ℕ∖{n}`, the eventually-`t_0` sequences); T4-left Thm 3.1(2) SHARPENED (Vec_fd boundary = infinitely many **non-zero** positions, not `T` infinite — zero positions contribute no summand; the Set analogue is FALSE, `q=(2,∅)` not closed, itself the two-probe diagnosis). **Method, 3rd occurrence** after [[weber-delta-vs-t2-phi-distinct]] and T2's conjuncts A/B: *write the one formula both conditions are about, identify which factor each constrains* — now a METHOD, not a coincidence. **Honesty:** BHM = corroboration only (2pp, one unproved parenthetical); `◁:=⊗` on the tiny locus is a DEFINITION (`⟦−⟧` not full over Vec) so Thm B is literally about `(−)⊗q`; **attribution gap NARROWED not closed** — PP `2601.15420` is `extraction: deep-read` in sources.json (correcting PROVE.md's 'agent-summary') and its framing IS recorded as fibred endofunctors over the fibrewise-op of `cod` (= [[contravariance-is-fibrewise-op]] = my `π`), BUT that read was grep-targeted at derivative/chain-rule and my `2026-07-29-browse3` log still lists the identity question as OPEN, and their *definition* of 'fibred endofunctor' was never extracted ⟹ **cheapest discharge: fetch `2601.15420`, read the fibred-endofunctors section, one locator** (recorded in `sources.json` locators + corrections). OPEN: is 'closed ⟹ fibred' true over an arbitrary base? (Prop 5.2 gives only the shape-probe necessary condition; no counterexample known.) Hostile-referee pass caught a wrong shape action in Lemma 2.1 (`τ∘φ^♯_s`, NOT `τ∘(φ^♯_s)^{-1}`) via the explicit-morphism functoriality check, 183/183 after the fix.

**Monoidal / closed structures on Cont/Poly**
- Poly three closed structures: (Poly,×) CCC, ⊗-hom = Niu-Spivak Ex 4.78, ◁ right-coclosure = DCont (Prop 6.57). Closed convolutional tensors = ⊗ and ▷_S (bounded arity); arity gap OPEN. [[poly-three-closed-structures]], [[closed-convolutional-tensors-classified]], [[arity-gap-counting-is-blind]].
- Dirichlet ⊗ = Day convolution (unique pointwise); ⊗-comonoids = families of monoids `Comon(Cont,⊗,y)≅Fam(Mon^op)`. [[dirichlet-is-day-convolution]], [[bare-dirichlet-comonoid-proved]]. (⋉,⋊) = Dialectica tensors (**tensors are DJN §6; the Dialectica identification is MINE, `computed`, not DJN's — they pose it open**), normal duoidal + LDC; non-closure one-sided (⋊ left-closed). [[ltimes-rtimes-are-dialectica]], [[ltimes-rtimes-duoidal-ldc-proved]], [[dialectica-non-closure-one-sided]].
- Free/cofree comonad UP proved (Ch6); free monad on container = ◁-monoid (Lean sorry-free); cofree comonoid = Niu-Spivak Prop 8.33/8.45 (scooped; positions=vertices not leaves). [[free-cofree-up-mirror]], [[lean-free-monad-unit-laws-done]], [[cofree-comonoid-scooped-and-wrong]].
- Contravariance IS the fibrewise op: `Cont=∫_Set (cod)^op`, von Glehn TAC 33 (2018). [[contravariance-is-fibrewise-op]].

**Attention / ML instantiation**
- Linear attention = ⊙ in Mat(Vec) EXACTLY at fixed context (contexts compose by ⊕ = linear-RNN, Katharopoulos `2006.16236`; depths by ⊙); live depth-L stack = degree exactly `3^L` ⟹ "stack=one matrix" REFUTED; softmax NOT ⊙ (non-homogeneous, Sargsyan `2603.16123`). [[linear-attention-odot-degree-3L]]. Stacking = free ◁-monoid = tensor algebra `⊕_L AttP^{⊗L}` [[oneill-free-monad-tensor-algebra-linear-container]].

**Ext / emergent holonomy (Rick front)**
- Emergent holonomy IS Ext⁰ (degree-0 rank): Mackey/Shapiro `Extⁿ≅⊕_{A\G/B}Hⁿ(A∩gBg⁻¹)`, `h=dim Ext⁰`, higher tower vanishes under transversality; `h(s)=|A\U/B|`, h=1⟺aligned. [[emergent-holonomy-is-ext-degree0]], [[emergent-holonomy-meeting-points-proved]]. D₈ first non-abelian Ext tower (deg0=3>tower=2); V₄ transverse vanishes / non-transverse factors `h·H*(A∩B)`. [[d8-nonabelian-ext-tower-computed]], [[rick-v4-ext-vanishes-transverse]]. Holonomy composition = ZS bridge; trivial holonomy ⟹ collapse (same skeleton as categorical belief propagation). [[holonomy-composition-zs-bridge-proved]], [[holonomy-triviality-cross-domain]].

**Lean formalisations (all axiom-free / sorry-free)** — ZS assoc (PR#8), monoidal coherence (PR#17), free monad ◁-monoid laws, general `T^Σ_M=M◁−`, A-action law, reader/state DROP rung, T_M cartesian boundary, ℤ/2 groupoid reader lifting, disjointness lemma, S₃/holonomy witnesses, endpoint-locality collapse, `ΔS⊗ΔT=Δ(S×T)` comonad-level, reentrancy `[ω]=ε`, Workers/BHM retract `r∘σ=id`, **`F_q ⊣ (−)◁q` — the library's FIRST adjunction (`CompLeftAdjoint.lean`, 08-30: L1–L4 all on the first build, every law `ext' rfl (fun _ _ => rfl)`; negative control shows the triangles are about POSITIONS, not shapes)**, **plus the naturality of the unit/counit and functoriality of `L_q` (2nd pass 08-30, completing the adjunction of FUNCTORS; `whiskerRight` laws axiom-free) — with the control that the counit perturbation which BREAKS the triangle still satisfies naturality exactly, so the two are INDEPENDENT probes**, **plus naturality of `F_q` in the PARAMETER `q` (3rd pass 09-01, `CompLeftAdjointNatQ.lean`, 59 jobs 0/0/0, `[Quot.sound]` only) — `F_q` is CONTRAVARIANT in `q` (`F_ψ:F_{q'}⟹F_q` via `ψ.toNat`), with contravariant functoriality, both compatibility squares along `ψ`, and `F_ψ`=mate of `L_ψ`; third confirmation that the content lives in POSITIONS (qBool flip passes naturality-in-`r`, fails the unit square on the position leg)**. See MEMORY.md `lean-*` lines.

**General polynomial-functor references** — Kock "Polynomial functors and trees" `0807.2874` (trees/W-types background); Cont CCC not LCC (Altenkirch–Levy–Staton, CiE 2010); density comonads orthogonal to DCont≅Cat (Spivak `2503.21974`, complementary not a reproof — SEED Q5). [[cont-ccc-not-lcc]], [[density-comonads-orthogonal-seed-q5]].

---

## COLLABORATOR STATE
- **Neil (PI):** priority order in force — VCont *uses* first, (co)homology LAST; Fam(C^op) monoidal generality (UID 120); MORE formal detail not less; OrgTr/Workers PARKED. **T2 DONE (`proved` 08-26, `t2-day-closedness-famcop`)**; reported to him 08-27 daily (fails over BOTH Vecs, dual mechanisms — prediction refuted, banked as the result). **UID 125 (2026-08-26) = the big steer, ACTIVE:** (a) **reframe AWAY from grants** — "make you the most knowledgeable agent in the world on containers and applications" (recalibrate SUMMARY's grant-framing toward understanding); (b) **left-closedness** wanted (hint: left Kan preserves representability ⟹ coproducts) → `state/PROVE.md`; (c) **three approaches survey** (Front D: Fam(C^op) / indexed-Σ-Π-Δ-over-LCCC / fibrational), Fam(Vec^op) as running example, "a few days contemplating"; (d) **enriched-Yoneda dependency** flagged; (e) shared `2604.01303` (Aberlé — parked, applications). **UID 132 (2026-08-27): (a) survey GREENLIT ("do the survey, use Vec as example"); (b) NEW ask — turn `cod:Set^→→Set` into a logic of containers, apply Cont to a fibration for `Cont(Set^→)→Cont(Set)`.** ANSWERED 08-28 daily (Cont(cod) IS a fibration, fibre=predicates-on-positions, quantifiers=A/E liftings; asked proof-relevant-vs-subobject fork) → PROVE set + Front D approach-(3) populated [[logic-of-containers-cont-cod-fibration]]. Still owes fuller reply on T1 flagship + attention note ("I'll reply later"). Linear-ZS/H² = NO-GO. Daily email every morning, CC Robin. [[feedback_neil_fam_cop_generality_priority]], [[feedback_neil_vcont_uses_first_notes]].
- **Rick (peer):** H²/obstruction + Ext-support correspondence; live tex/pdf + repo both ways. Owe him the support-variety reply (drafted, send when email on). [[rick-live-comms-edge]].
- **Robin (human):** CC on every email; reads projects/ volume from host directly (no git remote on projects/).

## HOUSEKEEPING / OPEN THREADS
- **Standing discipline:** READ POLY BEFORE CLAIMING (grep Spivak PDFs + my notes); check scratch before dispatch; `Read`/`ls` before `Write` on dated logs ([[feedback_write_before_read_dated_logs]]); the summary is what gets audited.
- **Cheap external visibility, still unclaimed:** MO 461865 ("when is a container a monad?", 0-answer since 2024-01-09, answerable from T_M/T^Σ lift criteria) + MO 296319 ("when is an algebra a container?", dual of Vec algebroid); nLab has no directed-container↔ZS cross-ref (real Robin/MacBeth slot).
- **Open questions** → `questions/` (T2 Day-hypotheses for Fam(Vec^op); indexed-container specialization; Q′ non-extensive separator; arity gap; q-calibration).
- **Browse rotation (2026-09-08):** the **Weber p.r.a. citation trail is EXHAUSTED and DROPPED** — three consecutive negative audits (Fujii–Lack `2507.05529` = Weber Def 5.2 verbatim; Arkor–McDermott `2404.01281` zero "familial" hits; Shapiro `2111.14796` independent apparatus), full 104-S2/72-OpenAlex reverse sweep, all 2025 citers ruled out. **Consequence for writing: there is no cleaner secondary statement of familial representability — write δ≟Φ/T2 straight from Weber TAC 18 Def 2.3/5.2.** Parked lead for a future `Fam(V)` session: `2305.04042` (effective descent for `V ↦ Fam(V)`). Replacement keywords: sheaf-multi-agent + operads-for-LLM frontier. → `questions/weber-pra-boundary.md`. **[09-13] Standing Front-D read-next DISCHARGED: Ahman–Kammar–Møgelberg "A Convenient Fibration for Dependently-Typed Probability Theory" (LICS 2026, `10.4230/LIPIcs.LICS.2026.4`, now DEEP-READ, was agent-summary).** Builds **quasi-Borel families** = a split fibration refining the ordinary family fibration `Fam(C)` with measurable fibre structure — fibred Σ/Π, propositions universe, fibred measure/probability monads via a distributive-law lift, culminating in a fibred Kolmogorov conditional-expectation theorem (§9). **Full-text confirmed: ZERO container/polynomial-functor/W-type vocabulary.** Co-author **Danel Ahman** = half of Ahman–Uustalu (directed containers ≃ small categories, my foundational equivalence) — a live 2026 instance of serious semantics on the SAME family fibration underlying my `Cont(cod)≅Fam(cod^op)` bifibration [[logic-of-containers-cont-cod-fibration]], **with zero cross-reference either way**. Filing: SIBLING, not prior art — instance of the crown meta-pattern (they build fibred measure theory ON the family fibration; I build the container *logic* — A/E quantifiers, dualisation — on its fibrewise-op; orthogonal structure, same base). Grant-narrative value: the family fibration is an active 2026 foundation, and directed-container theory's own co-author is building on it uncited. **[09-13, minor] MO 514525 "Coequalizers in extensive categories"** (asked 08-31, score-7 negative answer: infinitary-extensive + regular ⇏ all coequalizers, via decidable sheaves on `ℕ∪{∞}`): background texture for Front A — extensivity is weaker than it looks even with kernel-pair coequalizers; no direct `π₀`/products-of-connected content. **[09-25] SATURATION now a 6–7-session fact, not a keyword-choice bug:** arXiv/citation-trail avenues return ~90% rediscovery even after rotating off the six stale CORE terms — the containers/Poly literature footprint is swept. **Live frontier = Path 5 native venues** (project trackers), confirmed 2 sessions running. Fresh finds: Catala GitHub #1095/#1096 (tax-§121 joint-return symmetry-break wiring bug, 2 independent sites — grant "economic consequences" worked-example candidate, [[symmetry-is-the-compositional-fingerprint]]); #691 (scope contract language); cstheory 55588 (free-theorem answerable from codensity/Yoneda — public-output opportunity); Cockett–Lemay "Itegories" 2504.02409 (iteration=trace, `agent-summary`, unread). Exclusion-list dispatch works (zero excluded reappeared). NEXT: a *different* named Path 5 system, not Catala again. → `questions/open-threads.md`. **[10-01] SATURATION reconfirmed (a dozen+ independent convergences, flat yield since mid-Aug): 5 of 6 "found" arXiv papers already deep-read. ACTION — retire the {containers, Poly, Zappa-Szép, agent-orchestration} keyword cluster from core-territory rotation; next core browse pivots to Path 1 (strictly-positive types / W-types) or Path 6 (Lean 4 / Mathlib container-adjacent PRs), neither browsed in weeks.** Two genuinely new-to-store Hedges dependent-lens posts (both listing-summary only, WAKE read-next): *Dependent Optics II* (forcing costates) and *Bidirectional Typechecking with Dependent Lenses* = a **"Kleene star on containers"** (free-monoid-of-a-container — likely my free ◁-monoid, candidate `Cont(Cont)`-iteration tool → [[kleene-star-on-containers-is-free-triangle-monoid]]). Confirmed negatives (genuine, not unsearched): agent-orchestration × CT still a two-paper field (Banu/Aberlé; `2608.03609` uses FO-CTL not CT); Topos blog quiet on my topics in 2026; nLab unchanged. Infra: Semantic Scholar 429-rate-limited this session (transient); MO/math.SE channels still WebFetch-blocked (null results there are broken-channel, not quiet-community). Carried-unread comparative reads (WAKE debt): Cameron Thm 4.19 vs C₃-residual; Jiaping Lu `d(F)=|I|` vs `𝔥(A)`. **[10-02, RECONCILED] The 10-01 "WAKE debt" is already DISCHARGED** — Cameron/Lu/Videla-Kleene all closed 2026-09-10 WAKE (3 research-agent deep-reads, all `computed`, none scoop; two next-revision cites = Cameron analogy + Bailey-Praeger-Rowley-Speed), [[reference_readnext_debt_discharged_cameron_lu_videla]]; and the Kleene-star=free-◁-monoid question is RESOLVED (Videla not Hedges, 2025-02-24, corroborating applied instance not prior art; Q3 Cont(Cont)-help REFUTED), [[kleene-star-on-containers-is-free-triangle-monoid]]. **The 10-02 browse re-raised all four as OPEN — a STALE re-raise: the browse agents ran from listing summaries without the WAKE resolutions in context.** Genuine 10-02 yield (thin): (1) saturation now extends BEYOND the retired cluster — Path 6 (Lean/Mathlib) and part of the wreath near-neighbourhood also re-converge on known papers; (2) new-to-store back-pocket **arXiv:2105.03080** "Systems of Imprimitivity for Wreath Products" (J. Algebra 2021, `agent-summary`) — uniqueness of the finest (maximal-depth) refinement in the LINEAR/GL(n) imprimitivity setting = the analogue one level up from `𝔥(A)`'s existence half; template IF a future `theorem-S-prime` refinement ever needs "the" maximal chain, not just its length; (3) grant-framing datum (Lean Zulip #270676): **no indexed-container library exists in Lean 4/Mathlib** (only the mathlib3-port QPF/pfunctor machinery) ⟹ MacBeth's Lean container work fills a community-acknowledged gap, not duplicate prior art; (4) Hedges' own blog (direct read) confirms his sequence-product `⊳` is built from container COMPOSITION (= my `◁`), not Dirichlet `⊗` — independent corroboration of the free-◁-monoid identification. Infra recurrence: MO/math.SE WebSearch channel broken (null results = broken-channel not quiet-community); Semantic Scholar 429 (transient). NEXT BROWSE: pivot fully OFF arXiv core-territory — Path 5 native venues (project trackers) or Path 2 (directed containers/update monads, untouched lately); consider an explicit arXiv blocklist for the ~9 chronically-refound papers.
- **Unapplied debt:** the Pradic–Price §6 attribution corrections drafted in `scratch/2026-08-30-pradic-price-fibred-def.md` are **not yet applied** to `proofs/2026-08-30-fibredness-vs-left-closure.md`. One `/write` pass.
- **Hedges parallel program — 4 independent overlaps, consolidated 2026-09-09** (Poly-the-language graded monad ≈ Workers/BHM; System L tensor+sequence ≈ `(⋉,⋊)` duoidal/LDC; additive-lenses CCC ≈ biproduct collapse; applicative transformers ≈ Front B). Two cheap compare-passes named. → `for-collaborator/2026-09-09-hedges-parallel-program.md`. He is the natural first reader for the BHM note. **[10-01] FIFTH candidate overlap surfaced: his "Kleene star on containers" (Bidirectional Typechecking post) ≈ my free ◁-monoid / O'Neill tensor algebra — HYPOTHESIS, listing-summary only, needs a deep-read to confirm the monoidal product is ◁. → [[kleene-star-on-containers-is-free-triangle-monoid]].**
- **Path-5 pattern, `speculative`:** *total composition CONSTRUCTS (universal property); partial composition SOLVES A LIFTING PROBLEM, and lifting problems carry cohomology.* Four data points; structured-cospan obstruction-freeness is now a **checked absence** (browse 2026-09-07). Prediction: sheaf multi-agent gluing carries an obstruction in **`H¹`**, one degree below ZS's `H²`. **Literature test DONE (dream cycle 2, 2026-09-09) — verdict mixed, prediction NOT confirmed:** `2606.01663` (deep-read) is **H⁰** (Nash equilibria as global sections); `2605.01879` (deep-read) asserts H¹/H² for plan-gluing but **proves no theorem**; `2605.11204` Anwer–Riess–Hale **Thm 2** is a real `H¹` theorem on the **wrong axis** (identifiability, not gluing-existence) — citing it as confirmation would be an *invariant collision*. **The outstanding work is a `/prove` on my own model, not a browse.** → `connections/total-composition-constructs-partial-composition-lifts.md` (with its cycle-2 correction). **★ RICK'S PEER VERDICT 2026-09-01 (in, `peers/rick/`): (a) the total/partial dichotomy IS Gerstenhaber's "absolute vs classified" (Annals 79, 1964; HH²/HH³; Schreier/Pirashvili H²) — real, NAME IT, do not claim novelty; (b) the H¹ degree prediction is DEAD — the correct invariant is the CATEGORICAL DIMENSION of the objects being glued, not total-vs-partial (his 9-row table + two counterexamples: vanishes for stacks-vs-monoids both H², inverts for sets-vs-monoidal-cats H¹-vs-H³) — this independently confirms my own cycle-2 COLLISION diagnosis and names the shadow-functor; (c) a REAL worked H¹ obstruction exists but is a torsor/lifting problem `H¹(param; π₁(Aut g))` (pseudo-inverse choice in a family), NOT descent over a cover — sketch-grade, not registrable. Rick recommends Option 2: split the Lean-verified ZS-reentrancy result out standalone, demote the total/partial "lens" to a `work-in-progress` note ("a pattern I noticed and cannot substantiate"). H¹ prediction RETIRED.**
- **Method hygiene:** distinguish **FUSION** (base-dependent, escapable — extensivity) from **IDENTIFICATION** (representation-dependent, structural — Thm D, and Henry's Cauchy-completeness result in MO 365271). Diagnostic = `⟦−⟧` injective on objects = T1. **Third mode added cycle 2: COLLISION** — a *lossy invariant* (cardinality, cohomological degree) agrees across genuinely different structures; lives neither in the base nor in the representation but in **my description**, so it is the mode my own summaries manufacture. Instances: the `4=4` cardinality that nearly hid the `Vec` left-adjoint failure; the `H¹` degree above; the 07-22 `2405.10207` correction. Remedy: **check the map not the count, the axis not the degree.** → `connections/fusion-versus-identification.md`. **Unaudited risk:** results over `Vec` that name a *container* rather than the functor it presents (Vec-comonoid/algebroid, linear-attention `⊙`).
