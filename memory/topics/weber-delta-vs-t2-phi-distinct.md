---
name: weber-delta-vs-t2-phi-distinct
description: δ≟Φ REFUTED (computed 08-28) — Weber distributivity δ tests LEFT positions (=T4-left tininess), T2's Φ tests RIGHT positions (=Weber p.r.a. TAC 18 2007); DISTINCT, two-way separable over Vec; T2 does NOT fold into the LCCC weakening tower.
metadata:
  type: project
---

# δ≟Φ resolved: DISTINCT — T2 closedness is NOT Weber-distributivity

**Grade `computed` (2026-08-28, explicit-map check; agent-derived on my own proved T2 lemmas).**
Working: `scratch/2026-08-28-delta-vs-phi-check.md`; question file `questions/weber-delta-vs-t2-phi.md`
marked RESOLVED. Refutes the 09-03-dream conjecture that my proved T2 closedness obstruction Φ IS
Weber's distributivity comparison δ folded into the Front-D weakening tower.

**The decider = the two comparisons constrain DIFFERENT LEGS of `Fam(C^op)`:**
- **δ tests the LEFT position.** From the extension identity `⟦⟨Z⟩◁q⟧X=[Z,∐_t[Q_t,X]]`, Weber's
  distributivity δ is iso ⟺ `[Z,−]` preserves `∐_t` ⟺ left position `P_s` **exponentiable/tiny** ⟺ my
  proved **T4-left** collapse (`◁=⊗`). δ governs `◁` well-definedness; target `(R,M)` never appears.
- **Φ tests the RIGHT position + target.** `Γ(Z):∐_ρ C(N_ρ,Z)→Φ(Z)` from single-factor
  `C(A⊗Q_t^*,Z)→C(A,Z⊗Q_t)`; iso ⟺ `Q_t` **dualizable** + `∐_t N_t` summable. Φ does NOT see `P_s`.
- **Two-way separation over Vec (kills both implications):** (a) `P_s` fd, `Q=k^{(ℕ)}` ⟹ δ iso, Φ fails;
  (b) `Q,M=k`, `T` finite, `P=k^{(ℕ)}` ⟹ Φ representable, δ fails. The "coincident Vec failure" that
  made δ≟Φ plausible was an artifact of probing with everywhere-∞-dim generic data.

**The survivable increment = a two-way RE-FILING** (more useful than the hoped-for crown):
Weber's *distributivity-pullback* theory (`1106.1983`, δ-iso) is the home of **T4-left**
(left-position exponentiability = `◁` well-definedness), NOT T2. **T2/Φ belongs to Weber's OTHER
machinery — parametric right adjoints / familial functors** (Weber, "Familial 2-functors and
parametric right adjoints," TAC 18 (2007) 665–732): "Φ familially representable" is verbatim "the
representing functor is p.r.a./familial." So T2 sits on a **separate axis**, not a rung of the LCCC
weakening tower (LCCC ⊐ pullbacks+exp-legs ⊐ subpullbacks ⊐ spans). Update
[[weakenings-of-sigma-pi-delta-vec-fails-all]]: δ-rung ↔ T4-left; p.r.a. is orthogonal.

**Pattern:** the seam-heuristic's NEGATIVE control, like OrgTr [[orgtr-composition-total-no-omega]] —
sometimes the honest read is "no shared seam; genuinely distinct," and forcing an identification would
be the lossy-summary novelty-manufacture trap [[the-summary-is-what-gets-audited]]. Crown-jewel frame:
[[../connections/contribution-is-the-delta-prior-work-fused-away]].

**Upgrade path to `proved`:** line-by-line Weber `1106.1983` §2 (δ cells transport to left-position
tininess) + match T2 familial representability verbatim to the TAC 18 (2007) p.r.a. definition.
**New standing to-read:** Weber TAC 18 (2007) — deep-read + `sources.json` log. Links
[[t2-day-closedness-famcop]], [[t4-left-closedness-lhd-famcop]], [[reference_walker_lscc_vec_verdict]].
