# Dream Journal — 2026-07-29

## What happened today
Full pipeline ran on Neil's 07-29 effects⊗coeffects steer.
- **WAKE** — Workers-vs-Contextads-A.4 **CLOSED orthogonal** (verified quotes: their grade is
  dependent `X→S` over fixed `S` via one monad's `seq`; mine is external `S` over all `(Set,×)`,
  cartesian `Δ(S×T)=ΔS⊗ΔT` — settles NO fragment of A.4; self-identify via their Example 3.24
  trivially-fibred colax-action corner). Spivak 2503.21974 race-risk **CLEARED** (neighbour, builds
  NEW base categories via density comonad; not my carrier). Neil steer: the entwining mixes a Kleisli
  arrow (effect) + coKleisli arrow (coeffect); arrows/profunctors = the FP answer; is it Plotkin–Turi?
- **PROVE** — effect–coeffect **arrows** `p⇝q:=Cont(G_M p,T_M q)` form a biKleisli category **iff M
  non-branching**; compositor is the **REVERSE** law `κ:GT⇒TG` (NOT the proved `λ:TG⇒GT`); assoc=E2′.
  Dichotomy: arrow face (κ, non-branching) vs bialgebra face (λ, all M) = Plotkin–Turi YES for λ.
  `effect-coeffect-arrows.json` = proved. Pf non-associative (explicit witness); Maybe & Writer/ℤ₂ = genuine.
- **LEAN** — `Workers.lean` sorry-free; `Worker.comp` state-multiplies axiom-free; 3 graded laws
  Quot.sound-only; `state-object-delta` T3 promoted proved→lean-verified.
- **WRITE** — Workers into book Ch7; both neighbour cites settled (A.4 via Ex 3.24; Spivak 2503).
- **BROWSE** — retargeted hard to the effects⊗coeffects landscape (see below).

## What I noticed
- **★★ CROWN — composition now has THREE modes, three obstructions, one grant question.**
  Directed axis (ZS): `[ω]∈H²(Sk_C;𝒟)`, may fail. State axis (Workers): none, always composes,
  grade accumulates `S×T`. Effect–coeffect axis: obstruction = **branching** (M arity ≥ 2), arrow
  category exists iff M non-branching. Three genuinely different constructions, three genuinely
  different obstruction *types* (cohomology class / nothing / a Boolean arity condition), all
  container-native and computable. Grant Path-5 spine, ready as prose. → NEW
  [[three-modes-of-composition]].
- **★★ Branching does DOUBLE duty in mode 3.** The 07-27 entwining and the 07-29 arrows are two
  faces of ONE entwined structure, and branching is the hinge: the **bialgebra face** `λ:TG⇒GT`
  exists for ALL M (Plotkin–Turi YES), the **arrow face** `κ:GT⇒TG` exists iff M non-branching. Same
  monad, same two feeds — the obstruction is *which direction* you commute the effect past the
  coeffect. This is the honest content of "arrows unify effects and coeffects for containers": the
  unification is unconditional (bialgebra), the *categorical* packaging is branching-obstructed (arrow).
- **★ Neil's arrow synthesis is genuinely OPEN.** The browse found every ingredient scattered —
  arrows=monoids (Heunen–Jacobs), coeffects=indexed comonads (Petricek–Orchard–Mycroft), graded
  eff+coeff distributive laws (Gaboardi et al.), a Chu-space/Day-convolution alt-unification
  (Katsumata–Rivas–Uustalu 1912.13477), a modern Plotkin–Turi continuation (Goncharov et al.
  2405.16708) — but **nobody has assembled them for containers/Poly, none framed as an arrow
  category.** MacBeth already owns the hard technical core (both entwining faces + the branching
  obstruction). Difficulty AND novelty both high.
- **★ Route (b) reuses my own machinery.** Katsumata–Rivas–Uustalu frame monad–comonad interaction
  as monoid objects in **Chu over (Endofunctors, Day convolution)** — and Day convolution is already
  load-bearing in my Dirichlet-tensor work ([[dirichlet-is-day-convolution]]). So the next PROVE has
  two routes: (a) build the Freyd/arrow category directly (already have the biKleisli result);
  (b) reframe the entwining as a Chu-space monoid object over Day — likely more tractable.
- **Process (recurring, 3rd+):** browse agents re-reported 3 already-deep-read items as "new"
  (Purdy–Damato 2503.17191, n-Café 2017 thread, Sannier–Baillot). The 07-28 fix (grep sources.json
  in dispatch) was only applied to the citation-trail agent. Paste it into EVERY agent's brief next time.

## What I consolidated
- NEW connection `connections/three-modes-of-composition.md` (crown jewel — the three-obstruction table).
- `questions/open-threads.md` — new 07-29 block (two PROVE routes; five read-in-full targets).
- SUMMARY.md dream block; MEMORY.md pointer.
- No registry changes (nothing demoted; reading note flagged no grade conflicts).

## Open threads
- **Next PROVE:** effects⊗coeffects, route (a) Freyd/arrow OR route (b) Chu-space/Day monoid object.
  Before attempting: read Petricek–Orchard–Mycroft "Coeffects" + Heunen–Jacobs in full;
  deep-read 1912.13477 (full text) and 2405.16708 (is the entwining a special case/dual of their
  dinatural higher-order GSOS laws?).
- Comonad-dual of Grodin–Spivak's Poly effect handlers: clean self-contained deliverable? (glue the
  proved cofree-comonad UP onto their Grothendieck-construction-of-handlers picture.)
- E2′ general-`j` case (inherited, mechanical); full Arrow interface (`first`/costrength) unchecked.

## Tomorrow
Wake: answer Neil — the arrow synthesis he proposed is genuinely open (parts exist, nobody
assembled them, especially not for containers/Poly); the branching dichotomy (arrow face
branching-obstructed, bialgebra face unconditional = his Plotkin–Turi YES); flag the Chu-space/Day
route (b) as the tractable one that reuses my Dirichlet machinery. Then reset triggers toward the
effects⊗coeffects paper (PROVE route (a) or (b)), a Lean target on the biKleisli composition, and
WRITE the three-modes table into the book/grant Path-5.
</content>
