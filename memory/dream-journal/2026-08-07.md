# Dream Journal — 2026-08-07

## What happened today
Full pipeline ran on the **proof-relevance boundary** (Neil's UID-91 challenge, conceded 08-06
wake → sharpened):
- **WAKE**: conceded Neil is right — Reader/State DO have a predicate lifting. Reframed the
  06-Aug "outside ∏-Mendler" refutation as narrower than its phrasing.
- **PROVE**: UPGRADED computed→**proved** (all 3 PROVE.md parts, general — all E, all S, all
  leaf-supported M). Part 2 (the real work): `□`-predicate lifting `M̂(X,P)=(MX,∀leaf.P)` on
  Sub(Set) is a monad lifting ⟺ `R` reverse-total (single test predicate `P₀=Lab(I(mm))`, no
  `decide`). Part 3: Reader/State reverse-total ∀mm, forward fails ⟹ □ lifts, `T_M` doesn't.
  **★ A ℤ/2 grading fell out** (`fourfold.py`, brute-force, zero mismatches): direction =
  (is-limit) XOR (is-proof-relevant). Node `proof-relevance-boundary` = proved.
- **LEAN**: `ReaderStateOutsidePiMendler.lean` §§4–6 certify the SURVIVING □ side beside the
  already-Lean'd DROP failure. 8 new decls, NO axioms at all (real `P:Prop`). Boundary now
  Lean-verified BOTH directions. Child `proof-relevance-box-lean` = lean-verified.
- **WRITE**: one remark + the ℤ/2 table folded into the book stratification box; 68pp, 0
  undefined refs. Honest `\prov` hedge on the Σ-candidate preserved.
- **BROWSE (08-07, replayed tonight)**: Neil asked me to cite Jacobs/Hermida for "fibred monad"
  and *skeptically* check the match. Pulled both PDFs, grepped, produced three verdicts.

## What I noticed (ASSOCIATE — the crown)
**The proof-relevance direction-flip IS the difference between the two canonical fibrations over
Set in the Hermida–Jacobs induction framework.** The SAME base monad `M` lifts along:
- **Sub(Set)** (posetal, predicates) → `□`, mult is an *entailment* → **reverse-total**;
- **Cont→Set** (proof-relevant, families) → `T_M`, mult is a *function* → **forward-total**.

"Data is sourced at its codomain; entailment is discharged at its conclusion" — proof-relevance
turns the property into data and swaps which end of `R` must be covered. This is not my
invention; it's *exactly* the posetal-vs-codomain fibration distinction that is the point of
Sub(Set) in Hermida–Jacobs 1998 (Inf. Comput. 145(2)). The container census `κ_μ` is their
reindexing; my "proof-relevant vs propositional" is their "codomain fibration vs subobject
fibration." **A genuine bridge: Path 2/3 (containers/Poly) ↔ fibrational logic.** →
`connections/proof-relevance-is-the-fibration-flip.md`.

Second noticing: **the citation audit and the proof-relevance result are the same fact from two
sides.** The audit says the word "fibred" (Hermida Def 5.4.1) *bakes in* Cartesian-preservation,
so `T_M` is a fibred monad IFF `M` cartesian — that's the crown boundary. The proof-relevance
result says `T_M` (proof-relevant, forward) can fail where `□` (posetal, reverse) survives.
Both locate the fragility on the `T_M`/proof-relevant side; `G_M` and `□` (the posetal / vertical
sides) are unconditionally fine. The fibration doesn't collapse the zoo — it **stratifies it,
now in 2 dimensions**: up the cartesian ladder (08-05) AND across the ℤ/2 leaf-lifting grade
(08-07).

Third: **meta-pattern, 5th instance.** Conjecture a flat refutation ("Reader has no lifting");
structure returns a finer 2×2 ("it has the reverse-total ones, `□` and `Σ`"). This is now a
reliable signal, not luck — I should *expect* refinement and pre-plan the retreat/expository.

## What I consolidated
- **NEW** `connections/proof-relevance-is-the-fibration-flip.md` (crown jewel): two fibrations,
  opposite totality, ℤ/2 grading, the three citation verdicts, book/grant placement.
- **UPDATED** `connections/fibration-stratifies-monad-zoo.md`: added the 2nd axis (ℤ/2 leaf
  grade) + the terminology verdicts (T_M lifting-not-fibred-unless-cartesian; G_M vertical
  fibred comonad ∀M; λ = entwining not BC).
- SUMMARY.md dream entry (top).

## Open threads
- **`Σ`-monad coherence** (node `sigma-monad-coherence-open` = in-progress). Laxator exists;
  unit-direction + assoc pentagon UNCHECKED. If it holds, Reader/State have a *proof-relevant*
  monad lifting (the Σ one) — a lovely object, and it would make the ℤ/2 grading a statement
  about *monads*, not just laxators. Next PROVE + LEAN target.
- **Is the parity dichotomy exhaustive?** Is every proof-relevant lifting `∏`/`Σ`/mixed? Open.
- **A non-`∏`, non-`Σ` proof-relevant lifting of Reader on Cont?** Still open (flagged to Neil).
- **Orestis's Agda** (Neil's #1) still not arrived; directed sweep deferred per Neil "after we've
  passed our pass." The two-sided Lean boundary + the fibred-monad citation map are the Cont-side
  groundwork ready for it (`for-collaborator/2026-08-07-orestis-agda-integration-map.md`).
- **Book Ch7**: the fibred-monad terminology fixes (cite Hermida 5.4.1 for T_M; Jacobs Ex 1.7.9
  only for the vertical G_M; λ = entwining, not BC) need to land in the Monads-and-Comonads
  chapter — the current draft still risks the loose "fibred monad" / "λ is BC" phrasing.

## Tomorrow (next wake)
1. Daily to Neil (CC Robin): report the proof-relevance boundary PROVED both directions +
   Lean'd both sides + the ℤ/2 grading; deliver the three fibred-monad citation verdicts he
   asked for (Hermida 5.4.1 not Jacobs 1.7.9 for T_M; λ = entwining not BC); ask whether to
   attempt Σ-coherence next or hold for Orestis.
2. Consider triggers: **PROVE** = Σ-monad coherence (unit+pentagon) — with expository retreat to
   liftings-over-Sub(Set) (Hermida–Jacobs, Katsumata) if stuck. **LEAN** = `reader_sigma_*` once
   PROVE lands past laxator. **WRITE** = fold the terminology verdicts into book Ch7 (gated on
   the daily / Neil's nod).
3. Register a small **reference memory** for the fibred-monad citation verdicts (currently only
   in `reading/2026-08-07-fibred-monad-citations.md` + the connection file) — they're
   load-bearing for the book and easy to lose.
