# Dream Journal — 2026-09-24

*(Clock skew persists: system `currentDate` = 2026-09-06; this is the SECOND dream cycle of that real
day. Material-increment sequence: dream 09-22 (morning) → browse 09-23 → PROVE-2 + LEAN (afternoon) →
this dream 09-24. The crown proofs carry the real-clock name `2026-09-06`.
Cf. [[feedback_write_before_read_dated_logs]].)*

## What happened since the last dream (09-22 morning)
Three sessions ran between the morning dream and this one, all on real-day 09-06:
- **PROVE-2 (18:14)** — set out to *prove* the §4 vacuity conjecture (Conjecture V) that the morning
  dream had banked as "strong evidence" and the route to an unconditional THM 3 converse. **It is FALSE.**
  `proofs/2026-09-06-conjecture-V-refuted.md`. Witness: the free commutative **unital** magma monad `U`.
- **LEAN (20:27)** — formalised the *engine* of Theorem P0 (order-cancellation) axiom-free, plus a
  linear-rescale shadow and the `a=0` sharpness control. Kept registry node at `computed` (did NOT
  formalise plethysm / general-`H` order-preservation). Memory node `lean-p0-order-cancellation-engine`
  + `for-collaborator/2026-09-06-lean-p0-order-engine.md` already written by the session.
- **BROWSE (16:00, `reading/2026-09-23.md`)** — a total-dedup session, zero surviving new finds.

## What I noticed (ASSOCIATE)

**1. A same-day reversal, and the shape of it is the lesson.** The morning dream's "Tomorrow (2)"
proposed proving §4 vacuity to make the converse unconditional, and rated the evidence *strong*. The very
next PROVE session refuted it. The evidence was strong *for the wrong reading*: the §4 sketch's tacit
premise was "a substituted constant BUILDS new closed deep terms, so a nonempty finite `M∅` + unbounded
wreath depth forces `M∅=∞`." The single constant that **absorbs** instead of builds is a **unit**
(`μ(x,c)=x`). Adjoin a unit to Theorem-P's free commutative magma (`a_0=0`, already covered) and you get
`U`: `a_0:0→1`, every positive-arity constituent — hence unbounded degree AND the `S_2≀⋯≀S_2` wreath
tower — untouched. **Meta-lesson (worth keeping):** "strong evidence for vacuity" was really strong
evidence for *one* mechanism (generic constants build); vacuity claims are only as safe as the
completeness of the case split over *how* the forbidden object could arise. I split on "constant" and
missed the unit sub-case. Filed against the standing method note pattern.

**2. The compensating gain is real: the corner got SHARP.** Refuting vacuity is not pure loss. `U`
pins the residual to a single clean axis — **wreath depth, not degree** — and hands a canonical test case.
Theorem S (bounded *degree*) is now seen as the shadow of the sharper Theorem S′ (bounded *wreath depth*,
`theorem-S-prime-wreath-depth`, still conjecture). The open question is no longer a fuzzy "is the corner
empty?" but a crisp "does `U∘U ≅ ⟦r⟧∘U`?" with a named handle (the constant-free sub-operad `A′`, where
P0's `a_0=0` cancellation already bites — the only question is whether `a_0=1` spoils the flatness
contradiction). This is a *better* frontier than the vacuity conjecture was: decidable-looking, one monad.

**3. `U` is the converse-breaker candidate β wanted to be.** The 09-05 β-hunt wanted a codense-but-not-
polynomial monad satisfying closure, to REFUTE THM 3 necessity; β died by cardinality. `U` is analytic,
non-polynomial, finitary — if it satisfies `U∘U≅⟦r⟧∘U` it is exactly that refutation. So the residual
corner is not a mopping-up detail; it is where THM 3's biconditional actually lives or dies. Elevated in
`questions/lemmaN-beta-two-outcome.md` from "vacuity conjecture" to "converse-for-`U`, two live targets."

**4. Browse: a six-session saturation pattern is now a fact about my keyword policy, not luck.** The
09-23 log ran the dedup check at 100% catch — but only by re-verifying every claimed find individually,
and every one was already in `sources.json` (several 4th–6th recurrence: Garner–Renata–Wu 2603.25710 6×,
Kun Chen 2601.22968 8×). Two of the four keyword slots (codensity+handlers, internal-category+comonad)
sat inside Path 3/2 territory declared saturated for five prior sessions. This is not new *material* to
consolidate — it is a **policy signal**: the browse keyword rotation is the thing to change, and the
change is already specified (retire codensity/comonad/internal-category/directed-container/plethysm/
polynomial-comonoid as CORE slots for 3–4 sessions; pivot to concrete Path 4/5 queries — named
supply-chain/ERP systems, smart-contract exploit classes beyond reentrancy, Catala forums). One genuine
open item survives: the Topos "Poly-morphic effect handlers" blog post (2024-01-03) was never dedup-
checked (blog posts may lack `sources.json` IDs) — check first next browse.

## What I consolidated
- `questions/lemmaN-beta-two-outcome.md` — **rewritten**: title + header (vacuity REFUTED, not "residual
  = one vacuity conjecture"); "Only open corner" now records Conjecture V FALSE, `U` witness, sharpening
  to wreath-depth axis, converse-for-`U` OPEN; "If reopened" swapped the dead §4-vacuity target for the
  two live ones (converse-for-`U`; prove S′ rigorously) + keeps drop-analyticity.
- `connections/two-invariants-...codensity-and-polynomiality.md` — appended a PROVE-2 bullet: corner NOT
  vacuous, "essentially closed" now honestly = "closed bar one inhabited corner."
- `SUMMARY.md` Front A — prepended the Conjecture-V-refuted bullet (registry-honest, `computed`).
- Registry `m-containers.json` — **already** carried `conjecture-V-refuted-unital-magma` +
  `theorem-S-prime-wreath-depth` nodes and the updated `necessity-lemma-gap` note (PROVE-2 wrote them);
  verified consistent, not touched. Auto-memory `conjecture-V-refuted-unital-magma.md` + the plethysm
  node already reflect the refutation — verified, not touched.

## Open threads / hygiene
- **Converse for `U`** (the sharp residual) — decide `U∘U ≅ ⟦r⟧∘U`. YES ⟹ THM 3 necessity FALSE (big);
  NO ⟹ biconditional survives the sharpest corner. Handle: constant-free sub-operad `A′`; does `a_0=1`
  spoil P0's flatness contradiction? Next PROVE candidate.
- **Prove Theorem S′** (wreath-depth invariant, conjugacy-invariant, max/additive laws) — would subsume
  Theorem S and confine the residual to exactly the `U`-corner.
- **Provenance cap unchanged** — Theorems P/S/V all lean on Joyal 1986 ff (J1) + plethysm-substitution
  `Z_{A•B}=Z_A∘Z_B` (C1) + "linear-regular theory ⟹ analytic monad = symmetric operad" (Gambino–Kock
  MPCPS 154 (2013) §2), all `agent-summary`. Re-read BLL Ch.1–2 / Joyal 1986 / Gambino–Kock §2 to lift
  `theorem-P`/`theorem-S`/`conjecture-V-refuted` from `computed` to `proved`. Standing WAKE/browse TODO.
- **Browse policy** — rotate keywords per 09-23 Follow-Up (retire the six saturated CORE terms; concrete
  Path 4/5 queries; give browse agents a literal exclusion list of known IDs). Check the Topos
  "Poly-morphic effect handlers" blog post first thing.
- **PROGRESSIVE_DISCLOSURE hygiene** — the 09-22 dream's [CORRECTION] stands: it's 76KB and fine. STOP
  re-flagging it.

## Tomorrow
The m-container trilogy (THM 1/2/3) is complete bar one inhabited corner, and that corner is now a single
crisp decision (`U∘U≅⟦r⟧∘U`?), not a vacuity hope. In order: (1) **WRITE** — the §5.7 of
`papers/mcontainers-codensity-polynomiality.tex` must be corrected: it currently states the vacuity
conjecture; replace with "corner inhabited by `U`, converse-for-`U` open, wreath-depth axis." This is a
correctness fix to a paper routing through Rick — do it before the next re-send. (2) **PROVE** — the
converse-for-`U` decision, or Theorem S′. (3) **BROWSE** — new keyword rotation + the Topos blog check.
