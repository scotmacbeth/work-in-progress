# Dream Journal — 2026-09-06

*(Clock skew persists: system currentDate reads 2026-08-29; dated 09-06 by the
material-increment convention, following the 09-05 entry. Since that dream the loop ran
cycle 2/2: WAKE (12:50), browse2 (15:01), PROVE (17:24), LEAN (19:32); WRITE and REVIEW
skipped, no triggers. One new `proved` theorem, one `lean-verified` upgrade, one browse
session that actually found something. WAKE/PROVE/LEAN had already folded their own results
into SUMMARY, registry and MEMORY correctly — audited, no corrections needed — so this dream
is mostly ASSOCIATE.)*

## What happened this cycle

- **PROVE (17:24) — sub-Q2 ANSWERED, negatively, and it got better on the way.**
  `fibredness-vs-left-closure`, `proved`, validator green, `verify.py` six checks green.
  BHM's "`▷` is not fibred in its left variable" is **NOT** my T4-left `◁`-closure
  obstruction. **Thm A (Set):** `(V)⟺(F)⟺(C)⟺|T|=1`. **Thm B (Vec_fd):** `(F)` always,
  `(V)⟺|T|=1`, `(C)⟺#{t:Q_t≠0}` finite ⟹ **`(V)⊊(C)⊊(F)` strictly**, witness `q=(ℕ,k)`
  fibred-but-not-closed. Two-sided, so reading-independent. Bonus: `◁` **is** fibred in its
  *right* variable with base functor literally `⟦q⟧`, and **both** variables preserve
  cartesian morphisms unconditionally — so non-fibredness is pure failure of
  *base-functoriality*, never cartesianness. Two of my own results got stronger: Workers
  Thm 2 superseded by a least-support lemma covering all `|T|≥2` (and `|T|=0`); T4-left
  Thm 3.1(2) sharpened to "infinitely many **non-zero** positions".
- **LEAN (19:32) — `WorkersRetract.lean`, sorry-free, five of six theorems axiom-free.**
  `r∘σ=id` and `r∘δ=Δ(d)` both by `rfl`; the optional L5 dichotomy (δ' coassociative but not
  counital) went through too, upgrading it from computed-at-`n=2,3` to machine-checked.
  Registry `P2-retract` / `P3c-diagonal-collapse` / `P3d-not-oplax-full-set` → `lean-verified`.
- **Browse2 (15:01) — the saturation advice worked.** Following the 09-05 instruction to
  diverge keywords, it recovered **Weber TAC 18(22):665–732 (2007)** in full (author's mirror,
  the caches-binary + `pdftotext` trick, found independently by two agents), corrected two
  speculative Path-5 filings, and added the CT Zulip public archive to the toolkit.

## What I noticed (ASSOCIATE)

- **★ The crown: three occurrences make a method, and the method explains my own strategy.**
  The PROVE note flags it and it is right: this is the **third** time two conditions that both
  look like "canonical map is iso" have turned out to constrain **different legs of one
  formula** (after T2's conjuncts A/B, and δ≟Φ). The resolving move was identical all three
  times. Its sharpest form is the **two-probe** diagnosis — one functional
  `G_r(Z)=Fam(⟨Z⟩◁q,r)`, a shape probe that sees *collapse* and a position probe that sees
  *summability*. And then the sentence I did not have before: **over `Set` there is exactly
  one collapse mechanism (`|T|=1`) and it trivialises summability too, so the two probes
  fuse.** Extensivity is not only what carries container theory — **it is what makes
  logically independent conditions indistinguishable.** That is the mechanism under my own
  crown meta-pattern: prior work keeps fusing seams because prior work works over an
  extensive base, and `Fam(Vec^op)` is therefore a *fusion-breaking instrument*, not just a
  generalisation. Neil's "use Vec as the running example" instruction now has a reason
  attached, not just an authority.
- **The browse handed the method a possible home the same day.** "Familially representable"
  is Weber's p.r.a. vocabulary, and the primary source stopped being a 2021 blog summary this
  cycle (TAC 18(22), 2007, Def 2.3/Ex 2.4/Ex 2.5, `deep-read`). A p.r.a. condition **is** a
  slice-indexed family of representability conditions — so my probes may literally be its
  slice objects. **Conjectural, and I am obeying the 09-05 lesson:** falsifier named in
  advance (over `Set`, is `L_q=(−)◁q` p.r.a. for every `q`? if yes, the re-filing is wrong),
  one session, nothing load-bearing until it runs.
- **A gate quietly opened.** The δ≟Φ `computed→proved` upgrade was blocked on deep-reads of
  *both* Weber papers. Both are now on file (`1106.1983` TAC 30 2015 for δ, 08-28; TAC 18 2007
  for Φ, 08-29). The blocker is gone; the mathematics still has to be written, so the grade
  stays where it is.
- **Weber Ex 2.5 is a boundary example aimed straight at my equivalence chain.** The
  free-category-on-a-graph endofunctor on `Graph` is p.r.a. but **not polynomial**. `Cat`
  therefore reaches me twice: as ◁-comonoids *inside* Poly, and as algebras for a monad
  *outside* the polynomial world. Is the co-side capturing what the algebra side cannot, or
  is Ex 2.5 genuinely outside container territory? Filed as a question, not an answer.
- **Fam is familial but not opfamilial (Weber §5.9/5.13) — and my `Cont(cod)` joint BC is
  one-sided in the same direction.** ∀/E carry co-Frobenius + Beck–Chevalley; ∃/A fail both,
  obstructed by co-topos non-distributivity (`∐∏ ≠ ∏∐`). Same construction, same left/right
  asymmetry, plausibly the same distributivity failure. Cheapest check named. Given the
  track record (three refutations of exactly this kind of identification), my prior is that
  they are **different**, and that would be the more valuable answer anyway.
- **Path-5 honesty correction: two schools, and only one can carry an obstruction.** Full
  reads killed a speculative filing — `2509.18475` and `2301.01445` compose by **structured
  cospans + pushout**, not Poly/comonoids. Siblings, not prior art. The structural payoff:
  pushout gluing is *total* so it has no obstruction class, exactly as OrgTr's total `#`
  has no `[ω]` — **totality of the primitive ⟺ absence of an obstruction**. A fourth mode of
  composition.
- **Process, positive control:** the 09-05 journal's two instructions (name the cheapest
  falsifier as the next-wake action; diverge the browse keywords) were both followed by the
  sessions that came after, and both paid — a theorem and a primary source respectively. The
  loop is using its own breadcrumbs.

## What I consolidated

- **`connections/one-representability-functional-two-probes.md`** — NEW, the cycle's crown.
  Three-instance table with registry ids, the method, the extensivity-as-fusion-machine
  sentence, the conjectural Weber-p.r.a. re-filing with its falsifier, and the carried
  honesty ledger (BHM = 2pp corroboration; `◁:=⊗` is a definition; PP `2601.15420` fibred-
  endofunctor definition still unextracted; "closed ⟹ fibred over an arbitrary base" open).
- **`connections/two-schools-of-compositional-applications.md`** — NEW. Dirichlet/ZS vs
  structured-cospan/ACSet, the totality⟺no-obstruction pattern, the power-grid Zulip lead.
- **`connections/cohomological-obstruction-family.md`** — instance **#8** appended:
  Bottman–Richardson `2606.13634` §3 (speculative, no construction; first LLM-side instance,
  Def 2.12 operadic consistency).
- **`questions/weber-pra-boundary.md`** — NEW, four questions (Q1 does p.r.a. sharpen Φ;
  Q2 the probe/slice falsifier; Q3 Ex 2.5 vs DCont≅Cat; Q4 opfamiliality vs my one-sided BC).
- **`questions/weber-delta-vs-t2-phi.md`** — gate-discharged block added; grade untouched.
- **SUMMARY.md** — two bullets (the method + the Path-5 correction). Nothing inflated:
  the conjectural parts are marked conjectural and the registry grades are unchanged.
- **Audited, no change needed:** registry (`fibredness-vs-left-closure`, `workers-retract-of-bhm-grading`
  incl. `lean-verified` upgrades), `sources.json` (Weber-2007 entry present with the explicit
  disambiguation from `1106.1983`; `2606.13634` upgraded; CSD/Aduddell corrections applied),
  `questions/workers-grading-vs-bhm-polynomial-grading.md` (sub-Q2 marked resolved).

## Open threads

- **Weber p.r.a. cluster (new, top of queue):** Q2's falsifier is the cheapest and the most
  consequential — it decides whether "one functional, many probes" is a named method or three
  anecdotes. Q1 then upgrades δ≟Φ.
- **Workers↔BHM sub-Q3 (still fully open):** Snoc (free-`ℕ`) / BHM (`▷`) / Workers (`⊗`) —
  three products on one line, Neil's own paper inside. Raised with him 08-29, no reply yet.
- **Outward, needs a decision, not a proof:** BHM's stated reason does not block their three
  listed fixpoints (all right-variable, and `◁` *is* right-fibred with base functor `⟦q⟧`);
  it blocks only their own `T_P`. Hedges is at Strathclyde. This should go via Neil/Robin, not
  from me directly — and gently, it is a 2-page abstract.
- **Cheap attribution debt:** fetch `2601.15420` and extract Pradic–Price's actual definition
  of *fibred endofunctor*. One locator closes a gap flagged in two proofs.
- **Carried:** Front C index-wise lift of `λ-inv⊊non-branch⊊cartesian⊊∏-Mendler`;
  Front-D survey pre-submission provenance debts (FGHW agent-summary, Street–Verity,
  enriched-Yoneda↔T1); Rick's drafted support-variety reply; MO 461865/296319/311510;
  nLab cross-refs; Hedges-duoidal compare-and-cite; Fujii–Lack `2507.05529` deep-read.
- **Browse:** core cluster still saturated; the divergence worked, keep diverging
  (Path-5 specifics, CT Zulip streams, p.r.a./familial citation trail). Stop polling the six
  standing MO questions for several sessions (three consecutive nulls).

## Tomorrow

Next WAKE: (1) inbox — Neil still owes T1 flagship + attention note, and the three-products
question from 08-29. (2) **Run Q2's falsifier** (`is (−)◁q p.r.a. over Set?`) before anything
else in this territory — it is one session and it decides whether the cycle's crown is a
method or an anecdote; if it survives, `/prove` or `/expository` on Q1 upgrades δ≟Φ to
`proved` with both Weber sources now in hand. (3) In the daily email tell Neil the fibredness
question came back **negative with a two-sided separation** — the more interesting answer —
and that the third occurrence has made the resolution move into a method; that paragraph is
better grant material than any single theorem. (4) Decide, with Robin, whether and how the
BHM note goes to Hedges.
