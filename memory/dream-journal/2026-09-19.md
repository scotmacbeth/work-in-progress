# Dream Journal — 2026-09-19

*(Clock skew persists: system `currentDate` reads 2026-09-05. Dated 09-19 by the material-increment
convention — last dream 09-18. Since then, the real-clock 2026-09-05 sessions landed genuine
mathematics (PROVE + WRITE) plus one browse (`reading/2026-09-19.md`). Cf.
[[feedback_write_before_read_dated_logs]].)*

## What happened this cycle

The first non-thin dream in several cycles — real new results to consolidate, not dedup:

1. **THM 3 promoted `computed → proved` (sufficiency)** by the 2026-09-05 PROVE.
   `proofs/2026-09-05-thm3-composition-polynomiality.md`; registry `m-containers.json` root `proved`,
   validator-green. M-containers compose (`⟦p◁_M q⟧_M ≅ ⟦p⟧_M∘⟦q⟧_M`) ⟺ `M` polynomial. Sufficiency
   line-audited (`p◁_M q = p◁⌈M⌉◁q`, associative, non-unital unless `M=Id`); necessity =
   cardinality growth-law half proved (fires at `p=q=Id`, kills `P⁺/P/D`, buries "commutative+affine"
   folklore); full converse OPEN = **Lemma N**, obstruction named = *trailing-M*.
2. **m-container trilogy written up:** `papers/mcontainers-codensity-polynomiality.tex` (13pp, clean,
   all 5 refs deep-read, honesty guards visible). Local; routes through Rick before `publishable-result`.
3. **Browse `reading/2026-09-19.md`** — deliberate pivot off Path 3 (mined out) to Path 1/2/4/5/6.
   Two deep reads: Iskander reentrancy (2606.01794), Hua–Xu π-clans (2602.05689). Mostly a coverage
   map confirming application-domain literature gaps.

## What I noticed (ASSOCIATE)

**1. The crown of the cycle is already named and now firmly proved: codensity ⊥ polynomiality.**
Two Kan-extension invariants of the *same* effect monad `M`, pulling opposite ways — **codensity
governs fullness (THM 2), polynomiality governs composition (THM 3)**. *Writer composes but isn't
full; D is full but doesn't compose.* This is the grant-Impact story for agent orchestration in one
line: an agent that "may decline" (Maybe/error lift) still **composes**; it pays for declining in
**fullness** (on-the-nose representability), not composability. Consolidated in
`connections/two-invariants-of-the-effect-monad-codensity-and-polynomiality.md`.

**2. Resolved a dream-surfaced table error (small but real).** The 09-05 WAKE stub filed the reader
monad `X^R` (|R|≥2) as "neither" (not full, doesn't compose), asserting it non-polynomial. WRONG:
reader **is** polynomial (`= y^R`, one shape / R positions), so by THM 3 it **composes**. Reader is
affine (`M1=1`) hence connected. So reader sits in the **polynomial-not-codense cell WITH
Maybe/error/writer**, not in a "neither" cell. The honestly-verified table now has three occupied
cells (both = Id; codense-not-polynomial = D; polynomial-not-codense = Maybe/error/writer/reader) and
a genuine **"neither" witness is OPEN** — a clean small question. (Honesty: reader's *non-codensity*
was not independently computed this cycle; inherited from the writer-failure analogy, flagged as such.)

**3. Iskander (2606.01794) is a genuine contrast case for [ω]=ε, worth flagging to Neil.** A
real-dollar-stakes Lean 4 verification of the OpenZeppelin reentrancy guard (DAO/Compound/Aave)
achieves composability by **pure proof conjunction** `⟨h_dao,h_compound,h_aave⟩` — zero categorical
machinery, no distributive law, no H². My [ω]=ε∈H²≅𝔽₂ account
([[lean-reentrancy-omega-equals-epsilon]]) treats the obstruction as essential. The reconciliation is
the point: **my account predicts WHEN cheap proof-conjunction composition is sound — iff the class
vanishes.** Fork to settle on the next reentrancy touch: are the three guard state-machines literally
independent (⟹ [ω]=0, confirming boundary instance) or is the "no-retrofit discipline" silently doing
the distributive-law work? → `connections/iskander-reentrancy-proof-conjunction-vs-omega-epsilon.md`.

**4. π-clans (Hua–Xu 2602.05689) = the admissibility question ONE LEVEL UP.** Minimal structure on
the *ambient* category (partial pushforward, weaker than LCC) rather than on the pole. Background for a
future "what is the minimal categorical structure containers actually need" push — adjacent to
Front-D's totally-ordered weakenings-of-Σ-Π-Δ tower ([[weakenings-of-sigma-pi-delta-vec-fails-all]]),
but about the ambient not the base.

**5. Coverage-map finding (browse, not math):** all three anchor citation trails (Ahman–Uustalu,
ZS-product hubs, founding container paper) show **no organic migration into the grant's application
domains** — supply-chain/blockchain/tax/orchestration connections would be *created* by Kodamai, not
followed. Worth stating plainly in any grant section that claims existing momentum.

## What I consolidated
- `connections/two-invariants-of-the-effect-monad-codensity-and-polynomiality.md` — de-stubbed;
  THM 3 sufficiency `proved`; reader cell corrected; paper recorded.
- `connections/iskander-reentrancy-proof-conjunction-vs-omega-epsilon.md` — NEW.
- SUMMARY Front A — new PROVE+WRITE bullet; WAKE bullet's stale "NEXT" marked superseded.
- sources.json — confirmed 2606.01794 + 2602.05689 already registered `deep-read` (no orphan).

## Open threads / hygiene (for the next WAKE, not this dream)
- **Lemma N** (closure ⟹ M polynomial) — the one gap to make THM 3 a full biconditional. Attack:
  run the classical familial-representability argument on the whole family of cardinality identities
  *without cancelling the trailing M* (Carboni–Johnstone shape). Next PROVE candidate.
- **A genuine "neither" witness** for the codensity×polynomiality table — not full AND doesn't compose.
- **HYGIENE — `PROGRESSIVE_DISCLOSURE.md` is ~262 KB** and errors on a plain Read (>256 KB). Needs the
  chronology→journal compression the SUMMARY got 2026-08-30 (keep Level-0 pointers, drop dated bullets).
  Flagged in the codensity connection file too. WAKE/dream task.
- **HYGIENE — auto-memory `MEMORY.md`** flagged by the PostToolUse hook for compaction (<17.1 KB
  target; currently ~20 KB, under the 24.4 KB hard read limit). The PROVE session deliberately
  deferred the full 60-entry compaction to a judgment-heavy cycle to avoid corrupting `[[slug]]`
  pointers. NOT done this dream (would consume the whole cycle and risk the record); explicitly queued.
- Iskander contrast case → flag to Neil (WAKE decision).

## Tomorrow
Highest-value move is a **PROVE on Lemma N** (turn THM 3 into a biconditional) — the m-container
trilogy is otherwise complete and written up. If instead a hygiene cycle: compress
`PROGRESSIVE_DISCLOSURE.md` first (it is currently unreadable), then MEMORY.md. Keep Path 3 off the
browse keyword list one more cycle; Path 4/5/6 (Iskander line, π-clans, Catala/GA) stay the viable mix.
