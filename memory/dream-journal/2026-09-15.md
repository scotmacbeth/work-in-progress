# Dream Journal — 2026-09-15

*(Clock skew persists: system `currentDate` reads 2026-09-03, and today's PROVE/WAKE/LEAN sessions
stamped their SUMMARY entries "2026-09-03". Dated 09-15 by the material-increment convention — last
dream 09-13, since then one browse (`reading/2026-09-14.md`) plus the Nat(F,id) PROVE, the status-memo
WAKE, and the VecCollapse LEAN all landed. Cf. [[feedback_write_before_read_dated_logs]].)*

*Light-to-moderate cycle. Audit first: today's three sessions had already self-consolidated — the PROVE
into SUMMARY line 23 + [[vec-admissibility-yoneda-rigidity]] + registry `conj-absorptive-dichotomy`; the
WAKE into line 24 (status memo to Neil `papers/status-containers-over-a-base-2026-09-03`, PROVE.md
redirected, ∞-copower sketch killed); the LEAN into line 27 + `agent.log` + `for-collaborator/
2026-09-03-lean-vec-collapse.md`. So the dream's real job was ASSOCIATE on the ONE unconsolidated input:
`reading/2026-09-14.md`.*

## What happened this cycle

- **REPLAY** — three 09-03 sessions, all already in SUMMARY:
  - **PROVE:** Nat(F,id)≅⊕_ℕ k^ℕ (THM 1, no-exotic), free⟹∞-dim summands (THM 2), **the wall located**
    (field engine works only for finite-dim target; direction A needs ∞-dim-target rigidity / Ext¹≠0).
  - **WAKE:** 6pp status memo to Neil (UID 141 wanted a catch-up); ∞-copower obstruction sketch
    stress-tested DEAD; PROVE.md redirected to (Q) is F=∏_ℕ∘⊕_ℕ projective in Add(Vec,Vec).
  - **LEAN:** `VecCollapse.lean` sorry-free (61 jobs) — the Theorem-D(ii) collapse witness, `p=({∗},k²)`
    and `p'=({1,2},k)` present the same `X↦X⊕X`, `shapes_not_equiv` axiom-free. Linear ext = Π-over-
    shapes; the single Σ↝Π shift IS the biproduct collapse.
- **BROWSE** — `reading/2026-09-14.md`, the sole unconsolidated input. Two consequential finds + texture.

## What I noticed (ASSOCIATE — the point of the cycle)

**1. The browse independently found the exact machinery for the wall the PROVE hit.** This is the browse
loop closing on itself: the 09-03 WAKE set the frontier keyword ("projective objects in additive/functor
categories, Baer–Specker generalizations") *from* the PROVE's wall, and the 09-14 browse returned
**arXiv:2407.04012** (stalk functors → intrinsic projective/injective criterion in `(Add(Vec,Vec);E)` —
the exact setting) and **Kaplansky 1958** (via Schröer's Baer–Specker note — the classical `∏∘⊕`
non-projectivity technique). The gap named a *mechanism* (Ext¹ / splitting an ∞-dim-target retract) and
the tool matched at that mechanism, not just the topic. → `connections/browse-found-the-machinery-for-
the-conj62-wall.md`, `questions/vec-admissibility-projectivity-crux.md`. Open worry carried: does
2407.04012 reach the non-Hom-finite, infinite-index case, or die at Hom-finiteness (where the wall lives)?

**2. Possible 15-year prior echo of my π₀-multiplicativity — Xarez 1112.4277.** "Stable units ⟺ finite
products of connected components are connected" (categorical Galois theory, 2011) reads verbatim as my
◁-admissibility criterion (Cor 3.1, 2026-09-02) — under a *different* sense of "admissible." This is the
crown meta-pattern in REVERSE: not a seam prior work fused, but a **vocabulary COLLISION** where a sister
subfield may already own my underlying lemma. If literal on a full read, Cor 3.1 cites Xarez for the
connected-components fact and claims only the ◁-bridge — a citation correction, not a demotion; the
◁-admissibility application stays novel. nLab (09-14) independently says products of connected objects
need not be connected — raises the odds the match is literal. → `connections/xarez-stable-units-vs-pi0-
multiplicativity.md`, `questions/xarez-stable-units-prior-art.md`. **This must be settled before the
admissibility paper is called `publishable-result`.**

**3. Dedup tax texture (no action).** Browse caught its own near-misses: Kim–Nam–Hur `2511.21093`
already deep-read; Garner–Renata–Wu `2603.25710` (Stone Duality for Monads) flagged "NEW" for the 5th
time — standing NOT-A-SCOOP. Both correctly discarded at synthesis. Discipline holding.

## What I consolidated
- `connections/browse-found-the-machinery-for-the-conj62-wall.md` (REPLAY×BROWSE association — new)
- `connections/xarez-stable-units-vs-pi0-multiplicativity.md` (possible prior art — new)
- `questions/vec-admissibility-projectivity-crux.md` (the (Q) crux + its two technique leads — new)
- `questions/xarez-stable-units-prior-art.md` (citation check — new)
- SUMMARY Front A: added the Xarez flag + the two technique leads for the wall.

## Open threads
- **(Q) projectivity crux** — multi-session; next PROVE reads 2407.04012 + Kaplansky 1958 first.
- **Xarez prior-art** — a `/expository` or citation-check pass (deep-read 1112.4277), not a PROVE.
- MO access broken again (WebFetch fails on mathoverflow.net) — try the Stack Exchange API path.
- Lamiaux et al. nested-inductive-types (POPL/HAL-only, bot-blocked) — re-check arXiv in a few weeks;
  Path-6 QPF sibling technique for genuinely *nested* shapes.

## Tomorrow
Next PROVE session on (Q): deep-read arXiv:2407.04012 (stalk functors, intrinsic projectivity in
`Add(Vec,Vec)`) BEFORE attempting — test whether it reaches `F=∏_ℕ∘⊕_ℕ` (non-Hom-finite, infinite-index)
or stops at Hom-finiteness. Pull Kaplansky 1958. If the criterion supplies `Ext¹(F,K)≠0`, that is (Q) NO
⟹ Conj 6.2 direction A. In parallel (low-cost, citation-affecting): schedule the Xarez deep-read before
the admissibility paper ships. Front C (indexed containers) remains the self-contained retreat if (Q) stalls.
