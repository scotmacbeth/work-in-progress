# Dream Journal — 2026-09-26

*(Clock skew persists: system `currentDate` = 2026-09-07; crown proof carries the real-clock name
`2026-09-07`. Material-increment sequence: dream 09-24 → browse 09-25 → PROVE (U-corner) → this dream.
Cf. [[feedback_write_before_read_dated_logs]].)*

## What happened since the last dream (09-24)
The 09-24 dream set a three-item agenda; all three are now DONE.
- **(1) WRITE — done.** Paper §5.6/§5.7 of `mcontainers-codensity-polynomiality.tex` corrected: the
  vacuity conjecture is gone, replaced by "corner INHABITED by `U`" (Prop) + open Problem
  `U∘U≅⟦r⟧∘U`. Re-sent to Rick (wip `842db92`, supersedes `b94bc32`). SUMMARY Front A line 24.
- **(2) PROVE — done, and the cliffhanger RESOLVED IN FAVOUR OF THE THEOREM.** The 09-24 dream said the
  converse-for-`U` is "where THM 3's biconditional lives or dies." **It lives.**
  `U∘U ≇ ⟦r⟧∘U` for every polynomial `⟦r⟧` (answer to (★) is NO). Necessity holds at `U`; THM 3 is an
  **unconditional biconditional for `U` and every named analytic monad.** Proof
  `proofs/2026-09-07-U-corner-resolved-stabilizer.md`, registry node `U-corner-resolved-stabilizer`
  (`computed`). SUMMARY Front A line 23. [[U-corner-closed-stabilizer-per-component]]
- **(3) BROWSE — done (`reading/2026-09-25.md`).** Saturation continues (6th–7th consecutive
  ~90% rediscovery session); the exclusion-list dispatch worked (zero excluded items reappeared).
  Genuine new finds all in Path 5 native venues, not arXiv.

## What I noticed (ASSOCIATE)

**1. The weapon that closed THM 3 necessity is a SYMMETRY invariant — and symmetry is exactly the
diagnostic that surfaces in the fresh Path 5 material.** The U-corner proof works because `M∘M` carries
a *correlated swap* stabilizer `C_2 = ⟨(01)(23)⟩` on the each-label-once component that no polynomial
outer `⟦r⟧∘M` can produce (polynomial outer ⟹ block-fixing products only, no part-swap). Presence of an
*unexpected* symmetry certifies genuine (non-polynomial) composition. The 09-25 browse's freshest find —
Catala tax-code issues #1095/#1096 — is the dual: a scope that *should* be symmetric in
(person1, person2) has that symmetry silently BROKEN by the wiring; absence of an *expected* symmetry
certifies a defective composition. Same principle, opposite sign. Consolidated as a connection (below).

**2. arXiv/citation-trail avenues for containers/Poly are now provably saturated — the live frontier is
Path 5 native venues (GitHub issues, DSL trackers), not papers.** Seven sessions of near-total
rediscovery, even after rotating away from the six saturated CORE keywords (W-types, traced-Poly,
island-models are NOT the previously-flagged stale terms yet still returned ~90% known). This is a fact
about the field's small literature footprint, not a keyword-choice bug. The two sessions that pivoted to
Catala's own GitHub repo are the only source of unambiguously fresh material two sessions running. The
GA-island-model × categorical-framing intersection returned a 4th–5th consecutive null everywhere —
confirmed no literature exists outside Robin's own GECCO 2026 work.

**3. cstheory 55588 is a public-output opportunity that directly exercises my codensity/Yoneda engine.**
The question asks for the relational-parametricity proof that `∀A.((A→A)→P)→Q ≅ P→Q` — a free theorem
forcing a natural family to factor through evaluation at `id`. This is *exactly* the shape of THM 2's
engine `Nat(Set(A,M−),H) = (Ran_M H)(A)`: naturality out of a representable factors through the identity.
Answerable directly from container/polynomial-functor first principles. Flagged as a candidate
"answer a real open question in public" (distinct from background-reading forum finds).

**4. A same-agenda success, cleanly executed.** Unlike the 09-24 dream (which recorded a same-day
*reversal* — vacuity refuted), this cycle records a same-agenda *confirmation*: the last dream named the
crisp decision (`U∘U≅⟦r⟧∘U`?), the PROVE session made it, and the answer preserved the theorem. The
frontier the last dream engineered ("decidable-looking, one monad") behaved as designed.

## What I consolidated
- `connections/symmetry-is-the-compositional-fingerprint.md` — NEW. Stabilizer invariant (THM 3
  necessity) ↔ symmetry-breaking-signals-wiring-bug (Catala #1095/#1096). Heuristic bridge, Catala side
  flagged `agent-summary`.
- `questions/open-threads.md` — appended cstheory 55588 (codensity/Yoneda public-output opportunity) and
  the Cockett–Lemay "Itegories" 2504.02409 read-next (thin provenance, iteration=trace, potential
  rival to Kondyrev–Spivak Int(Poly_*)).
- `SUMMARY.md` HOUSEKEEPING browse-rotation — 09-25 update (saturation now 6–7 sessions; Path 5 native
  venues = live frontier; Catala #1095/#1096 fresh; exclusion-list dispatch confirmed working).
- No registry changes: this cycle changed no belief about a registered claim (U-corner stays `computed`,
  correctly; general-stabilizer-necessity remains an open target, not a claim).

## Open threads / hygiene
- **General theorem `theorem-general-stabilizer-necessity`** — does the correlated swap escape `𝒫_M` for
  *every* non-flat analytic `M`? Would retire the plethysm/degree machinery entirely and make THM 3
  necessity uniform. Next PROVE candidate (the U-corner is the seed case).
- **Provenance cap unchanged** — THM P/S/V and the U-corner all lean on Joyal 1986 ff + plethysm-
  substitution `Z_{A•B}=Z_A∘Z_B` (Gambino–Kock MPCPS 154 (2013) §2), all `agent-summary`. Re-read
  BLL Ch.1–2 / Joyal 1986 / Gambino–Kock §2 to lift `computed → proved`. Standing WAKE/browse TODO.
- **Browse policy** — Path 5 native-venue pivot confirmed (09-25 Follow-Up #4): next browse try a
  *different* named system (a supply-chain/ERP OSS project or a smart-contract-auditing tool's issue
  tracker), not re-query Catala. Keep the exclusion-list dispatch; extend it with 09-25's confirmed
  rediscoveries.
- **Catala #1095/#1096 as a worked `/expository` example** — clean, citable, fresh (filed 2026-09-02,
  open) instance of the grant's "compositional correctness has economic consequences" narrative. Candidate
  only, not committed.

## Tomorrow
The m-container trilogy (THM 1/2/3) is now complete with THM 3 an unconditional biconditional for every
named analytic monad — no inhabited-but-undecided corner remains at `U`. In order: (1) **PROVE** — the
uniform `theorem-general-stabilizer-necessity` (swap-escapes-`𝒫_M` for all non-flat analytic `M`), which
would generalise the U-corner from "every named monad" to "every monad." (2) **WAKE** — reply to Neil's
3 pending confirmations on the THM 1 fork (side (i)? change-of-base spine? Cat-case now/later — SUMMARY
line 24). (3) **BROWSE** — new Path 5 named-system venue, per above.
