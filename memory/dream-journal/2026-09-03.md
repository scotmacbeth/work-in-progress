# Dream Journal — 2026-09-03

*(Clock skew persists: system currentDate reads 2026-08-28; dated 09-03 by the material-increment
convention — last dream was 09-02, and this cycle processes the material that post-dates it: the
`reading/2026-08-28` browse log and the 08-28 WRITE-session output. A focused dream: one browse + one
write to consolidate; no new proofs, registry untouched.)*

## What happened this cycle (since the 09-02 dream)

- **No new proofs, no registry changes.** T2 (`proved`), T4-left (`proved`), copowers gap (`proved`),
  `cont-cod-predicate-fibration` (`proved` 08-28), Q′ (`in-progress`) all unchanged. Correct.
- **One WRITE session shipped:** `papers/containers-over-a-base.tex` revised 12pp → 14pp, carrying the
  08-28 proved `Cont(cod)` fibration into a real **§5 "logic of containers"** (was a 40-line stub) —
  Fam-preserves-fibrations lemma, `Cont(cod)=Fam(cod^op)` bifibration, `A⊣Δ_c⊣E` = A/E liftings, the
  dualisation theorem (container hyperdoctrine = fibrewise opposite of Set's, each fibre a co-topos).
  for-robin note `2026-08-28-containers-survey-revision.md`.
- **One rich browse to fold in: `reading/2026-08-28`.** Its genuine payload = the top-priority
  direct-read finally discharged (Weber `1106.1983`), plus one new-territory Walker paper, plus small
  citable finds. Honesty note in the log: 2 of 4 "papers found" were dedup rediscoveries
  (Purdy–Damato 2503.17191, Kun Chen 2601.22968), caught before data loss.

## What I noticed

- **Q3 resolved, and it inverts my own guess (branch b, not a).** I had feared Weber's "categories with
  pullbacks" hypothesis was *literally satisfied* by Vec (abelian ⟹ all pullbacks), reopening a Front-D
  corner. The deep-read kills that cleanly: the name is misleading shorthand — Weber requires every
  polynomial's **middle leg to be exponentiable**, run through **distributivity pullbacks** (a canonical
  comparison **δ must become iso**). Exponentiability IS Vec's missing internal `Π`. So the
  abelian-pullbacks observation was a red herring; Vec fails Weber the same way it fails GK and Walker.
  The whole weakening tower now has Vec falling off the bottom with a *unified* mechanism, and every rung
  is deep-read except Street/comprehension. The discriminator thesis is firmer, not weaker.
- **The real increment isn't Q3's death — it's what fell out of it. δ≟Φ.** Weber's δ (distributivity
  comparison → iso) and my *proved* T2 closedness obstruction Φ (`⊗` on `Fam(C^op)` closed ⟺ Φ familially
  representable) are the **same logical shape**: "the tensor/composite has an internal hom / dependent
  product ⟺ a canonical map is invertible." They even fail over Vec at the same seam (T2's
  dualizable-and-summable corner = Weber's exponentiable middle leg). And my own T4-left collapse already
  showed `◁=⊗` exactly when positions are *tiny* = dualizable = a representability condition on `[Z,−]` —
  which is Weber's exponentiability flavour. So half the δ↔representability bridge is already built inside
  my proved work. **If δ=Φ, then T2 closedness is Weber-distributivity in disguise** — a proved result
  folds into the Front-D weakening tower. New question `questions/weber-delta-vs-t2-phi.md`. This is now
  the highest-value Front-D thread, ahead of any survey prose.
- **The seam-heuristic held again, twice.** (i) Weber "smooths over which pullbacks" — I named the seam
  (exponentiable middle leg) by direct-reading rather than trusting the summary's "mere pullbacks," and
  the correction was load-bearing. (ii) The δ≟Φ observation is itself a seam-find: two literatures
  (Weber's polynomial machinery, my Day-closedness) each state a "canonical-comparison-iso" criterion
  without noticing they might be the *same* criterion on `Fam(C^op)`. Consistent with
  [[../connections/contribution-is-the-delta-prior-work-fused-away]].
- **Small finds banked, not lost:** Walker `2208.13088` (derivatives via negation) recovers McBride's
  container derivative as `¬δ_p` at I=J=1 — relevant to the standing ∂p↔Weihrauch-degree open item
  (`weihrauch-containers-frontier`); worth reading Pradic–Price `2501.17250` against it. Hedges' "Poly"
  blog defines a duoidal linear-distributivity `a⊗(b▷c)→(a⊗b)▷c` — structurally my proved (⋉,⋊)
  duoidal/LDC result, uncited: a live group re-deriving what I have Lean-checked (same "cheap external
  visibility unclaimed" pattern as the dormant MO questions). MO 478413 (coreader comonads) = clean
  citable directed-container example. ACT 2026 accepted: Neil's "Snoc Trees", Polylang (Braithwaite–
  Hedges–Mihejevs), Aberlé compositional verification — fetch when posted.
- **Provenance debt flagged:** von Glehn TAC 33 (2018), now cited in the shipped survey §5, is NOT in
  `sources.json` (cited from starred memory). Must deep-read + log before any arXiv submission of the
  survey. Recorded in SUMMARY Front D and the for-robin note.

## What I consolidated

- **`questions/vec-subcartesian-closure.md`** — Q3 marked RESOLVED (branch b, Weber deep-read); next
  actions rewritten; δ≟Φ spun out as the new top thread.
- **`questions/weber-delta-vs-t2-phi.md`** (NEW) — the δ≟Φ conjecture: why plausible (same shape, same
  Vec failure locus, half-built by T4-left tininess), why it might be corner-only or false (`⊗` vs `◁`;
  external vs internal `∐`), and a cheap explicit-map decision procedure.
- **`connections/weakenings-of-sigma-pi-delta-vec-fails-all.md`** — Weber row upgraded to deep-read +
  "exponentiable middle leg / δ-iso"; Weber-rung verdict rewritten (branch b); δ↔Φ spinoff section added;
  next-actions reprioritised around it; survey-revision noted.
- **`topics/containers-over-vec.md`** — neighbour ledger: Weber deep-read + Q3 resolution + δ≟Φ pointer.
- **SUMMARY.md Front D** — Q3 resolution, δ≟Φ, survey-revision-shipped, von Glehn gap, Walker
  `2208.13088`, Hedges duoidal, arxiv_search tooling break.
- **Registry:** no grade changes (correct — no new proofs).

## Open threads (carried + new)

- **NEW top Front-D thread — δ≟Φ** ([[../questions/weber-delta-vs-t2-phi]]): write both comparison maps
  for a 2-shape family; decide same-map vs corner-only vs distinct. `/expository` or short `/prove`.
- **Front-D survey prose** (`/expository`): spine = the ordered weakening tower; §5 logic-of-containers
  now anchored. Owes: von Glehn deep-read + sources.json log (BLOCKING for submission); Street–Verity
  comprehensive factorization (TAC 2010) direct-read for approach (3); enriched-Yoneda reconcile w/ T1.
- **Front C (carried):** does `λ-inv⊊non-branch⊊cartesian⊊∏-Mendler` lift index-wise to DPUV's `IC_I`?
- **Q′ residual** (T1 copowers): non-extensive closed base with (A) not (C) — `in-progress`, Neil-gated.
- **∂p↔Weihrauch:** read Walker `2208.13088` negation operator vs Pradic–Price `2501.17250`; also their
  new follow-up "Problems with fixpoints of polynomials of polynomials" (Weber reverse-citer).
- **Purdy–Damato asymmetry:** "functional monoid actions ≠ matching pairs" — does the directed/comonad
  ([ω]∈H²) side of my ZS bridge have a counterpart? Re-read §6 before next orchestration `/write`.
- **Owed:** Rick support-variety reply (drafted, send when email on, CC Robin); Neil daily email;
  MO 461865/296319/311510 (multi-year 0-answer, machinery ready); nLab directed-container↔ZS +
  family-fibration↔poly cross-refs (both re-confirmed open); Hedges-duoidal compare-and-cite;
  von Glehn sources.json log.

## Tomorrow

Next WAKE: (1) check inbox for Neil (owes fuller word on T1 flagship + attention note; UID-125/132
steers). (2) **Highest-leverage concrete move = the δ≟Φ explicit-map check** — cheapest path to either a
new Front-D delta (T2 = Weber-distributivity) or a clean "corner-only" answer; do it before writing
survey prose. (3) Send Rick's drafted reply if email is on. Standing browse heuristic holds: for any
poly-over-a-base / weakening-of-LCCC paper, direct-read the *base hypothesis* (don't trust "mere
pullbacks"-style shorthand — it hid Weber's exponentiability requirement), and grep `sources.json` for
the arXiv ID first (dedup gap recurred an 8th+ time this browse).
