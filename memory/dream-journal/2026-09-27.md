# Dream Journal — 2026-09-27

*(Clock skew persists: system `currentDate` = 2026-09-07; results carry the real-clock name
`2026-09-07`. Material-increment sequence since last dream: dream 09-26 (U-corner consolidated) →
PROVE-2 (Theorem S′) → this dream. Cf. [[feedback_write_before_read_dated_logs]].)*

## What happened since the last dream (09-26)
The 09-26 dream's "Tomorrow" named the general theorem as the next PROVE. Instead the PROVE session
landed the **middle engine**: **Theorem S′** — bounded wreath depth `𝔥(A)<∞` (any `a_0`, any degree) ⟹
`Ã∘Ã ≇ ⟦r⟧∘Ã`, so closure ⟹ `M` polynomial. `proofs/2026-09-07-theorem-S-prime-wreath-depth.md`;
registry node `theorem-S-prime-wreath-depth` promoted **speculative → computed** (validator-green);
`scratch/2026-09-07-wreath-depth-ht.py` confirms every predicted `h`-value. The PROVE session already
updated SUMMARY (Front A line 23), PROGRESSIVE_DISCLOSURE, auto-memory
([[theorem-S-prime-proved-wreath-depth]]), and wrote `for-collaborator/2026-09-07-theorem-S-prime-proved.md`.
So the bookkeeping was done before this dream — the dream's job was purely to find the *shape*.

## What I noticed (ASSOCIATE)
**The shape: P / S′ / U are one diagnostic at three resolutions of symmetry.** This is the crown of the
cycle. All three THM-3-necessity engines detect the *same* thing — `M∘M` carries a symmetry no
block-fixing polynomial outer `⟦r⟧∘M` can produce — but at coarsening grains:
- **P** (plethysm cancellation) collapses symmetry into a cycle-index identity in `ℚ[[p_i]]` and cancels
  — global/algebraic, needs `a_0=0`.
- **S′** (today) keeps the *lattice of invariant block systems* and measures its **height** `𝔥(A)`;
  self-composition adds a level (wreath superadditivity), polynomial outer cannot (block-fixing ⟹
  `h=max`). Structural, any `a_0`, bounded depth.
- **U** descends to a *single* correlated diagonal swap `C_2` no block-fixing product contains.
  Pointwise, covers `a_0>0` unbounded depth at the one named witness.
Coarse→fine, each covers the case the previous drops. And the **residual is exactly where all three go
blind**: `a_0>0 ∧ 𝔥=∞`. That's not a coincidence to file away — it *names its own unifier* (a
support-indecomposable factor of `M∘M` outside `𝓕(A)`), which is the general PROVE target.

**S′'s invariant is imprimitivity depth = subgroup-lattice height.** `h_t(T)` = longest strictly-refining
chain of `T`-invariant block systems = max subgroup chain from a point-stabilizer to `T`. This is the
same block-system lattice that classical permutation-group theory uses for *primitivity* (primitive ⟺
`h_t=1`). We're reading a textbook invariant as a compositional certificate. The wreath superadditivity
law `h_t(T_1≀T_2)≥h_t(T_1)+h_t(T_2)` is the engine — self-plethysm literally stacks the lattice.

**It sharpens the crown, not just extends coverage.** "Composition ⟺ M polynomial" now reads:
polynomiality = flatness of the species = *no surplus symmetry survives self-composition*. Three
grains at which surplus symmetry is visible = three sufficient conditions for non-composability.

## What I consolidated
- `connections/necessity-trichotomy-is-symmetry-resolution.md` — NEW crown jewel. The P/S′/U table
  (invariant / grain / bites-when / blind-when / registry node), why S′ is the load-bearing middle
  (numerical certificate where U had existence and P had algebra), and how the residual names its own
  unifier. Links to [[symmetry-is-the-compositional-fingerprint]] (the fingerprint principle) — this new
  note is the *core-side stratification*, that one is the *core↔Path-5 bridge*; complementary, not dup.
- No SUMMARY/registry/auto-memory edits this cycle: PROVE-2 already made them and they are correct
  (Theorem S′ = `computed`, general theorem = open target, not a claim). The registry wins; nothing to
  reconcile.

## Open threads / hygiene
- **`theorem-general-stabilizer-necessity`** remains THE open target: does the surplus-symmetry escape
  exist for *every* non-flat analytic `M` (the `𝓕(A)`-outside-factor conjecture)? Hard sub-case =
  wreath-closed free magmas (`𝔥=∞`, no wreath escape, must peg-build a diagonal as for `U`).
- **Provenance cap unchanged.** P/S′/U all rest on Joyal 1986 ff + Gambino–Kock MPCPS 154 (2013) §2
  plethysm substitution, all `agent-summary`. Re-read BLL Ch.1–2 / Joyal 1986 / Gambino–Kock §2 to lift
  `computed→proved`. Standing WAKE/browse TODO — now load-bearing for THREE registry nodes, worth doing.
- **Neil still owes a reply** on the THM 1 fork (3 pending confirmations: side (i)? change-of-base spine?
  Cat-case now/later — SUMMARY line 24). Unchanged from 09-26; next WAKE.
- Browse saturation (6–7 sessions) unchanged; Path 5 native venues remain the live frontier.

## Tomorrow
1. **WAKE** — reply to Neil's 3 THM-1-fork confirmations; the trilogy (THM 1/2/3) is now complete with
   THM 3 an unconditional biconditional for every named analytic monad, so this is the moment to close
   the framing loop with him.
2. **PROVE** — `theorem-general-stabilizer-necessity` via the `𝓕(A)`-outside-factor frame; the wreath-
   closed free-magma sub-case is the crux (S′ can't reach it, U's peg trick is the model).
3. **Standing** — consider a WAKE/browse slot to deep-read the Joyal/BLL/Gambino–Kock plethysm sources
   and lift P/S′/U from `computed` to `proved` (one read, three promotions).
