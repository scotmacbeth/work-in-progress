# Dream Journal — 2026-07-28

## What happened today
Full Ch4 "Monads on Cont" pipeline ran on Neil's 07-27 **category of Workers** steer.
- **PROVE** — Workers = `(Set,×)`-graded category **PROVED** (`state-object-delta.json`).
  `ΔS=(S,s↦S)` codiscrete category → store/costate comonad `S×(−)^S`; a Worker `p→q`
  with state `S` is a container map `ΔS⊗p→q`; `ΔS⊗ΔT=Δ(S×T)` strict ⟹ composition
  multiplies context to `S×T`; Workers = coKleisli of graded comonad `S↦ΔS⊗−`. `⊗`
  forced (product tensor mis-sizes the fibre). Gaps: Para exactness (Core(Set)) and
  FKM packaging, both `computed`.
- **LEAN** — `StateComonad.lean`: `deltaDC` D1–D5 all **rfl / axiom-free** (cleanest
  DCont instance in the repo), store comonad, **Lemma 3.1 `ΔS⊗ΔT=Δ(S×T)` rfl**.
- **WRITE** — entwining (proved 07-27) folded into book **Ch7**; Neil's 5 additions
  landed (lit sentences, Ghani–Kurz teachbox, "one op two faces" paragraph,
  predicate-lifting language, Workers forward-pointer). Standalone entwining note =
  yes-eventually, after general-`j` or Lean.
- **BROWSE (2 same-day sessions)** — retargeted to Workers/graded/Para.

## What I noticed
- **★★ CROWN — the Workers proof lands on an UNPROVEN published theorem.**
  Capucci–Myers "Contextads as Wreaths" (2410.21889, App. A.2), finally deep-read via
  `download_pdf`+`pdftotext` after 2 sessions stuck at abstract-only, has
  **Theorem A.4 `Kl(T)≅Ctx(⊙)` left explicitly UNPROVEN** — polynomial monad `T`
  transposing to a dependently graded comonad whose grade multiplies by the monad's
  `seq`, "close to verbatim" on the Workers shape. Sharp open question (→ next PROVE):
  is `S↦ΔS⊗−` an instance of `⊙` (⟹ my coordinate proof settles a *case* of the open
  A.4) or orthogonal (external `S` vs dependent `X→S` grade ⟹ cite as neighbour)?
  Kept distinct from my own transfer `(S,P)↦(S,M∘P)` — three constructions, one
  neighbourhood. → [[workers-graded-and-contextads]].
- **★ Two axes of agent composition.** Workers = the **state axis** (context
  multiplies `S×T`, `(Set,×)`-graded, *no obstruction* — always composes, grade
  accumulates), dual to the ZS **directed axis** (`[ω]∈H²`, may fail to exist).
  Unobstructed-but-accumulating vs obstructed-but-non-accumulating. Grant Path-5
  duality. → [[workers-graded-and-contextads]] (II).
- **Para↔Poly↔graded-monad bridge is genuinely unwritten** (3 independent search
  angles). Novelty upside; no shortcut citation — build from Contextads + Gavranović's
  Para primitives. `Δ` functorial on bijections only ⟹ Core(Set) home question is real.
- **Ghani–Kurz CALCO 2007 Thm 3.2** (textually pinned): `X↦μY.X×FY` is a comonad =
  the previously-empty 4th quadrant of the free-monad/cofree-comonad 2×2 table. **BUT
  it's higher-DIMENSIONAL (Rogers/linguistics) trees, not higher-ORDER (HOAS)** —
  item-1 citation risk; confirm with Neil before citing.
- **Race-risk breadcrumb:** Spivak "Categories by Kan extension" CT2026 talk abstract
  says the paper builds categories/comonads "from **distributive laws of monads over
  comonads**" — on my entwining/transfer territory. Not in the abstract-level note of
  2503.21974; re-check the current version for whether it's public or talk-only.

## What I consolidated
- NEW connection `connections/workers-graded-and-contextads.md` (crown jewel).
- NEW topic `topics/graded-workers-para.md`.
- `questions/open-threads.md` — sharpened the Contextads item to the A.4 target;
  logged Ghani–Kurz quadrant + CT2026 race risk.
- SUMMARY.md dream block; MEMORY.md pointers; memory `workers-contextads-a4.md`.

## Open threads
- **Next PROVE:** settle Workers vs Contextads A.4 (instance / generalization /
  orthogonal). If instance: is my proof a proof of A.4's special case?
- **Next LEAN:** Worker graded-category composition laws (defeq-shaped, via
  `Container.dir₂`+`deltaS_tensor`) to promote T3 core to lean-verified.
- Re-check arXiv:2503.21974 current version (CT2026 race risk).
- Read Contextads §3.5 + App. A.2 fully before next Workers PROVE (locally saved).
- Confirm Ghani–Kurz higher-dim vs higher-order with Neil before citing item-1.

## Tomorrow
Wake: answer Neil (Workers proved+Lean'd; the A.4 open-theorem opportunity; the
two-axes framing; the Core(Set)/Para question). Then reset triggers toward
**Workers-vs-A.4** (PROVE) and **Worker composition laws** (LEAN).
