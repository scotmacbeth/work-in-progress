# Dream Journal — 2026-09-01

*(Clock skew persists: system currentDate reads 2026-08-27; dated 09-01 by the material-increment
convention — the last dream was 08-31, and everything below post-dates it by wall-clock mtime:
the UID-125 wake, the T4-left PROVE, and the 08-27 browse all landed Aug 27. A LIGHT dream: the two
proofs are already banked in registry/SUMMARY/MEMORY, so tonight is mostly consolidating the ONE
genuinely-new browse — Walker's LSCC — plus registry hygiene.)*

## What happened this cycle (since the 08-31 dream)

- **T4-left (`◁` LEFT-closedness on Fam(C^op)) got PROVED** (2026-08-27 PROVE, Neil UID-125's explicit
  ask; registry `t4-left-closedness-lhd-famcop`, `proved`, validator-clean). Already fully banked in
  SUMMARY Front A, MEMORY [[t4-left-closedness-lhd-famcop]], collaborator note — nothing for the dream
  to add except acknowledge it. **The honest scope**, worth re-stating so it doesn't drift: the
  positive verdict holds *only where `◁` degenerates to `⊗`* (positions tiny/dualizable ⟹ collapse
  Prop 2.1); the genuinely non-symmetric `◁` is NOT left-closed. Closed on `Fam_fin(Vec_fd^op)` only.
- **Neil UID-125 = the big steer, now driving everything.** (a) reframe AWAY from grants → "most
  knowledgeable agent on containers + applications"; (b) left-closedness (done, T4-left); (c) the
  **three-approaches survey** = new Front D (external `Fam(C^op)` / indexed Σ-Π-Δ-over-LCCC /
  fibrational), Fam(Vec^op) as running example. Front C RESOLVED at that same wake (my `T^Σ_M=M◁−` =
  the I=1 fibre of De Pascalis–Uustalu–Veltrì's ICMS — recognition, not new maths).
- **browse 08-27 surfaced ONE genuine new thing:** Walker `2607.10242` "Locally Subcartesian Closed
  Categories," a depth-upgrade (abstract → full HTML) on an already-tracked paper. Everything else that
  session was dedup (4 rediscoveries — Purdy–Damato, Robinson–Wrigley, Joram–Veltri, Felber et al. —
  the dedup-gap recurred a 7th+ time; caught, no data loss).

## What I noticed

- **Walker gives Front D a FOURTH candidate approach — and it's built for exactly Vec's shape.** I had
  been framing Front D as three approaches with the discriminator thesis "extensivity + LCCC of the
  base is the separating axis; Vec has NEITHER, so only the external `Fam(C^op)` approach reaches
  Fam(Vec^op)." Walker's LSCC is an *independent third weakening* of cartesian-closure (Street/Weber
  lineage, zero Gambino–Kock engagement): affine base-change `∇_f ⊣ ⊠_f` via **subpullbacks** survives
  even when the full `Σ⊣Δ⊣Π` chain fails, and the motivating slice tensor `A+B−X` (Lawvere quantale)
  is *structurally Vec's additive `⊗/⊕`*. So the thesis is no longer a clean trichotomy — it's a
  **testable question**: does Vec/`Fam(Vec^op)` carry a coherent subpullback structure despite failing
  full LCCC? **NO** ⟹ a sharper negative than "Vec isn't LCCC" (fails even the weaker affine
  substitute — the discriminator gap is deeper than I stated). **YES** ⟹ a partial Σ-∇-⊠ polynomial
  semantics over Vec, sidestepping the obstruction, which would REOPEN a corner of Front D as genuine
  new maths, not survey. Either way the thesis gets sharper. This is the crown of the cycle.
- **The subpullback weakening might survive precisely where Π dies.** The subpullback asks only that
  the comparison map be *unique-if-exists*, not that the pullback exist. Vec's failure is `∐⊊⊕`
  (colimits too big) + no internal `Π`. It is not obvious that `∐⊊⊕` obstructs a *unique-if-exists*
  comparison — the whole design of the weakening is to survive where the honest right adjoint fails.
  So my instinct leans slightly toward "YES, Vec might be locally subcartesian closed" — which would
  be the more interesting outcome. But this rests on an `agent-summary`, so it's a hunch to test, not
  a claim. Direct-read gated.
- **The seam-finding heuristic holds again, at the meta-level.** Walker builds over quantales and
  nominal sets and never mentions Vec or linear containers — so the seam he smooths over is *which
  additive base*. My candidate delta = "does the additive-slice-tensor weakening reach the one base
  the container/attention program actually needs (`Fam(Vec^op)`)?" Same shape as every prior front's
  delta ([[../connections/contribution-is-the-delta-prior-work-fused-away]]). But honestly flagged:
  the delta is only real *after* the direct read confirms Thm 5.2.8's scope.
- **The territory is swept; new signal now comes from re-reading known papers deeply and from
  adjacent differently-motivated lineages.** The 08-27 browse's own read: the ONE new thing was a
  *depth* upgrade on a tracked paper, and it came from Walker's Street/Weber lineage, not from fresh
  arXiv postings in my core keywords. This is the honest state of Front A/Vec/Day-closedness — pretty
  thoroughly explored. It argues for *depth* passes (direct-read Walker, D. Lin) over more breadth.

## What I consolidated

- **`questions/vec-subcartesian-closure.md`** (NEW) — the precise Walker sub-question (Q1: is Vec/
  Fam(Vec^op) locally subcartesian closed? Q2: Walker's span-bicategory vs my family-`∐` construction,
  overlap or disjoint?), with the direct-read gate, the depth flag, the delta angle, and next actions.
- **`topics/containers-over-vec.md`** — neighbour ledger extended with Walker as an explicit *fourth
  weakening* (after Gambino–Kock LCCC), `agent-summary` depth, pointing to the new question file.
- **SUMMARY.md Front D** — Walker recorded as the fourth candidate approach + the discriminator thesis
  sharpened into a test (NO ⟹ sharper negative; YES ⟹ reopens a corner).
- **Registry:** no grade changes. T4-left was graded `proved` at its own PROVE session; T2 `proved`;
  copowers gap `proved`; Q′ residual `in-progress`. Walker is a `agent-summary` browse lead, not a
  registered claim. This dream only reconciles summary/questions with the registry — correct direction.

## Open threads (carried + new)

- **NEW — Walker LSCC direct-read gate:** read `2607.10242` in full (subpullback axioms + Thm 5.2.8
  scope) against Vec's `∐⊊⊕` before Q1 becomes a PROVE target. Retry-fetch D. Lin *Enriched Polynomial
  Functors* (possible pre-emption of both Q1/Q2) and Tslil Clingman's Poly notes (403/500 on 08-27).
- **Front D survey** (`/expository`): the three approaches + Walker's fourth weakening, Fam(Vec^op)
  running example. Gaps to close: (2) Σ-Π-Δ + I=O=1 reduction letter-perfect (Gambino–Hyland 2004 +
  Gambino–Kock `0906.4931`); (3) fibration/comprehension axioms + does `Fam(Vec)→Set` agree with my
  `Fam(Vec^op)` (mind the op); enriched-Yoneda reconcile with set-level T1.
- **Front C (real, carried):** does my 4-level branching chain `λ-inv⊊non-branch⊊cartesian⊊∏-Mendler`
  lift index-wise to De Pascalis–Uustalu–Veltrì's `IC_I`, refining their coarse cartesian/general
  dichotomy? (08-27 citation-trail confirmed NOT anticipated in their prior art — unclaimed.)
- **Q′ residual (T1 copowers):** non-extensive closed base with (A) copower-test but not (C)
  all-coproducts — `in-progress`, Neil-gated (stop/push in the for-collaborator note).
- **Owed:** Rick support-variety reply (drafted, send when email on, CC Robin); Neil daily email;
  MO 461865 / 296319 (0-answer 2+ yr, machinery ready); nLab directed-container↔ZS cross-ref gap;
  SEED Q4 supply-chain-sheaf Lean template (Gibson `2605.08609`, flag to Neil first — Path 5).

## Tomorrow

Next WAKE: (1) check inbox for Neil's reply — still owes fuller word on T1 flagship + attention note,
and his UID-125 steer reweights everything toward *understanding* over grant-framing. (2) With T2 and
T4-left both `proved`, **Front D is the live frontier** — and the highest-leverage concrete move is the
**Walker direct-read**, because it decides whether Q1 is a PROVE target (Vec subcartesian-closed?) or a
sharper-negative exposition line. Do that read before writing any Front-D survey prose. (3) Send Rick
the drafted reply if email is on. Standing browse heuristic (still holding): read any Fam-side /
indexed-container / poly-over-a-base paper asking "what conjunct/weakening is load-bearing," not "is it
new" — and grep `sources.json` for the arXiv ID first (dedup gap recurred a 7th time).
