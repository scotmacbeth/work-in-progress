# Dream Journal — 2026-09-13

*(Clock skew persists: system `currentDate` reads 2026-09-02. Dated 09-13 by the material-increment
convention — last dream was 09-12; since then a WRITE (reentrancy standalone note) and a BROWSE
(reading/2026-09-12.md) landed. Cf. [[feedback_write_before_read_dated_logs]].)*

*Light cycle. The 09-12 dream had already consolidated the π₀ PROVE, the admissibility WRITE, and the
QPF browse. Two genuinely-new inputs remained: (a) the reentrancy standalone-note WRITE, and (b) the
09-12 reading log. Audit + ASSOCIATE + a compact CONSOLIDATE on those.*

## What happened this cycle

- **WRITE — Rick's Option 2 executed: `papers/reentrancy-machine-checked-obstruction.{tex,pdf}`** (7pp,
  clean pdflatex, wip commit `733b76b`, supersedes `e6aaa75`). The standalone note that makes
  `Reentrancy.lean` (sorry-free, no Mathlib) the **load-bearing artifact** — an *emphasis inversion* vs
  `containers-for-orchestration.tex` (which proves `[ω]=ε` analytically, Lean a corollary). Quotes the
  Lean theorems verbatim (`phi_omega`, `omega_inB2_iff_zero`), draws the verification boundary (Lean
  certifies the finite-𝔽₂ last mile; the categorical→complex reduction stays cited pen-and-paper,
  Prop 3.1). All WRITE.md binding constraints honoured: ZS-from-distributive-laws = Ahman–Uustalu 2013
  (they build no obstruction theory ⟹ `[ω]∈H²` is my delta); total/partial lens demoted to one
  paragraph = Gerstenhaber absolute-vs-classified (Annals 79, 1964), H¹ prediction retired, no novelty
  claimed; citation footprint floor = `deep-read`; no unsourced numbers. NOT `publishable-result` until
  Rick reviews. `for-robin/2026-09-02-reentrancy-standalone-note.md`.
- **BROWSE (reading/2026-09-12.md) — three items, one discharge, one self-correction, one texture.**
  (1) **Ahman–Kammar–Møgelberg LICS 2026** (`10.4230/LIPIcs.LICS.2026.4`) upgraded agent-summary →
  DEEP-READ (both an arXiv-agent and a web-agent fetched the DROPS PDF; no arXiv preprint exists).
  Quasi-Borel families = split fibration refining `Fam(C)` with measurable fibre structure; fibred Σ/Π,
  propositions universe, fibred probability monads via a distributive-law lift, fibred Kolmogorov
  conditional expectation (§9). Full-text confirmed ZERO container/poly/W-type vocabulary. Standing
  Front-D read-next DISCHARGED. (2) The 09-11 QPF-DOI "correction" (`.6`→`.17`) was ITSELF wrong —
  article 17 of ITP 2019 is Forster–Kunze, unrelated; the QPF paper is and always was article 6
  (`10.4230/LIPIcs.ITP.2019.6`). Fixed in `sources.json` (renamed back, `corrections` entry added), the
  deep-read technical content stands, the wrongly-pulled "reverse citers" were discarded. SUMMARY +
  auto-memory already carry `.6` correctly. (3) **MO 514525 "Coequalizers in extensive categories"** —
  negative answer (infinitary-extensive + regular ⇏ all coequalizers); Front-A background texture only.

## What I noticed

**1. The Ahman–KM discharge is a fourth clean instance of the crown meta-pattern — and the sharpest one
yet for the grant narrative.** Danel Ahman is a *co-author of my foundational equivalence* (directed
containers ≃ small categories). In 2026 he builds serious dependently-typed probability semantics
directly on the **family fibration** `Fam(C)` — the exact base of my `Cont(cod)≅Fam(cod^op)` bifibration
— and there is **zero cross-reference either way**. This is not prior art and not corroboration of a
theorem; it is a SIBLING: he equips the family-fibration fibres with *measure* structure, I equip the
fibrewise-opposite fibres with *logical* structure (A/E quantifiers, the dualisation theorem). Same
base, orthogonal decorations. It sits alongside the two-schools filing
([[two-schools-of-compositional-applications]]) and the crown meta-pattern
([[contribution-is-the-delta-prior-work-fused-away]]) — "read prior work asking what seam they smoothed
over": here the whole *container-logic* layer is simply absent from their picture, and my `Cont(cod)`
fibration is precisely that missing layer over the identical fibration. Grant value: the family fibration
is a live, load-bearing 2026 foundation (an LICS paper, a directed-container founder), which retro-frames
my logic-of-containers result as sitting on infrastructure the field is *actively* deepening.

**2. The reentrancy note closes a loop that has been open since 09-01, and it does so by SUBTRACTION.**
The move that earned a separate paper is not a new theorem — it is demoting almost everything: the
analytic proof becomes a cited input, the total/partial lens shrinks to one honestly-named paragraph
(Gerstenhaber), the H¹ prediction is retired outright. What is left standing — the machine-checked
finite-𝔽₂ last mile — is the only thing that was ever unimpeachable. This is the same discipline as the
crown meta-pattern read from the other end: instead of "my delta is what they omit", it is "my
*publishable* core is what survives when I strip everything I cannot certify". The honest inverse of
megalomania, again.

**3. No new mathematics this cycle, and that is the correct state.** Both inputs were execution
(a WRITE deliverable) and audit (a browse that mostly *corrected* prior sessions and *discharged* a
read-next). The registry saw no grade changes. The valuable output of a dream cycle like this is keeping
the top-line state honest — the reentrancy note is now recorded as DONE (not "recommended"), the Ahman–KM
read-next is DISCHARGED (not "standing agent-summary"), and the crown meta-pattern gained its fourth
instance.

## What I consolidated

- **UPDATED** `SUMMARY.md`: (a) ZS/reentrancy ledger — Rick's Option 2 recorded as EXECUTED (commit
  `733b76b`, the emphasis-inversion rationale, the honesty ledger); (b) browse-rotation housekeeping —
  Ahman–KM discharged + filed as SIBLING/crown-meta-pattern instance, MO 514525 noted as Front-A texture.
- **DREAM JOURNAL** this file.
- Registry: **no grade changes** (audit cycle). `sources.json` QPF-DOI already fixed by the browse
  session itself (`.6`), SUMMARY/auto-memory consistent.
- No new `connections/` file: the Ahman–KM point is a fourth instance of two existing crown connections,
  not a new axis — recorded inline with `[[links]]` rather than duplicated.

## Open threads (carried, unchanged unless noted)

- **Gap 1 (admissible ∧ non-collapse ∧ non-cartesian):** still only the *product* `Set×Vec_fd`; no
  irreducible inhabitant, no proof of emptiness. Conj 6.2 (absorptive dichotomy) OPEN — the corner the π₀
  characterization does NOT reach. The queued tractable first move (prove full `Vec` inadmissible via the
  ∞-copower obstruction `X↦[k^(ℕ),⊕_ℕ X]=∏(⊕)` not a coproduct of representables) stands in
  `state/PROVE.md`. [[vec-admissibility-yoneda-rigidity]].
- **QPF-as-collapse-template, unchecked:** does `Wequiv`/`Mcongr` quotient a *specific* collapse-locus
  container I have already built? A `/lean` pass — would upgrade [[qpf-is-the-tool-for-collapse-loci]]
  from `demonstrated (pattern)` to a worked instance, feeding the grant formalisation deliverable.
- **Reentrancy first-mile Lean target:** formalise orbit category + freeness (L) + defect `=(0,ε)` —
  currently the pen-and-paper input to the standalone note. Would make `Reentrancy.lean` end-to-end.
- **Pradic–Price §6 attribution corrections** (`scratch/2026-08-30-pradic-price-fibred-def.md`) STILL not
  applied to `proofs/2026-08-30-fibredness-vs-left-closure.md`. One `/write` chore, carried since 09-09.
- **Hedges compare-passes** — three queued, all cheap, none done (System-L = `(⋉,⋊)`?; additive-lenses
  CCC = biproduct collapse?; units-coincide-⟹-LDC mechanism match?).

## Tomorrow

Next WAKE, in priority order:

1. **Email Neil** — daily. Lead with the π₀ characterization (still the freshest headline; extensive-CCC
   corner of his #1 SOLVED). Second item: reentrancy standalone note shipped (Rick's Option 2, wip
   `733b76b`, routes via Rick for review per protocol). Third: Ahman–KM as a *grant* data-point — a 2026
   LICS paper by a directed-container founder building on the same family fibration as my `Cont(cod)`
   result, uncited both ways (the container-logic layer is the missing piece). No unsourced numbers
   ([[feedback_no_unsourced_numbers_leave_container]]); Gmail MCP may be absent, use `email_client.py`,
   verify CC to Robin in `mail/sent/`.
2. **PROVE candidate — Gap 1 irreducibility** (the `Vec`-inadmissibility first move in `state/PROVE.md`)
   or **Front C** (branching stratification lifts index-wise) — the only front not gated on someone
   else's paper.
3. **Consider a `/lean` trigger** for the QPF-as-collapse-template check (open thread 2) — turns a
   `demonstrated (pattern)` connection into a worked instance and directly feeds the formalisation
   deliverable.
