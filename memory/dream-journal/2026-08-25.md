# Dream Journal — 2026-08-25

*(Clock skew persists: system currentDate reads 08-22, agent.log 08-22; dated 08-25 by the
material-increment convention — this consolidates the 08-25 browse + WRITE, both above the 08-24 dream.)*

## What happened today

- **WAKE (08-25):** Neil's decisive steer — VCont *uses* first, (co)homology LAST; linear-ZS/Bourn = NO-GO
  for now (already in SUMMARY top block). Triggers reset to the applied front.
- **WRITE (08-25):** refined + staged the plain-language VCont note (`expository/vcont-plain-note.tex`,
  4pp, compiles clean). Key fix: §4 collapsed "four wirings → three" because in finite Vec `⊕=×`
  (biproduct), so "choose a branch" and "hold both" are the same combinator — presented honestly as the
  §2 biproduct collapse seen at workflow altitude. Three genuine wirings survive: `⊕` (menu/both),
  `⊗` (parallel/entangling), `◁` (pipeline). Email staged in `for-robin/…-READY-TO-SEND.md` (write
  session forbids email; next wake sends it).
- **BROWSE (08-25):** four-agent sweep chasing Neil's "find a USE" steer. Two crowns below.

## What I noticed

**★★ CROWN 1 — the base ZS/distributive-law connection may be Ahman's own; my `[ω]∈H²` is the layer on top.**
The web agent surfaced **Danel Ahman, "Distributive laws of directed containers"** (`danel.ahman.ee/papers/pi13.pdf`,
personal-site PDF, no arXiv ID yet). Search-engine summary (full text extraction FAILED — binary parse
error, so this is `agent-summary` at best) indicates it studies distributive laws `D∘C → C∘D` between
comonads in Poly and shows the resulting comonoid **recovers the Zappa–Szép product of monoids** when
`C,D` are monoids — i.e. *exactly* the `λ:N×P→P×N` combinator my [[orchestration-composition-is-zappa-szep]]
and [[cohomological-obstruction-family]] work is built on, tied to strict factorization systems. This is a
**core Path-2 author** (whom I already cite for D1–D5), so this is almost certainly the *source* of the
ZS-in-Poly connection, not a scoop I can claim. **Honesty consequence:** the SEED/grant "composition = ZS
product `C⋈D`" link (link (2) in the orchestration weld) is likely re-attributable to Ahman; the
**surviving novel layer** is the `[ω]∈H²(Sk_C;𝒟)` *obstruction* to that composition (re-entrancy = nonzero
class), which Ahman does not appear to build. This *sharpens* rather than weakens my stake — but only after
a direct read settles what pi13 actually contains. MUST read before the next `/write` on ZS/orchestration.

**★★ CROWN 2 — the Vec biproduct collapse is UNIVERSAL: Hedges' additive lenses are the same lemma.**
Jules Hedges' blog "Autodiff through function types" (julesh.com, 2026-02-20) shows the category of
**additive lenses is cartesian closed because commutative monoids have biproducts** (products = coproducts).
This is the **identical phenomenon** to my proved [[vec-biproduct-collapse-proved]] — `∐=⊕` making
either=both — sitting under autodiff/gradient types instead of Vec containers. Punchline (a *cautionary*
companion): using function types as a learning parameter space just re-derives ordinary deep learning with
overhead. Reinforces that "biproduct ⟹ either=both" is a **structural phenomenon wherever a linear/additive
structure sits under a container-like formalism**, not a Vec artifact. Directly strengthens the grant's
*universality* value and is a strong citation for the Vec "uses" note.

**★ Two concrete precedents for the Vec "uses" front (Neil's #1 priority), neither framed as "container":**
- **Vertechi, "Neural network layers as parametric spans"** (cats.for.ai / CT-Zulip mirror): NN layers as
  parametric spans via one indexed-sum `y[t(p)] += x[s(p)]·w[π(p)]` — structurally a container/polynomial
  shape with a **linear (Vec-valued) action on positions** = an existing precedent for "Vec-container = NN
  layer." Does NOT cite Abbott/Ahman/Ghani.
- **Bradley, "Category Theory Inspired by LLMs"** (Categories-for-AI course): category of linguistic
  expressions enriched over `[0,1]`, enriched copresheaves modelling text→semantic-representation — a real
  worked categorical-semantics-of-LLMs framework, companion/alternative to the Vec-container approach.
  My container framing is a genuine contribution **only if it unifies/sharpens these, not just restates them.**

**★ HELD linear-ZS/Bourn bridge reconfirmed SAFE, not scooped.** Citation-trail agent: the Bourn
`2310.05568` / Gran–Sciandra `2411.19238` citing cluster (Sciandra–Volpe, Bevilacqua, Ferri, Zuo,
Albano–Stefanelli, Rivezzi–Sciandra–Weber, …) is the same tight Louvain/Milan group; **no new paper builds
a transgression / five-term / LES or extends `Opext` into cohomological machinery.** Rathee–Yadav
`2601.12371` still cites zero of Bourn/Gran-Sciandra/Hopf-braces/semi-abelian — the "two disjoint H²
traditions" reading is *strengthened*. nLab *skew brace* entry is thin (no ZS/semi-abelian/cohomology) —
the bridge is genuinely under-documented, a negative result not a search gap. → [[cohomological-obstruction-family]] #8.

## What I consolidated

- **[[orchestration-composition-is-zappa-szep]]** — new 08-25 block: the Ahman pi13 attribution flag
  (base link (2) likely Ahman's; `[ω]∈H²` obstruction = surviving novelty; MUST direct-read pi13).
- **[[extensivity-is-the-container-boundary]]** — new block: Hedges additive-lens biproduct = same lemma
  (universality); Vertechi/Bradley precedents for the Vec "uses" front.
- **questions/ahman-pi13-zs-attribution.md** — new focused open question (the load-bearing read).
- SUMMARY top block + one MEMORY.md line (Ahman pi13 caution).

No registry demotions: nothing proved was touched. The Ahman pi13 flag is a *lead* (agent-summary,
extraction failed) — it raises an attribution question but confirms nothing until a direct read.

## Open threads

1. **TOP — locate + fully read Ahman pi13** (`danel.ahman.ee/papers/pi13.pdf`) with a PDF-capable tool,
   or find a companion arXiv/conference version. Settle: does it already contain the `[ω]∈H²` obstruction,
   or only the base "ZS products arise from distributive laws of directed-container comonads"? Determines
   how much of Path 2/3's ZS story needs re-attribution before the next `/write`.
2. Two new Spivak-orbit titles (Libkind–Spivak reverse-cites, no IDs yet): Spivak "Interactions that
   reshape the interfaces of the interacting parties" (2026) + Lynch–Lohmayer "Directories: …Hierarchical
   Organization…" (2025) — both in Neil's hierarchical-agent priority zone. Look up IDs + full-read.
3. Read Vertechi parametric-spans lecture in full — does it already formalize NN layers as containers/lenses
   (corroborate or partially pre-empt the Vec applied face)?

## Tomorrow

The next wake should: (1) SEND the staged VCont note to Neil (CC Robin) — it's one paste. (2) Set the
Ahman pi13 read as the browse/prove priority — it's honesty-critical for the whole orchestration story and
directly under the standing "READ POLY BEFORE CLAIMING" rule. (3) When finalizing the VCont "uses" note,
add a dedicated comparison paragraph measuring the container framing against Vertechi + Bradley, and cite
Hedges' additive-lens collapse as the companion universality example. Keep linear-ZS/Bourn HELD (Neil's
lowest priority + no-go). No new PROVE target ripe until pi13 is read.
