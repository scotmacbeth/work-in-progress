# Dream Journal — 2026-08-08

## What happened today

A four-phase work day, all on the proof-relevance / leaf-lifting front:
- **PROVE** — the day's crown. The Σ-container lifting `T^Σ_M`, which 08-07 verified is a genuine
  monad for Reader and State via a "canonical section" `σ`, turns out to be nothing but **left
  ◁-multiplication by `M`**: `T^Σ_M(C) = M ◁ C`. So it's a monad on `Cont` **iff `M` is a ◁-monoid
  = a container monad**. The open node `reverse-total-implies-coherent-section-OPEN` is **REFUTED**:
  `reverse-total ⟹ Σ-monad` is FALSE, witness **Bag** (reverse-total in the strongest way, but not
  a container — analytic not polynomial). `proofs/2026-08-08-sigma-monad-is-triangle-monoid.md`.
- **LEAN** — Σ-monad triptych machine-checked axiom-free for **both** Reader and State
  (`reader_sigma_*`, `state_sigma_*`, `*_proof_relevance_triptych` ∏✗/□✓/Σ✓); nodes
  `sigma-reader-diagonal-coherent`, `sigma-state-threading-coherent` = lean-verified.
- **WRITE** — book Ch7 rung reframed to Neil's **subobject-vs-codomain** language (2×2 ℤ/2 table,
  Hermida–Jacobs 1998 + Orestis Agda as independent witness with the type-valued caveat); 71pp clean.
- **BROWSE** — Urbat 2507.18509 (POPL 2026) deep-read; MO 302631 (Bag not polynomial); Functorial
  Aggregation + Karamlou–Shah DOI action items resolved.

## What I noticed

**Crown association #1 — the Σ-survivor lands on the directed-container spine.** The 08-07 ℤ/2 story
told me Reader/State survive on the Σ side but not *why* or *which* monads. `T^Σ_M = M◁−` answers
both as an **identity of endofunctors**: directed containers are ◁-*comonoids*; **container monads
are the ◁-*monoid* partner**, and Reader/State are the two motivating monoids. The proof-relevance
boundary's surviving half is not exotic — it *is* grant Path 2. New connection file
[[sigma-lifting-is-triangle-monoid]].

**Crown association #2 — "polynomial not analytic" is a recurring discriminator.** Bag fails the
Σ-monad for the *same* reason it's not a container: no natural leaf-assignment, the `Sₙ` symmetry.
This is exactly the July slogan [[polynomiality-is-provenance-is-coherence]] — polynomiality =
coherence = provenance-tracking. So reverse-total : ◁-monoid :: analytic : polynomial :: forgets-
provenance : tracks-provenance. The "extra structure" PROVE.md kept asking me to name was never a
bespoke directedness axiom — it's "`M` is a ◁-monoid", i.e. polynomial coherence. MO 302631's
Lumsdaine proof and the Fiore-et-al groupoid angle are independent community corroboration.

**Crown association #3 — the field builds tools only for the thin side.** Three independent
literatures (Urbat CLat⊓-fibrations *provably faithful* Lemma 5.2(1); Goncharov et al. bialgebraic;
Orestis's all-`Type` Agda routing through `∃`/`◇`) all live on the thin/□/subobject side. Thinness
*is* propositionality in my sense — so classical predicate-lifting technology structurally never
crosses to the proof-relevant `Cont`/`T_M` side. That's precisely where my contribution sits, and
now (08-08) *both* legs of that non-thin side have clean monad-lift criteria: **∏ lifts ⟺ `M`
cartesian; Σ lifts ⟺ `M` a ◁-monoid.** Both halves are crisp fibrational statements.

**Meta-pattern, 6th instance.** Conjecture a clean implication, structure returns a finer
distinction. This is now a reliable enough signal that I should *pre-plan* the expository retreat
whenever I set a flat-equivalence PROVE target on this territory. "Fair is foul."

## What I consolidated

- **NEW** `connections/sigma-lifting-is-triangle-monoid.md` — the crown (M◁− = ◁-monoid, Bag
  refutation, both-legs criteria table, field-only-builds-thin-tools, 6th meta-pattern).
- **UPDATED** `connections/fibration-stratifies-monad-zoo.md` — 08-08 section: the Σ leg now has a
  criterion (◁-monoid), completing the codomain-side picture.
- **UPDATED** `SUMMARY.md` (dream entry prepended) + `questions/open-threads.md` (08-08 dream).
- No registry changes — `reverse-total-…-REFUTED` was already logged valid during PROVE; no demotions.

## Open threads

- **Exhaustiveness of the ∏/Σ parity dichotomy** (still open): is *every* proof-relevant monad
  lifting of Reader/State one of ∏, Σ, or a mix? A non-∏/non-Σ lifting would be a lovely object.
- **LEAN the general identity** `T^Σ_M = M◁−` + "◁-monoid ⟹ Σ-monad" once — Reader/State become
  corollaries, replacing the two bespoke per-monad rungs.
- **Slattery–Sterling 2504.09520** (Hofmann–Streicher lifting of fibred cats) — read properly:
  does its 2-bifibration-of-fibrations sharpen "`T_M` fibred monad ⟺ `M` cartesian" or is it
  orthogonal (universe semantics)?
- **Sprott's MO 461865** ("when is a container a monad?") — cheap, high-goodwill community answer
  now fully in reach (T_M/T^Σ lift criteria + cartesian/◁-monoid classification). Not urgent.
- Book Ch7 framing decision owed from Neil (lead with `T^Σ_M = M◁−`?). Orestis full name / citation
  preference still owed (flagged in the write collaborator note).

## Tomorrow

Ask Neil (daily): (a) confirm the Ch7 reframe tone + whether to lead with `T^Σ_M = M◁−`; (b) his
UID-92 reply (couldn't check this cycle). Then the natural PROVE/LEAN unit is the **general
identity** `T^Σ_M = M◁−` and its Lean statement, which collapses two bespoke rungs into one theorem
and makes the whole Σ side a corollary of ◁-monoid theory. Watch cadence: VPO CT2026 → quarterly
(8+ nulls); Comonads-as-Spaces citation recheck 2–4 wks. Orestis directed Agda sweep still on hold
per Neil until "we've passed our pass".
