# Dream Journal — 2026-08-19

Second dream in two days — a short, tight consolidation of a single-front day. Everything today
was on the **linear-container (Vec) front**: PROVE closed Part 3, WRITE resolved the Schur
question, browse secured the primary extensivity citation. No LEAN (no trigger), no registry
demotions.

## What happened (08-19)

- **PROVE — Part 3 upgraded computed→PROVED.** `◁`-comonoids over `Fam(Vec_fd^op)` = **families of
  unital associative k-algebras**, one per shape, no cross-shape composition;
  `Comon_◁(Fam(Vec_fd^op)) ≅ Fam(Alg_k^op)`. The crown **algebroid guess is REFUTED in finite
  dim**. Hypotheses came out cleaner than PROVE.md guessed: f.d. positions only, **S arbitrary**.
  `proofs/2026-08-19-vec-comonoids-algebras.md`, registry `part3-comonoid-algebras` (proved).
- **WRITE — the Schur-coincidence question RESOLVED** and merged as §8 of the expository
  `containers-over-vec.tex` (17pp). Verdict `computed`: **not a scoop.**
- **BROWSE — a dedup-heavy day** (3 of 4 agents re-flagged already-tracked papers), but two real
  wins: nLab *extensive category* ("Vect is not even finitely extensive", Carboni–Lack–Walters
  1993) as the **primary citable anchor** for the extensivity crux, and Djament–Touzé `2407.10502`
  (strict poly functors over Fp-linear additive categories) as a genuinely new neighbour.

## What I noticed (the associations)

**★★ The crown of this dream: the Vec front is ONE collapse with THREE faces, and they are now all
proved.** The single fact — *linearity flattens the Set dependent sum* — reads on three layers:

| Face | Set structure | Vec collapse | Consequence |
|------|---------------|--------------|-------------|
| Objects (Part 1) | `F(1)=S` | terminal`=0`, `∐=⊕=Π` | `⟦S,P⟧≅Id^N`, shapes invisible |
| Morphisms (Part 2) | hom-`∐` = disjoint union | `∐⊊⊕` non-extensive | `⟦−⟧` faithful not full |
| Comonoids (Part 3, NEW) | comp-shapes `=Σ_s S^{P_s}` | flattens to `S×S` | `δ_shape` diagonal ⟹ algebroid dies |

Face 3 is the deepest reading of extensivity-failure. The Set dependent sum `Σ_s S^{P_s}` is
*exactly* the composable-arrow bookkeeping of `DCont≅Cat`; linearity replaces it by the plain
product `S×S`, so multi-object composition cannot survive — not "shapes invisible," not "hom not
full," but **the composition data itself destroyed**. I'd banked Faces 1&2 as "one phenomenon" in
the 08-18 dream; today Face 3 completes the triad and gives it its sharpest statement.

**★★ The Schur question is not a coincidence and not a scoop — it is the SAME degeneration.** A
`Vec`-functor (k-linear on homs) is *automatically* additive = homogeneous degree 1; that is not an
extra hypothesis, it is forced. Schur functors `S_λ` with `|λ|≥2` act as `λ^{|λ|}` on scalars, so
they are **not even `Vec`-functors** — they live in a different functor category above degree 1. The
only degree-1 Schur functor is `S_(1)=Id`. So in my corner the entire Young-diagram classification
degenerates to "the only indecomposable is `Id`, the functor is `Id^N`" — which is *precisely Face 1
of the collapse*. My additive lifting = the `n=1` term `M_1⊗W` of the analytic expansion
`⊕(M_n⊗W^{⊗n})_{S_n}`; a degree-1 truncation, not a disjoint world. **The Schur relationship and the
biproduct collapse are the same event.** Schur does not touch Face 2 (the morphism-crux) or Face 3
(the `◁`-comonoid classification) — those are mine. Adversarial catch banked: species DO carry
plethystic substitution = the analytic `◁`, and my strict `◁` is its degree-1 shadow — but the
shape-indexed comonoid classification is still my own.

**★ The fibrewise op strikes a third time.** `Comon_◁(Fam(Vec^op))≅Fam(Alg_k^op)` is the exact
`◁`/Vec analogue of the Set `Comon(Cont,⊗)≅Fam(Mon^op)` — one enrichment level up (monoid ⤳
k-algebra), and the reason is *position-contravariance*: `δ♯_s=μ_s:P_s⊗P_s→P_s`,
`ε♯_s=η_s:k→P_s`. A *co*monoid over Vec is an *algebra*. This is the same fibrewise op that runs
monad→comonad transfer and free/cofree — a fourth load-bearing appearance. The Vec front is not a
detour from Path 2/3; it is Path 2/3 read over a non-extensive base.

**★ The extensivity crux now has a textbook source, not just my diagnosis.** Before today "Vec is
not extensive" was corroborated only indirectly (Diers, Gambino–Kock). nLab states it as a named
fact. Face 2 of the collapse now cites Carboni–Lack–Walters 1993 in one line.

## What I consolidated

- Topic `containers-over-vec.md` — Part 3 COMPUTED→PROVED; new "three faces" table; Schur ledger
  line flipped to RESOLVED; crown target sharpened (strict `◁` refuted ⟹ lax/bimodule `◁` is the
  open crown); gaps 1&2 closed, gap 3 = lax `◁` promoted to top.
- Connection `extensivity-is-the-container-boundary.md` — added the comonoid-collapse (third face);
  primary-citation caveat updated (nLab extensive-category now indexed).
- Question `vec-schur-coincidence.md` — was already flipped to RESOLVED by the WRITE session; left
  as-is (consistent).
- SUMMARY head dream marker.

## Open threads

1. **The lax/bimodule `◁` over Vec for genuine algebroids** — the sharpened crown. Strict `◁` gives
   only disjoint algebras; a dependency-carrying composition must keep off-diagonal `P_a⊗P_b` alive
   as `Hom(a,b)`. This is the natural next PROVE (or `/expository` scoping) target on the front.
2. **Full-read Djament–Touzé `2407.10502`** — does the Fp-linear additive base route around `∐≠⊕`,
   or simply not need representability? Check before treating as background citation.
3. **Cybercat "Categorical Pipelines" graded category vs my Workers/(Set,×)-graded category** —
   monoid-annotated morphisms; same shape or superficial? Side-by-side read (browse open Q).
4. Standing debts: MO 461865 (container-monad answer) still unposted; §8 neighbour citations
   (Friedlander–Suslin, Sam–Snowden, Mitchell) still general-knowledge, deep-read before circulation.
5. **Dedup at the root:** Garner–Renata–Wu `2603.25710` re-flagged as "new" on 08-07, 08-18, AND
   08-19. Either deep-read it once (it's genuinely near DCont≅Cat — monads↔internal categories via
   retrofunctors in locales) or accept the orchestrator-side grep as the permanent gate.

## Tomorrow

The front's (co)monoid classification is now *complete* for the strict structure — three faces
proved, Schur situated. The next real content is the **lax/bimodule `◁`** (open thread 1): that is
where an actual k-linear category (algebroid) could finally appear, the true Vec-analogue of
`DCont≅Cat`. If Neil wants a written deliverable first, the three-faces + Schur-corner story is now
a clean, honest, publishable "why the naive linear generalisation of `DCont≅Cat` fails and exactly
which extensivity step kills it."
