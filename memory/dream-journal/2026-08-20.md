# Dream Journal — 2026-08-20

A short, single-front consolidation. The Vec front CLOSED today (WAKE resolved the crown, two WRITE
cycles shipped Part 4 of `containers-over-vec.tex`); the only *unconsolidated* material this cycle is
the **08-20 browse** (`reading/2026-08-20.md`), keyed to the two PROVE-shaped ideas that surfaced when
the front closed. No PROVE/LEAN/WRITE research to replay beyond what SUMMARY + MEMORY already hold. No
registry demotions.

## What happened (08-20)

- **WAKE — the lax-`◁` crown RESOLVED.** The genuine algebroid lives in the bicategory of Vec-matrices
  `Mat(Vec)`: objects = Vec-enriched graphs `(S,(P_{a,b}))`, composition the matrix product
  `(P◁Q)_{a,c}=⊕_b P_{a,b}⊗Q_{b,c}`. A comonoid there = k-linear category (algebroid); strict single-index
  `◁` = its diagonal degeneration. Classical (Bénabou/Mitchell/`1704.00329`) ⟹ EXPOSITORY, not PROVE.
  → [[vec-lax-matrix-crown-resolved]], registry `lax-matrix-crown-resolved` (computed).
- **WRITE ×2 — Part 4 of `containers-over-vec.tex`** (now 22pp, compiles clean). The four-part note reads
  as ONE obstruction: Vec is non-extensive (`∐⊊⊕`), and the algebroid reappears exactly when the matrix
  `⊕_b` moves the coproduct from the shape index to the *composition* index. Provenance box scrupulous.
- **BROWSE — the obstruction layer gets a scaffold** (the new material; see below).
- (Rick's V₄ Ext¹=0 test was 08-20 WAKE too — already banked [[rick-v4-ext-vanishes-transverse]].)

## What I noticed (the associations)

**★★ The crown of this dream: the whole cohomological-obstruction family lifts one enrichment level, and
the browse just handed me the load-bearing citation.** The Vec front resolved the algebroid *object* as a
`Mat(Vec)` matrix comonoid. The obvious sequel is the *morphism/composition* obstruction — a "Zappa–Szép
product of algebras" carrying a degree-2 class. And that is **exactly the group-side `[ω]∈H²(B;A)` spine
read one level up**: group ⤳ Hopf algebra, semidirect ⤳ smash product, `H²_Grp` ⤳ Hochschild/Sweedler.
The browse found **Mastnak `math/0210123`**: a five-term sequence for the cohomology of a *smash product
of cocommutative Hopf algebras* that **explicitly generalizes Tahara 1972** — the very group-side citation
the `[ω]` family already rests on. So the linear obstruction is not a new idiom; it is my idiom, one
enrichment up, with the Tahara→Mastnak lift already published. This stacks perfectly on the *other* thing
the Vec front kept showing: the fibrewise op runs monoid⤳k-algebra one level up (`Comon(Cont,⊗)≅Fam(Mon^op)`
⤳ `Comon_◁(Fam(Vec^op))≅Fam(Alg_k^op)`). **Both the object layer and the obstruction layer of Path 2/3
lift by the same enrichment step.** Consolidated as sighting #8 in [[cohomological-obstruction-family]].

**★ The niche is genuinely open — a first-mover signal, not a scoop risk.** Direct arXiv phrase search
"Zappa–Szép product [of] algebras" = 5 papers total, all 2004–2020, all group-theoretic or self-similar
C\*-algebraic; **zero 2025–26**. The `zappa-szep-product` MO tag doesn't exist. Same "unclaimed territory"
signal that cleanly closed several past fronts (holonomy-triviality-of-a-monoid-action, the 5-for-5 sibling
hunt). Worth flagging to Neil: either a real first-mover opportunity or a signal the linear-ZS framing
needs rethinking before a theorem attempt.

**★ Front #2 (h cohomological iff aligned) stayed starved — and the negative evidence is itself useful.**
Two independent pieces: (i) **MO 132410** — no general Lyndon–Hochschild–Serre spectral sequence exists for
knit/ZS products (subadditivity `cd(A⋈B)≤cd(A)+cd(B)` only conjectured); the group-cohomology community
itself lacks a tool for *un-aligned* ZS products, which independently corroborates that my "aligned"
restriction does real mathematical work (mirrors how (c') was scoped to aligned+abelian). (ii) **Conrad's
Mackey handout** — `Res_K Ind_H^G V` decomposes over the double cosets `H\G/K`, *literally the same
double-coset object* as the emergent-holonomy count `h(s)=|A\U/B|` ([[emergent-holonomy-meeting-points-proved]]).
So `h` is the representation-theoretic (dimension-of-Hom) cousin; the honest open question is whether it
coincides with a cohomological (Ext) invariant only on the aligned locus — which is exactly what Rick's
V₄ Ext¹=0 test already showed fails *off*-diagonal. Front #2 is **textbook (Benson/Mackey/Green) territory,
not preprint** — needs a computation-first or non-arXiv pass, not another browse.

## What I consolidated

- [[cohomological-obstruction-family]] — added **sighting #8** (the linear/Hopf rung: Mastnak `math/0210123`
  generalizing Tahara; group-crossed-product HH `math/0212003`; the open niche; measurings hub). All
  `agent-summary`, flagged for direct-read before load-bearing.
- [[containers-over-vec]] — crown section flipped OPEN→**RESOLVED** (was still listing lax-`◁` as open;
  now = `Mat(Vec)` matrix comonoid, EXPOSITORY Part 4, front CLOSED); added **NEXT LAYER** obstruction
  scaffold; header status line; gaps 3–4 rewritten.
- This journal + SUMMARY dream marker + open-threads 08-20 block.

## Open threads

1. **The linear-ZS obstruction** — HELD, not a trigger. Needs direct-reads of Mastnak `math/0210123` and
   `math/0212003` FIRST; then a coordinate check: does Mastnak's five-term sequence specialize correctly
   against `Mat(Vec)`'s `(P◁Q)_{a,c}=⊕_b P_{a,b}⊗Q_{b,c}`? First-mover niche — flag to Neil.
2. **"h cohomological iff aligned"** — HELD. Download+read Conrad's Mackey PDF (WebFetch failed on the
   compressed stream); this is Benson-textbook machinery, attempt computation-first, not via browse.
3. **D. Lin, *Enriched Polynomial Functors*** — direct-read before Part 4 circulates (does it already
   state the `Mat(Vec)` framing?). TODO carried from the 08-20 write log.
4. **Djament–Touzé `2407.10502`** — full-read still owed (does Fp-linear additive base route around `∐≠⊕`).
5. Standing infra: `mcp__research__arxiv_search`/`arxiv_get` 301-redirect loop — every browse this cycle
   routed through raw WebFetch. Report to Robin.

## Tomorrow

The Vec front is closed and clean. The next real content is the **obstruction layer** (thread 1) — but it
is correctly HELD: it needs the two direct-reads before any PROVE attempt, and a Neil steer on whether
"Zappa–Szép of algebras" is worth staking as a first-mover result or is a signal to rethink the framing.
If Neil wants a written deliverable meanwhile, the closed four-part `containers-over-vec.tex` is publishable
as "why the naive linear `DCont≅Cat` fails, and exactly which extensivity step kills it." Absent a steer,
the wake session should email Neil the front-closed summary + the first-mover-niche question, and let
browse/dream surface the next sharp target off-front.
