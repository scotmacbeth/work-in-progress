# Dream Journal — 2026-09-04

*(Clock skew persists: system currentDate reads 2026-08-28. Dated 09-04 by the
material-increment convention — last dream (09-03) processed `reading/2026-08-28`
(main) + the 08-28 WRITE output, but left the **second browse pass**
`reading/2026-08-28-browse2` unconsolidated. This is a focused, light cycle: one
browse log to fold in, no new proofs, registry untouched.)*

## What happened this cycle

- **No new proofs, no registry changes.** All grades unchanged and correct.
- **Consolidated the one dangling reading log: `reading/2026-08-28-browse2`.**
  Its payload was already in `sources.json` (von Glehn deep-read, both ACT-2026
  BHM papers deep-read, Murphy-Hernandez logged) but had NOT reached
  SUMMARY/connections/questions — the 09-03 journal predated it.

## What I noticed

- **The von Glehn provenance debt is actually CLOSED, and the 09-03 journal was
  wrong to still flag it as open.** browse2 fetched the full TAC-33 PDF and logged
  exact locators (Prop 6.9 p.1132 `Π_B:=(Σ_B)^op`; Thm 7.1 p.1134; Thm 7.2). This
  is the *exact* source for [[contravariance-is-fibrewise-op]] and the `Cont(cod)`
  dualisation theorem — the survey §5 citation is now airtight. Fixed the ⚠ flag
  in SUMMARY Front D to ✓. Lesson: a browse pass can discharge a debt that a
  later dream (dated by increment, not clock) still lists as owed — cross-check
  `sources.json` extraction level before trusting a journal's "owed" list.
- **The real crown of this cycle — Workers grading is a fibre of BHM's Poly-grading,
  and the generality is theirs.** Braithwaite–Hedges–Mihejevs (ACT 2026, Neil's
  former student Hedges + co) model Poly's composition product as a graded monad
  `T_P(X)=X▷P` over an **arbitrary polynomial `P`**. My proved Workers result
  (`ΔS`-graded `(Set,×)`-category) is conjecturally the `P=ΔS`-representable fibre.
  I have been generalizing *from* a state object; they already grade by any
  polynomial. This is prior-art to CITE, not a delta to claim — the honest inverse
  of the usual "my contribution is the delta they fused away" pattern
  ([[contribution-is-the-delta-prior-work-fused-away]]): here the delta is theirs.
- **The fibredness seam lines up from both sides.** BHM state `▷`(=`◁`) is "not
  fibred in its left variable"; my proved T4-left says `◁`-left-closure fails
  generically and repairs only at tininess/representability. Two vocabularies, one
  obstruction. That is a genuinely useful *independent confirmation* of T4-left,
  and a candidate mechanism-identification (is BHM's non-fibredness literally my
  tininess collapse?).
- **Three graded-monad pictures now converge on my Workers line.** Snoc Trees
  (Ghani–NF–Fish, Thm 3.5: `F_*`=free `ℕ`-graded monad), BHM (`P`-graded
  composition product), Workers (`ΔS`, `(Set,×)`-graded). Neil's OWN paper is one
  of the three. Worth raising with him directly rather than filing silently — a
  three-fibres-of-one-construction unification with the PI's paper inside it is
  exactly the kind of thing he'd want flagged.
- **Small find banked:** Murphy-Hernandez `2608.23866` — `Grp^op ↪ Comonads(Set)`
  fully faithful (`K_G(X)=X^G` = representable container `y^G`), group recovered
  from its comonad. Sits opposite to my [[reader-liftings-are-categories-done]]
  (classify the group FROM the comonad, vs build comonad from structure on `E`).
  Citable independent example; not urgent.

## What I consolidated

- **`connections/workers-grading-is-fibre-of-bhm-polynomial-grading.md`** (NEW) —
  the crown; the `P=ΔS` fibre conjecture + the fibredness-seam alignment + the
  three-way graded-monad convergence, all citations in machine format.
- **`questions/workers-grading-vs-bhm-polynomial-grading.md`** (NEW) — the cheap
  `X▷ΔS` explicit check as first step; queued behind Neil's active Front-D steer.
- **SUMMARY.md** — von Glehn ⚠→✓ (debt closed, locators); Workers ledger line now
  points to the BHM connection + question.
- **Registry:** no changes (correct — no new proofs).

## Open threads (carried + new)

- **NEW — Workers `ΔS` = `P=ΔS` fibre of BHM `T_P(X)=X▷P`?** Cheap `X▷ΔS` check
  first ([[../questions/workers-grading-vs-bhm-polynomial-grading]]). Raise the
  three-way graded-monad convergence (Snoc/BHM/Workers) with Neil.
- **Carried, still top Front-D thread — δ≟Φ** ([[../questions/weber-delta-vs-t2-phi]]):
  already RESOLVED distinct/computed per 09-03 dream + `weber-delta-vs-t2-phi-distinct`;
  survivable increment = the two-way re-filing (δ↔T4-left in Weber 1106.1983;
  T2/Φ in Weber's parametric-right-adjoint TAC 18 2007). Upgrade→proved needs the
  TAC-18 deep-read (browsing-gated).
- **Front-D survey prose** (`/expository`): spine = ordered weakening tower; §5
  now anchored + von Glehn citation airtight. Remaining pre-submission: Street–
  Verity comprehensive factorization (TAC 2010) still agent-summary; enriched-
  Yoneda reconcile with T1.
- **Front C (carried):** does `λ-inv⊊non-branch⊊cartesian⊊∏-Mendler` lift
  index-wise to DPUV's `IC_I`?
- **Owed:** Rick support-variety reply (drafted, send when email on, CC Robin);
  Neil daily email (raise the three graded-monad convergence); MO 461865/296319/
  311510 (multi-year 0-answer, machinery ready); nLab directed-container↔ZS +
  family-fibration↔poly cross-refs; Hedges-duoidal compare-and-cite.

## Tomorrow

Next WAKE: (1) check inbox for Neil (owes word on T1 flagship + attention note;
UID-125/132 steers still active). (2) If time before the Front-D survey work, run
the one-page `X▷ΔS` check to decide whether the Workers↔BHM fibre conjecture is a
citation win or a `/prove` target — and in the daily email flag to Neil that his
own "Snoc Trees" now sits in a three-way graded-monad convergence with BHM and my
Workers result. (3) Send Rick's drafted reply if email is on. Standing browse
heuristic holds (grep `sources.json` IDs first; direct-read base hypotheses).
