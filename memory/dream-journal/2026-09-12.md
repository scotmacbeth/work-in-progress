# Dream Journal — 2026-09-12

*(Clock skew persists: system `currentDate` reads 2026-09-02. Dated 09-12 by the
material-increment convention — the last dream was 09-09, and two reading logs (09-10, 09-11) plus
a PROVE and a WRITE landed since. Cf. [[feedback_write_before_read_dated_logs]].)*

*Cycle processed since 09-09: PROVE (the π₀ characterization, `proofs/2026-09-02-lemmaS-sufficiency.md`),
WRITE (admissibility paper upgraded to the π₀ story, `papers/`, HANDOFF filed), BROWSE ×2 (09-10, 09-11,
the QPF trail). **Audit first:** the PROVE folded itself into the auto-memory
(`lemmas-sufficient-extensive-pole-pi0`) and registry (`lemmaS-sufficiency-extensive-pole`) correctly;
the WRITE recorded itself in `agent.log` and `papers/HANDOFF-admissibility-2026-09-02.md`. The
unconsolidated inputs were (a) the top-line state of Front A after the π₀ result, and (b) the QPF browse
material. This dream is ASSOCIATE + CONSOLIDATE on those two.*

## What happened this cycle

- **PROVE — Lemma S IS sufficient; the extensive-CCC pole is COMPLETELY characterized (`proved`).**
  `proofs/2026-09-02-lemmaS-sufficiency.md`, verify `scratch/verify_distributive.py` 0 fail, registry
  `lemmaS-sufficiency-extensive-pole`. On a nontrivial infinitary-lextensive **cartesian-closed** base,
  `Fam(C^op)` is `◁`-admissible **⟺ the connected-components functor `π₀` preserves finite products**.
  Two theorems: **Thm 1** (extensive closed-monoidal + external distributive law (D) ⟹ admissible —
  constructs `◁` explicitly); **Thm 2** (extensive CCC: (D) ⟺ S-weak, i.e. `[P,T·1] ` a copower of `1`).
  **Cor 3.2 is the unification:** the census's *two* inadmissible extensive bases collapse to ONE
  criterion — `Set×Set` (disconnected unit) and `Gl((−)²)` (connected unit, bald edge) both fail
  `π₀`-multiplicativity. `π₀(Set×Set)` = coproduct, non-mult; `π₀(Gl)` = graph components, `π₀(K×K)=2`
  by Weichsel. **The KEY TENSION honoured:** `Gl` is a topos, so the *internal* Gambino–Kock
  distributive law holds there and is useless — admissibility needs the **external** (hom-SET) law,
  and external = internal ⟺ S-weak. This corrects the Cor 5.1 prose overreach in
  [[gl-inadmissible-pi0-multiplicativity]] ("Lemma S can fail to give admissibility" — FALSE; `Gl` fails
  Lemma S).
- **WRITE — admissibility paper upgraded from "open" to "complete characterization" (`papers/`, 21pp,
  clean pdflatex).** New §5 "extensive-pole characterisation", `π₀` column in the map table, two real
  errors fixed (the false "connected ⟺ admissible" in intro+Cor; the §6 overreach that `Gl` refutes
  Lemma-S-sufficiency when it actually fails Lemma S). Not pushed (email/browse off; protocol routes via
  Rick). Everything a notification needs is in `papers/HANDOFF-admissibility-2026-09-02.md`. One flag for
  Robin: `memory/code/citation_check.py` is not at the documented path.
- **BROWSE ×2 (09-10, 09-11) — the QPF trail, one genuinely-new bridge, and a five-session dedup tax.**
  The headline: **Avigad–Carneiro–Hudon "Data Types as Quotients of Polynomial Functors" (ITP 2019)** is
  now properly **deep-read** (pp. 1–10/19) and its DOI was **wrong** in the index — corrected to
  `10.4230/LIPIcs.ITP.2019.17` (was `.6`). A QPF = a polynomial functor `P=A▷B` plus a **split
  surjective** `abs : P ⟹ F` — i.e. a *retraction of a container onto a functor*. Live Lean 4 successor:
  `github.com/alexkeizer/QpfTypes` (WIP, not Mathlib-integrated). Two genuinely-new Zulip threads: an
  unanswered **Indexed QPF** request, and **"Ordinal notations and QPF"** speculating a QPF ≈ Girard
  **dilator/ptyx**. Plus **Ahman–Kammar–Møgelberg LICS 2026** (dependently-typed probability) built on
  the *same family fibration* as my `Cont(cod)≅Fam(cod^op)` bifibration — still `agent-summary`, the
  standing Front-D read-next.

## What I noticed

**1. The π₀ result is the capstone of the whole Front-A admissibility arc, and it changes what the arc
*is*.** Across 08-30→09-02 the story went: connectedness necessary (Thm B) → `Gl` shows connected is not
sufficient → and now `π₀`-multiplicativity is **exactly** necessary-and-sufficient on the extensive-CCC
pole, *subsuming both* the disconnected-unit obstruction (`Set×Set`) and the connected-but-inadmissible
one (`Gl`). The reframe from 09-09 — "the `◁` generality question has the wrong shape, the pole is rigid"
— now has its sharp form: **the pole is rigid *and* the exact gate is a product-preservation property of
one functor, `π₀`.** That is the cleanest possible answer to a corner of Neil's #1 priority: for the
extensive-CCC bases, admissibility is decidable by a single check. Front A's honest state is no longer
"trichotomy with gaps" but "extensive-CCC pole SOLVED (`π₀` mult); flexible pole via copower-tiny;
Gap 1 (the genuinely-mixed middle) still open."

**2. QPF is the missing formalisation tool for the identification/collapse loci — a real Path-6 ↔ Front-A
bridge.** This is the crown of the cycle. Theorem D and [[fusion-versus-identification]] say: when `I` is
disconnected, `⟦−⟧` is *not injective on objects*, so `({∗},k²)` and `({1,2},k)` present the same functor
`X↦X⊕X` and "`◁ := ⊗`" is a **choice**, not a deduction. In formalisation terms this is precisely the
statement that *the collapse-locus "container" is not a literal polynomial functor — it is a **quotient**
of one*. And Avigad–Carneiro–Hudon give the exact typeclass for that: `abs`/`repr`/`abs_repr`/`abs_map`,
a retraction of a container onto the functor it presents, with `Wequiv`/`Mcongr` for the (co)inductive
quotients. **So the objects my generality program keeps producing at the collapse pole have a known,
Lean-ready presentation pattern.** The IDENTIFICATION caveat carried verbatim on four proof files — "`◁`
is a choice of presentation" — is, on the formalisation side, exactly "work in the QPF, not the naive
container." This is the first time the *non-injectivity of `⟦−⟧`* has had a constructive upside rather
than a caveat. → new crown connection.

**3. The QPF ↔ dilator speculation is outside my territory but is a cheap grant-narrative win.** A QPF
being (a restricted) Girard dilator would tie the polynomial-functor / container program to
proof-theoretic ordinal analysis — a bridge nobody has drawn and one that lands squarely on the grant's
named "formalisation" deliverable. I cannot evaluate it (dilator theory is not in my store), so the
honest move is to flag it to Neil/Robin, not chase it. → `for-collaborator/`.

**4. The dedup tax is now a five-session standing cost, and the discipline that contains it is
synthesis-time index-checking.** 09-10 and 09-11 both re-surfaced already-`deep-read` papers (Comonads as
Spaces ×4, the two sheaf-multi-agent papers, Distributive Laws of Monadic Containers, Coco's citer) as
"new". Nothing false was added — caught at synthesis every time — but the cause is structural:
**sub-agents never see `sources.json`.** Not a memory action, a named operating cost. ([[check-scratch-before-dispatch]].)

## What I consolidated

- **NEW** `connections/qpf-is-the-tool-for-collapse-loci.md` — ★★ the Path-6 ↔ Front-A bridge: QPF
  `abs`/`repr` retraction = the constructive presentation of the identification/collapse-locus objects
  where `⟦−⟧` is non-injective. Cites Avigad–Carneiro–Hudon `10.4230/LIPIcs.ITP.2019.17` (deep-read),
  QpfTypes, and ties to Theorem D + [[fusion-versus-identification]].
- **UPDATED** `SUMMARY.md` Front A — π₀ characterization promoted to the current-state top line; WRITE
  shipped recorded; QPF/Path-6 line added to the ledger.
- **NEW** `for-collaborator/2026-09-12-qpf-tool-and-dilator-bridge.md` — the QPF-as-collapse-tool point
  for Neil (grant formalisation angle) + the QPF↔dilator flag, explicitly marked as outside my
  competence to evaluate.
- Registry: **no grade changes.** The π₀ node (`lemmaS-sufficiency-extensive-pole`) was already validated
  by the PROVE phase; this dream did not touch a proof status. Audit pass, not an edit.

## Open threads

- **Gap 1 (the genuinely-mixed middle: admissible ∧ non-collapse ∧ non-cartesian):** still no inhabitant
  beyond the *product* `Set×Vec_fd`, still no proof of emptiness for an **irreducible** one.
  Conj 6.2 (absorptive dichotomy, [[gap1-inhabited-setxvec-rigid-flexible]]) OPEN — needs the
  non-extensive-non-additive-fg locus. This is the one corner the π₀ result does **not** reach (it is
  extensive-CCC only).
- **QPF-as-template, unchecked:** does `Wequiv`/`Mcongr` actually quotient a *specific* collapse-locus
  container I have already built? Not verified — a `/lean` pass, and the natural next step for the new
  connection to earn its keep.
- **Ahman–Kammar–Møgelberg LICS 2026** (family-fibration probability) still `agent-summary`; the free
  DROPS PDF URL is logged. Read-next if Front D reopens — it is built on my exact base construction.
- **Hedges compare-passes** — now THREE queued: (1) System-L tensor/sequence = `(⋉,⋊)`? (2) additive-lenses
  CCC = biproduct collapse? (3) "units coincide ⟹ LDC" — does his mechanism match my proved
  `(Poly,⋉,⋊,y)` (units literally the same object `y`)? All cheap, none done.
- **Pradic–Price §6 attribution corrections** (`scratch/2026-08-30-pradic-price-fibred-def.md`) STILL not
  applied to `proofs/2026-08-30-fibredness-vs-left-closure.md`. One `/write` chore, carried since 09-09.

## Tomorrow

Next WAKE, in priority order:

1. **Email Neil — the π₀ characterization is the daily lead.** "The extensive-CCC corner of your #1
   priority is SOLVED: admissible ⟺ `π₀` preserves finite products, and this one criterion unifies both
   inadmissible bases we found (`Set×Set` and `Gl`)." Then: the admissibility paper is written up and
   handoff-ready (routes via Rick per protocol). Attach the QPF-as-collapse-tool point as a *formalisation*
   item and the QPF↔dilator flag as an *exploration* item — both grant-relevant, both honestly scoped.
   No unsourced numbers ([[feedback_no_unsourced_numbers_leave_container]]); Gmail MCP may be absent, use
   `email_client.py`, verify the CC in `mail/sent/`.
2. **Consider a `/lean` trigger for the QPF-as-collapse-template check** (item 2 of Open threads) — it
   would turn the new connection from `speculative` into a demonstrated tool and directly feeds the grant
   formalisation deliverable.
3. **Front C or Gap 1 for PROVE.** Front C (branching stratification lifts index-wise?) remains the only
   front whose next result is not gated on someone else's paper. Gap 1's irreducible-inhabitant question
   is the harder, higher-value target the π₀ result just sharpened by *exclusion* (it is exactly what the
   π₀ characterization does not cover).
</content>
</invoke>
