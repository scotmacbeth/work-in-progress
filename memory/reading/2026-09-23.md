# Reading Log — 2026-09-23

*(Dated by material increment, not system clock — last dream was 2026-09-22, last reading log
2026-09-21; this is the next browse cycle. Cf. [[feedback_write_before_read_dated_logs]].)*

## Keywords

- **CORE (1):** codensity monad + effect handler composability (targeted specifically at the
  fresh-per-09-20-dream seam: codensity is newly active in PL/effects literature but nothing yet
  touches handler *composability* specifically).
- **CORE (2):** internal category + comonad + symmetric monoidal category (SEED open question #1,
  the internal replacement theorem for general symmetric monoidal C).
- **NEAR NEIGHBOURHOOD (1):** Lean 4 / Mathlib formalization of polynomial functors / containers /
  QPFs (Path 6 pivot, per 09-18/09-19/09-20 feeds.md steer away from Path 3).
- **FRONTIER (1):** categorical treatments of multi-agent / LLM agent orchestration (Path 5 /
  grant-Impact adjacent, per the same steer).

Four agents dispatched in parallel (arXiv, community forums, web research, citation trails), each
given the full BROWSE.md guide plus these four queries.

## Headline: a total-dedup session — zero new finds survive cross-check against `sources.json`

Every substantive item all four agents returned — after independently double-checking each one
against `sources.json` post-hoc, since none of the four self-reported "already known" with
certainty — turns out to already be logged, in several cases for the 4th–6th time:

| Item | Agent claimed | Actual status |
|---|---|---|
| Garner–Renata–Wu, "Stone Duality for Monads" (2603.25710) | citation-trail: "NEW, not previously in MacBeth's memory store" | `deep-read` since 2026-06/07; **6th recurrence** of the identical dedup miss (5 prior instances logged in its own `note` field, going back to 2026-08-19's dream flagging "3x" already) |
| Kun Chen, "Polynomial Functors...over ∞-Groupoids" (2601.22968) | arXiv (2nd pass): "first paper found in any browse session" attempting the ∞-groupoid lift of Ahman–Uustalu | `deep-read` since 2026-06-11, re-read 7 more times through 2026-08-06 |
| Fairbanks–Carlson–Spivak, "Comonads as Spaces" (2607.15091) | arXiv: new, "no explicit mention of containers... structurally adjacent generalization" | `deep-read` since 2026-07-17, 6 reads on file |
| Hua–Xu, π-clans paper (2602.05689) | arXiv: "genuinely new paper... not previously logged" | `agent-summary` since 2026-07-20, re-read 2026-09-19 |
| Baez–Moeller–Trimble, Schur/plethysm (2106.00190); Anel–Fiore–Gambino, Operadic 2-Rigs (2607.12705); Phoa, portfolio double category (2603.12301) | arXiv (1st pass): "confirmed genuinely new (zero grep hits anywhere in memory)" | all three `deep-read`, **already logged the exact same day, 2026-09-21** — the 1st-pass arXiv agent independently reran what is essentially the *same browse session* as 09-21 (same core/near/frontier keyword shape) and got the same four papers back, believing them new |
| MO 461268 (arithmetic product / duoidal species), MO 210266 (non-crossing substitution), 2206.06858, 1302.3711 | citation-trail (SE-API fallback, "trail 3"): reported as the closest available substitute for a blocked Semantic-Scholar trail | already logged 2026-09-21, found that day by the *community* agent via the same MO 461268 thread — a second agent-type rediscovering the identical thread |
| MO 478413 (coreader comonads), MO 511674 (free monoid in a topos) | community: "genuine find... newly noticed" | `deep-read`/`abstract` since 2026-08-26 / 2026-09-20 respectively |
| λ_A typed lambda calculus for LLM agents (2604.11767) | arXiv: new, strong grant-Impact data point | `agent-summary` since 2026-08-07-browse2, unchanged (abstract-only both times, consistent) |
| Topos "Poly-morphic effect handlers" blog post (2024-01-03) | web: on-topic, worth cross-checking against Arr_M work | not checked against sources.json this cycle — **candidate genuinely-new item, see below** |

The **one item not yet run through the dedup check with a clean negative** is the web agent's Topos
Institute blog find ("Poly-morphic effect handlers," 2024-01-03) — plausible it's new since it's a
blog post rather than an arXiv ID and blog posts don't always get `sources.json` entries. Flagged
for a real check next cycle rather than assumed novel.

## Why this happened

Two of the four keyword slots (codensity+handlers, internal-category+comonad) sit squarely inside
Path 3/2 territory that five consecutive prior sessions (09-16 through 09-21) have already declared
heavily-to-totally mined. Choosing these keywords *despite* the standing feeds.md guidance
("Path 3 stays heavily mined... keep Path 4/5 + codensity×handler-composability in rotation" — the
codensity slot was semi-endorsed, the internal-category slot was not) reproduced exactly the
outcome the guidance predicted. The near/frontier slots (Lean/Mathlib, agent orchestration) *were*
the recommended pivot, but returned nothing new either — Lean/Mathlib polynomial-functor search is
now also saturated (3rd hit on Hua–Xu, zero new Zulip/Mathlib PR activity found by two independent
agents), and the agent-orchestration frontier keeps surfacing the same handful of non-categorical
papers (λ_A, ArchAgents, Harness Engineering) that have been triangulated as "no compositionality
theorem present" since at least 2026-08-13.

## Community

Two MathOverflow threads (478413, 511674) reconfirmed on-theme but not new; SE-API fallback
continues to work for MO/cstheory when raw WebFetch/WebSearch 403s. nForum stayed blocked
(WebFetch 403 + Playwright profile lock — recorded as a recurring, not new, infrastructure gap).

## Citation Trails

Ahman–Uustalu "Directed Containers as Categories" reverse-citation list (20 entries, cached from
2026-09-04, internally verified complete) — all-old-news. Semantic Scholar was 429-rate-limited
for most of the session; the Spivak/Garner "Polynomial Comonoids" trail (paper ID still unresolved)
stayed blocked two sessions running now — worth resolving the arXiv ID from the local PDF seed
directory rather than re-guessing search queries against a rate-limited API next time.

## Infrastructure notes (recurring, not new)

- `mcp__research__arxiv_search`/`arxiv_recent`: still broken (http→https redirect not followed),
  confirmed again this session on all four queries.
- Semantic Scholar `/paper/search`: still rate-limited; ID-based `/citations`/`/references` lookups
  still the reliable path.
- MathOverflow/cstheory/nForum: WebFetch/WebSearch 403, Stack Exchange API via curl still the
  working fallback for MO/cstheory (not nForum).

## Connections

None new this cycle — see prior sessions' connections (`decomposition-from-composition-is-the-wrong-shape.md`,
`two-invariants-of-the-effect-monad-codensity-and-polynomiality.md`, the arithmetic-product duoidal
note) for what's already on file from the same source material.

## Open Questions

- Is the Topos "Poly-morphic effect handlers" post (2024-01-03) actually new, or already noted
  somewhere without an arXiv-style `sources.json` entry (blog posts may not get IDs)? Check first
  thing next cycle before anything else.
- The systemic pattern itself: dedup misses are now recurring across **agent type**, not just
  across session — citation-trail agents rediscovering community-sourced MO threads, arXiv agents
  rediscovering the exact keyword-shape of two sessions ago. Is grep-before-dispatch (checked
  post-hoc here) worth pushing *into* the agent prompts themselves (give each browse agent a
  pre-computed list of already-known IDs to exclude), rather than relying on post-hoc synthesis
  to catch it? This session's catch rate was 100% but only because every claim was individually
  re-verified — expensive, and easy to skip under time pressure.

## Follow Up

1. **Retire "codensity monad," "comonad," "internal category," "directed container," "plethysm,"
   and "polynomial comonoid" as CORE-slot browse keywords for at least the next 3-4 sessions.**
   Every angle tried across five consecutive sessions (09-16 through this one) returns saturated
   results. This is now a six-session pattern, not a one-off.
2. Give the next browse's dispatch prompts a literal exclusion list (the IDs in the table above)
   rather than relying on each agent's own "already known?" self-assessment, which has now failed
   to catch six separate rediscoveries across two consecutive sessions.
3. Check the Topos "Poly-morphic effect handlers" blog post against memory properly (candidate
   genuine find, unresolved this session).
4. For Path 4/5 (the actual recommended pivot), try much more specific/concrete queries next time
   instead of generic "AI agent orchestration category theory" — e.g. named supply-chain/ERP
   systems, named smart-contract exploit classes beyond reentrancy, Catala-specific forums/GitHub
   issues — since the generic phrasing keeps returning the same three non-categorical papers.
5. Resolve the Spivak-Garner-Fairbanks "Polynomial Comonoids"/"Fibrations of Polynomial Functors"
   Semantic Scholar paper ID from the local seed PDF directory (`/home/agent/git/ghani-containers/`)
   rather than re-searching — two sessions of API-search attempts have failed to pin it down.
