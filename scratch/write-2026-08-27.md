# Write session 2026-08-27 — "Approaches to Containers over a Base" survey

## Provenance caveat (recorded)
- WRITE.md NB says "check Neil's reply first." This is a WRITE session (no email allowed).
  Proceeding as directed by WRITE.md main body; noting the caveat in the email note at the end.
- Walker 2607.10242 was at `agent-summary` in sources.json. I READ the on-disk verbatim text
  (/home/agent/papers/walker_lscc.txt) FIRST-HAND this session — Def 4.0.1, Rem 4.0.2, fn 15,
  Rem 4.0.4 — and upgraded the entry to `deep-read` with a locator recording exactly that.
  So the fourth-approach section rests on my own reading, not a browse-agent paraphrase.
  Still frame the Walker VERDICT as a reading/understanding result (computed-level), per guardrail.

## The one-sentence seed
**Extensivity and local cartesian closure of the base category are the two axes that separate
the rival notions of "a container in a category"; the linear base Vec, which has neither,
collapses all approaches but the external-family one — and the fullness/closedness dichotomies
(T1/T2/T4) are the quantitative shadow of that collapse.**

## Audience / venue
Internal expository first. Audience = Neil + container/Poly community. Assume: containers, Poly,
LCCC, Day convolution, enriched categories. Do NOT assume T1/T2/T4 — prove in my own words.

## Citable (deep-read) references
- Dorta–Jarvis–Niu 2305.05655 — ⊗,◁ over general ΣΠV base; ◁-comonoids ≃ enriched cats (Thm 4.2).
  MUST cite for the tensor/composition DEFINITIONS; my delta = closedness/fullness they omit.
- Gambino–Kock 0906.4931 — polynomial functors over LCCC; Vec not LCCC (boundary cite).
- De Pascalis–Uustalu–Veltrì 2509.25879 — monoid structures on indexed containers; my M◁− = I=1 fibre.
- Walker 2607.10242 — LSCC (fourth weakening); Vec NOT LSCC (my direct-read verdict).
Non-arXiv classics to cite by name (foundational, standard): Abbott–Altenkirch–Ghani (containers,
FoSSaCS'03/TCS'05); Diers (familial representability); Day (LNM 137, 1970, Day convolution);
Niu–Spivak (Poly book). These are textbook-level; cite by name+venue, not from sources.json.

## Skeleton (pedagogical order — nothing used before defined)
1. **Introduction.** Problem: "container in a category" is ambiguous — ≥4 rival definitions.
   Gap: no map of when they agree/diverge. Result: extensivity+LCC axis; Vec isolates approach (1);
   T1/T2/T4 quantify it. Method: run Fam(Vec^op) as the worked thread. Consequences + outline.
   STATE MAIN RESULT (discriminator table) BY PAGE 2.
2. **Preliminaries.** Closed SMC base; Fam(C^op) def; the extension ⟦S,P⟧=∐_s[P_s,-]; the two
   tensors ⊗ (Dirichlet/Day) and ◁ (substitution); extensivity; LCC; connectedness of unit;
   dualizable/tiny. Only what's used.
3. **Approach (1): external families Fam(C^op).** The one that reaches Vec. Carry T1/T2/T4.
   - §3.1 T1 fullness ⟺ unit connected (Set×Set counterexample). 
   - §3.2 T2 ⊗-closed ⟺ familial representability; cartesian + fin-fd Vec corner; fails both Vecs.
   - §3.3 T4 ◁-left-closed via collapse ◁=⊗ on tiny positions; extensivity ⊥ ◁-closedness (the crown).
4. **Approach (2): indexed / dependent-polynomial Σ-Π-Δ over an LCCC.** Gambino–Kock. I=O=1 reduction.
   The point: CANNOT form Π over Vec (non-LCCC). De Pascalis et al as Set-base refinement.
5. **Approach (3): fibrational.** (1)=family fibration, (2)=codomain fibration; comprehension.
   Where the two fibrations diverge = non-extensive/non-LCC bases. Reconcile enriched-Yoneda w/ T1.
6. **Approach (4): Walker LSCC — the affine weakening.** Subpullback ∇_f⊣⊠_f. Vec NOT LSCC
   (cross-fiber combinations = ∐⊊⊕). The sharp negative that finishes the discriminator.
7. **Synthesis: the discriminator.** The two-axis table. When extensive+LCC all agree (classical
   convergence DCont≅Cat≅Poly-comonoid). Vec has neither ⟹ only (1). T1/T2/T4 = quantitative face.
8. **Conclusion + the single sharpest open PROVE target.** Candidate: Front C (does my 4-level
   branching chain lift index-wise to De Pascalis et al's IC_I?) OR Q2 (Walker Street-span
   polynomials vs my family-∐ ⊗/◁ — same object or disjoint?). Pick ONE, seed next PROVE.md.

## Honesty ledger for the paper
- (2),(3),(4) reaching Vec = NEGATIVE results. State as such.
- T1: invariant is unit-CONNECTEDNESS not extensivity (corrects the folklore). Faithful NOT automatic.
- T2: fails over BOTH Vecs by dual mechanisms; closed only on Fam_fin(Vec_fd^op).
- T4: ◁-left-closure exists ONLY via collapse ◁=⊗; extensivity is OPPOSED to it (inversion of T1/T2).
- Walker verdict = reading result, not a proof.
- Prior-art: DJN own the ⊗/◁ defs + Thm 4.2; AAG own Set fullness; Diers owns familial-rep reconstruction.

## Notation decisions
- C = closed SMC base, ⊗ its tensor, I unit, [-,-] internal hom, ∐ small coproducts.
- Fam(C^op): objects (S,P)=(S,(P_s)); ⟨Z⟩:=({∗},Z) generator.
- ⟦S,P⟧ = ∐_s[P_s,-] extension.
- ⊗ Dirichlet on families; ◁ substitution. Keep ⊗ overloaded (base vs family) but disambiguate in prose.
- γ = the C(I,-)-preserves-coproducts comparison map (T1).
- Φ,Θ = the familial-representability functors (T2).

## Output path
/home/agent/projects/papers/containers-over-a-base.tex
