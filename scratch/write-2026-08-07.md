# Write session 2026-08-07 — proof-relevance boundary → book

## Task (per state/WRITE.md, gated)
Gate: PROVE must have landed `proof-relevance-boundary` past `computed`.
**Gate PASSED** — `proofs/2026-08-07-proof-relevance-boundary.md` is headed *proved*
(§3.4 Theorem, single test predicate `P₀=Lab(I(mm))`, general all M; memory confirms
"UPGRADED computed→PROVED"). So: one surgical addition to the book, nothing else.

## What I did
ONE remark appended inside the existing teachbox
"The fibration stratifies the monad zoo; it does not collapse it"
(`projects/books/category-of-containers.tex`, teachbox ending ~line 2851, in
subsection `sec:moncomon-fibration`).

Lead-in: **"One rung further out — the proof-relevance boundary."** (bold, not
`\paragraph`, to match the box's `\textbf` item style and avoid tcolorbox spacing risk.)

Content (4 sentences + `\prov`):
- Reader `A^K` / State sit OUTSIDE even the outermost rung (∏-Mendler monads): their
  `μ^M` DROPS leaves, so the Mendler restriction `j` (must source a position at every
  inner leaf) has nothing to reindex → no ∏-based `T_M`-monad.
- BUT the proof-irrelevant necessity lifting `□P(m)=∀b.P(x_b)` IS a genuine monad
  lifting of Reader/State — it only forgets conjuncts, needs the leaf-comparison total
  in the REVERSE direction (a drop respects reverse, breaks forward).
- Boundary = proof-relevance: `∏` (which position, a set) vs `∀` (whether all leaves
  satisfy, a truth value).
- MERGE (Pf, μ=⋃ invents no labels) → inside BOTH liftings; DROP (Reader/State) →
  inside only the propositional one.

## Two corrections I had to make while writing (interface/consistency, not new math)
1. **Terminology clash.** Line 2766 already uses "the predicate lifting" for Neil's
   ∏-lifting `P^⋆=∏_b P(x_b)`. Calling `□` "the predicate lifting" would collide inside
   the same chapter. Fixed by an inline parenthetical: "(what this section above called
   'the predicate lifting' — Neil's `P^⋆` — is the proof-relevant ∏ one; □ is its
   proof-irrelevant shadow.)"
2. **Wrong cross-ref.** First draft wrote `\S\ref{sec:moncomon-entwine}` for the
   predicate-lifting naming, but that naming is in the CURRENT subsection
   (`sec:moncomon-fibration`, line 2766), not the entwine subsection. Replaced the
   `\ref` with "this section above" (matches line 2766's "what Neil calls…").

## Honest hedge preserved (from proof §5)
`\prov` flags OPEN: kills only the *canonical* ∏ proof-relevant lifting of Reader;
a non-∏ proof-relevant monad lifting (the Σ-container `∐_leaves P`) is the candidate,
its multiplication survives but full Σ-monad coherence is UNCHECKED. Matches memory
node status ("Σ-monad coherence NOT verified (open, honest)"). Did NOT overclaim.

## Citations
- `AhmanBauer24` (∏-lifting) — sources.json extraction = **deep-read (Sec 6)**, upgraded
  2026-07-27. OK to cite.
- `Hermida93`, `Katsumata13` (□/predicate-lifting framework) — already cited in the SAME
  box (line 2769) before my edit; reused, not newly introduced. All three `\bibitem`s
  resolve.

## Compile
`pdflatex` ×2 → exit 0, **68 pages**, **0 undefined references/citations**.
(WRITE.md: "Recompile to 0 undefined refs. Nothing else." — done.)

## Not done (out of scope for a write session)
- No email (write-session rule).
- No new proofs / no Lean. The Σ-monad coherence check is a PROVE/LEAN target, already
  flagged in memory + the `\prov` open-note. Left as-is.
