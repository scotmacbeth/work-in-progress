# WRITE scratch — applications-outlook.tex (2026-07-23)

## Task
Standalone grant-Impact **outlook** (~4–6pp), NOT the book. Path-5 spine. Frame four
domains (orchestration, supply chains, ologs/KG) as one obstruction theorem, with the
computed supply-chain ZS instance (proofs/2026-07-23-supply-chain-zs.tex) as the §4
illustration. Flag for Neil; do NOT touch books/.

## Phase 1 — the one sentence
> Because a directed container *is* a small category (machine-checked), composing any two
> systems that present categories — supply chains, ontologies, orchestrated agents — is a
> single Zappa–Szép problem whose only obstruction is one degree-two cohomology class
> `[ω]∈H²(Sk_C;𝒟)`, and that one class is simultaneously supply-chain inventory
> inconsistency, ontology-merge conflict, and agent re-entrancy.

## Phase 2 — skeleton (position paper, not theorem paper)
1. Introduction — the composition question across three domains; the hinge; the one class; grades up front.
2. The hinge (DCont ≃ SmallCat, lean-verified) — one page, cite AU + DContCat.lean.
3. Three domains present categories — orchestration (proved), supply chains (open object-level, morphism-level ours), ologs/KG (definitional import, Spivak).
4. The payoff: composition = ZS; exists ⟺ (L)∧(G); fails ⟺ [ω]≠0. State the theorems (cited proved). Fold computed supply-chain instance as worked illustration + the Z/n refinement + olog n=2 sibling.
5. Honest status table (grades).
6. Outlook / what this buys the grant + open modelling questions (SEED Q4).

## Structural decisions
- State the payoff (one class, three domains) on page 1. Grades in a box up front — this is
  the honesty backbone of the whole note; put it where the reader can't miss it.
- The paper is an OUTLOOK, so no long proofs. The supply-chain instance is summarised, not
  reproduced (it lives in the companion computed note 2026-07-23-supply-chain-zs.tex, cite it).
- Cut the Baues–Wirsching/Rosebrugh–Wood coboundary bookkeeping — reference the proved note.
- Every domain row explicitly graded inline: lean-verified / proved / computed / definitional-import / open.

## Citation provenance (checked sources.json)
- Ahman–Uustalu "Directed Containers as Categories" 1604.01187 — **deep-read** ✓ (hinge cite)
- Spivak–Kent "Ologs" 1102.1889 — **agent-summary** ✗ → import DEFINITION only, cite by name,
  NO theorem number, FLAG deep-read TODO explicitly (per WRITE.md).
- Own notes: pairwise-zs (proved), g-obstruction (proved), orchestration-zs (proved,
  [ω]=ε Lean-verified Reentrancy.lean), supply-chain-zs (computed). DContCat.lean lean-verified.
- BW (1985) / Rosebrugh–Wood (2002 JPAA 175): classical background; appear inside the proved
  (G)⟺H² note's own bib. In the outlook, name them as classical background only.

## Honesty caveats to preserve
- Object-level "a real supply chain IS a category" = OPEN (SEED Q4). Never assert fidelity.
- Domain instances graded at most `computed`; general theorems at registry grade.
- Ologs = prior art (Spivak). The delta = running the obstruction THROUGH these domains.
- No book commitment. Flag for Neil.
