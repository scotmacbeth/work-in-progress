# Write session 2026-07-16 — Chapter 0 "The Machinery"

Target: polish + **personally verify** `expository/preliminaries-representables-yoneda-day-kan.tex`.
Deliverable Neil asked for (email UID 61). Retitle Chapter 0, forward-refs to Thm C, recompile.

## MATHEMATICAL AUDIT (done personally, not trusting draft agent)

### §1 Yoneda (Thm 1.2), Cor 1.3 (ff), Density (Thm 1.5)
- Covariant Yoneda `Nat(y^A,F)≅F(A)`, `y^A=Set(A,-)`. Θ(α)=α_A(id), Ψ(x)_X(g)=F(g)(x).
  Both round trips check. **CORRECT.**
- Cor ff: `Nat(y^A,y^B)≅Set(B,A)=Set^op(A,B)`, embedding `y^(-):Set^op→[Set,Set]` covariant
  (morphism A→B in Set^op = f:B→A in Set induces y^A→y^B by PREcomposition). **CORRECT.**
- Density: `F≅∫^A F(A)·y^A`, i.e. `∫^a C(c,a)×F(c)≅F(a)` for copresheaf F. **CORRECT.** (=(★★) below)

Co-Yoneda forms I'll trust (derived from Thm 1.5):
  (★★)  copresheaf F: ∫^c C(c,x)×F(c) ≅ F(x)          [kernel C(c,x), F covariant]
  (★★') presheaf   G: ∫^c C(x,c)×G(c) ≅ G(x)          [kernel C(x,c), G contravariant]

### §2 Neil obs (i) — free coproduct completion IS Lan (Thm 2.4)
- (Lan_Y F)(d)=∫^a Fam(A)(Ya,d)·F(a); Fam(A)(Ya,(a_s))≅∐_s A(a,a_s). Coproduct pulls out of coend;
  inner ∫^a A(a,a_s)·F(a)≅F(a_s) by (★★). ⟹ Lan_Y F(d)=∐_s F(a_s)=F̂. **CORRECT.**
- Specialise A=Set^op, F=y^(-): ⟦S,P⟧=∐_s y^{P(s)}=Lan_Y y^(-). **CORRECT.**
- ⚠ SIZE CAVEAT: Thm 2.3 (pointwise coend) assumes C small; Set^op is LARGE. Fine because (a) families
  S are small so each coend piece exists, and (b) the universal property of Fam gives the result
  directly without the coend. → add a footnote. (Editorial, not new math.)

### §3 Day (Thm 3.2 dayuniv) + Prop 3.4 (degen)
- Rep computation `y^a⊛y^b≅y^{a⊗b}` with copresheaf Day `∫^{c1,c2}C(c1⊗c2,c)×F(c1)×G(c2)`,
  y^c=C(c,-). Collapse c1 by (★★') [kernel C(a,c1), presheaf C(c1⊗c2,c)], then c2. **CORRECT.**
- ⚠ VARIANCE: draft writes embedding domain `(C,⊗)`. But y^(-):c↦C(c,-) is a functor OUT OF C^op
  (morphism a→b in C^op ⟹ y^a→y^b). Fix: domain **C^op**. (Consistent w/ §1 line 130.)
- ⚠ BASE: Prop degen says "via Set^op ... taken in C=Set^op". WRONG. For copresheaf Day on [Set,Set]
  the base monoidal cat is **C=Set** with ⊗=⋆; the OP only appears in the embedding's domain /
  in Cont=Fam(Set^op). Verified: y^a⊙_⋆ y^b=y^{a⋆b} with base (Set,⋆). ← matches four-monoidal
  chapter Prop (Niu–Spivak 3.79): y^A⊙_⋆y^B=y^{A⋆B}, base (⋆,I) on Set. FIX the phrase.
- Formula `p⊙_⋆q=(S_p×S_q,(s,t)↦p[s]⋆q[t])`. **CORRECT** (cocontinuity + rep collapse).

### §4 Neil obs (ii) — density-transport of closed structure (Thm 4.3 neil2)  ★★ THE PROBLEM
- (a) Day tensor is CLOSED (AFT on loc. presentable), `[G,H]_Day(c)≅Nat(y^c⊛G,H)`, determined on
  reps, `[y^c,H]_Day≅H(c⊗-)`. **CORRECT.**
- (b) draft claims: for ANY cocontinuous strong-monoidal L, comparison λ_{G,H} is invertible.
  **FALSE IN GENERAL.** Verified COUNTEREXAMPLE:
    C=1 ⟹ [C,Set]=Set (Day=cartesian, [G,H]=H^G). V=Vect_k closed. L=k[-] free vector space:
    cocontinuous ✓, strong monoidal (Set,×)→(Vect,⊗) since k[A×B]≅k[A]⊗k[B], k[1]=k ✓.
    λ_{G,H}: k[H^G] → Hom_k(k[G],k[H]). For G=ℕ, H=2:
      LHS=k[2^ℕ] (span of "column-indicator" maps e_n↦e_{f(n)}, finite combos);
      RHS=Hom(k^{(ℕ)},k^2)=∏_ℕ k^2 (arbitrary images of basis).
    λ NOT surjective: a general ψ has infinitely many distinct column-values v_n∈k^2; a finite combo
    of column-indicators realises only finitely many distinct patterns. ⟹ λ NOT iso. □
  Precise gap: the "reduce to representables" step needs L to preserve the PRODUCTS ∏_s[y^{c_s},-]
  that internal-hom-out-of-a-coproduct becomes. Cocontinuous L preserves COPRODUCTS, not these
  products. Draft's "enough right adjoints" was gesturing at exactly this, but then the single-rep
  step "iso of left adjoints ⟹ iso of right adjoints (mediated by L)" is the Beck–Chevalley /
  adjointability condition — NOT automatic. k[-] is the counterexample.

- WHAT'S ACTUALLY TRUE (= Neil's real observation, about the YONEDA embedding, not arbitrary L):
  Day's embedding theorem: for CLOSED (Set,⋆), the Day convolution is closed and the embedding is
  strong-monoidal-CLOSED with `[y^a,y^b]_Day≅y^{[a,b]_⋆}`. TRUE (Day 1970). k[-] fails because it is
  NOT fully faithful; the Yoneda/⟦-⟧ embedding IS. So restrict neil2 to the embedding / fully faithful
  dense case, OR state Day's theorem + note preservation-by-⟦-⟧ is a SEPARATE fact checked directly
  in the four-monoidal chapter (Niu–Spivak Ex 4.78 + Lean Dirichlet closure).

### PLAN OF EDITS
1. dayuniv: domain C^op.
2. Prop degen proof: base C=Set (kill "C=Set^op" phrase).
3. Thm 2.4: size footnote.
4. §4: REWRITE neil2(b) honestly — reduction correct, invertibility is the strong-closed condition,
   NOT free; give the failure reason; state Day's true rep formula; container case established
   DIRECTLY in monoidal chapter. Cor contclosed + Ex ccc reworded to not assert the false "for free".
5. Retitle "Chapter 0 — The Machinery"; forward-refs "needed for Theorem C"; voice pass.
6. Recompile; citation_check footprint.
7. STAGE email to Neil+Robin (NO SEND — write-session rule). Note the obs-(ii) correction for Neil.

### DECISION LOG
- Keep copresheaf Day convention (matches Def 3.1 + §1 embedding). Only variance/base wording fixed.
- neil2 counterexample is a genuine correction to Neil's obs (ii) AS FORMALISED BY THE DRAFT AGENT.
  Neil's own observation (Yoneda embedding) stands. Flag to Neil in staged email.
- No email sent (session rule overrides WRITE.md step 4). Stage in for-robin/ + note.

## MID-SESSION CORRECTION (caught by cross-checking Niu–Spivak Ex 4.78)
- My FIRST rewrite of neil2(b) claimed `[y^a,y^b]_Day ≅ y^{[a,b]_C}` (Day's strong-closed embedding).
  Checked concretely for ⋆=×: `[y^a,y^b]_×(d)=Set(b,d×a)=d^b·a^b`, NOT `d^{b^a}`. So that formula
  is WRONG for containers. Root cause: Day's strong-CLOSED embedding needs the BASE C closed; but
  Set^op with ⋆∈{+,×} is NOT closed, so the internal hom of two reps is a COPRODUCT of reps, not a
  single rep. The draft's ORIGINAL Example ccc (`[y^a,y^b]≅y^{b^a}`) was ALSO wrong the same way.
- TRUE formula (direct, convention-free): `[y^a,y^b]_Day(d)=Nat(y^{d⋆a},y^b)=Set(b,d⋆a)`.
  For ⋆=×: `=∐_{Set(b,a)} y^b = (Cont(y^a,y^b) shapes, b positions each)` = Niu–Spivak Ex 4.78
  "shapes are morphisms". VERIFIED against four-monoidal-chapter Def 4.78 (shapes=Cont(q,r)). ✓
- Fixed: thm statement (b), proof (b), Cor contclosed, Example ccc all now give the coproduct-of-reps
  answer with the a=2,b=1 numeric check (guess=d, truth=2d). LESSON logged to memory.

## FINAL STATE
- Compiles clean: 12pp, 0 errors, 0 undefined refs (2 pdflatex passes). PDF built.
- Citation footprint: Niu–Spivak 2312.00990 = deep-read ✓ (source for Ex 4.78/Prop 3.79).
  Textbook cites (Day1970/Loregian/MacLane/Riehl) = canonical, cite direct.
- Net change vs draft: removed a FALSE general theorem (neil2 over-generalised Neil's obs ii),
  replaced with the true density-principle + correct representable hom + counterexample; kept
  the two genuinely-correct Neil observations (i) [Lan] and (ii-as-density) intact.
