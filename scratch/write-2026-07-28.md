# Write session 2026-07-28 — fold the ENTWINING into Ch7

## Task (from state/WRITE.md)
Insert the proved *entwining of the two feeds* into Ch7 of `books/category-of-containers.tex`,
directly after the transfer section, as a short subsection. Plus Neil's five 07-27 additions.
NO PRs (seed off GitHub). Back up (done: /tmp/coc-backup-entwine.tex), edit in place, recompile
clean, report page count + 0 undefined refs.

## The one sentence (seed of the subsection)
> The two container-liftings of a single Set-monad `M` — the Ahman–Bauer shape-monad `T` and the
> transfer position-comonad `G` — do not merely coexist: `M`'s oplax product-comparison `str`
> assembles, on positions, into a mixed distributive law `λ : TG ⇒ GT`, and the *opposite*
> orientation is obstructed precisely by branching.

## Notation reconciliation with the book
- Book already calls the transfer comonad `G` (Def def:transfer-G). KEEP `G`.
- Ahman–Bauer shape-monad has NO symbol in the book yet — appears as `\cont{MS}{P^\star}` in the
  teachbox (line 2426). INTRODUCE `T` for it in the new subsection. `T` and `\str` are free (grepped).
- `P^\star` already denotes AB positions (line 2392, 2426). Reuse.
- Neil's item 4: call `P^\star : MS → Set` a **predicate lifting**; the ∏-cointerpretation = the
  *universal / Π-based* lifting. Use this language in the new subsection.

## Placement
New subsection `\subsection{The two feeds entwine}` at END of transfer section
(after the "[to be developed]" teachbox at line 2475, before `\section{Syntax and behaviour}` 2477).
Reason: entwining IS the interaction of the two feeds introduced in the transfer; culminating result.

## Skeleton of the new subsection
1. Hook: the "two ways to feed M" teachbox left two liftings side by side. Do they interact? Yes.
2. Name `T` (AB Thm 6.3) and `G` (transfer). Localise TG, GT at a shape `m` with leaves. Position table.
3. The canonical comparison `str` = oplax product comparison = predicate-lifting compatibility. Direction forced.
4. Theorem [The two feeds entwine]: `(T,G,λ)` entwining; TG⇒GT; each axiom = naturality of η/μ/str/i. \prov.
5. What λ means: Beck–Chevalley 2-cell; "M oplax-preserves products, on positions". One line + picture.
6. The branching obstruction (FEATURE): reverse GT⇒TG fails once M branches; Pf witness
   union-of-products ≠ product-of-unions; dichotomy table (arity≤1 vs branching).
7. Honest scope: ∏-cointerpretation class only; general Mendler open (registry dead-end). \prov.

## Neil's other four additions (all Ch7)
1. Literature sentences → ALREADY PRESENT in rem:transfer-novelty (Topos-PLTL 2404-9, Hinze 2412,
   AB first-named 2379). VERIFY reads right after insert; no dup with new subsection. Topos-PLTL I
   only have summary of — but it's already cited in the book at deep-read? CHECK sources.json floor.
2. Ghani–Kurz higher-order trees — existing teachbox stub at 2449 is `[stub — browse TODO]`. WRITE
   session, no browsing, GK not deep-read. DECODE the free-monad formula generically (I have the
   free-monad Lemma) + keep the exact n-dim signature as `[signature TODO — ask Neil]`. Do NOT guess.
3. Position-op "two faces" paragraph — NOT yet in book. Add one paragraph in Ch7: transfer applies
   `(−)^op` to the fibre OBJECT (M↦M^op ⟹ G comonad); free/cofree applies the SAME op to the
   recursion SCHEME (initial→final, syntax μ → behaviour ν). One op, two targets.
4. Predicate-lifting language — in new subsection (see above).
5. Workers forward-pointer — existing teachbox "On the horizon" (2459) has reader/Kleisli/oracle.
   ADD a one-line Workers `[to be developed]` item (PROVE opened it 07-28, proof file exists).

## Citations needed (entwining framework)
Standard/canonical triple, cite from field knowledge (not browse paraphrase):
- Beck 1969 (distributive laws), Brzeziński–Majid 1998 (entwining structures / coalgebra bundles),
  Power–Watanabe 2002 (combining a monad and a comonad = mixed distributive laws).
Add bibitems. These are foundational — honest to cite from knowledge.

## Honesty discipline
- Entwining = **proved** (not novel/unprecedented). Nearest prior = entwinings/mixed dist. laws.
- Contribution: the specific λ for the two feeds, forced orientation, branching obstruction.
- E2 general index-chase still mechanical-gap; machine-verified for ∏-Mendler incl. Pf. SAY SO.
- Whole-book cite floor = agent-summary from 2405.13157; introduce NO new sub-deep-read cites
  beyond the canonical entwining triple (which are textbook, not repo sources).

## Standalone-note verdict (owed to Neil, 07-26 daily)
TBD at end — does the entwining want its own short note? Distributive laws = seed core tool.

## Compile checkpoints
Baseline: 54pp, 0 undefined. Recompile twice after each block of edits.

## DONE (end of session)
- Entwining subsection written into Ch7 (`sec:moncomon-entwine`, `thm:entwine`, `ex:entwine-branching`,
  `rem:entwine-scope`). Placed after Maybe example, BEFORE the two forward-looking teachboxes (moved them down).
- Neil items: (1) lit sentences already present (07-25); (2) GK teachbox = generic decode + `[signature TODO]`;
  (3) "one op, two faces" paragraph in outlook; (4) predicate-lifting language used; (5) Workers line added.
- Bib: Beck69, PowerWatanabe02, BrzezinskiMajid98 added (field-knowledge foundational cites).
- Referee catch + fix: "str=iso for arity≤1" WRONG (Maybe M(1)→1 non-iso) → "no overlap"; fixed in book,
  proof-memory, and MEMORY.md.
- Book: 58pp (was 54), compiles clean, 0 undefined refs. Backup /tmp/coc-backup-entwine.tex.
- citation_check footprint: my additions cite only deep-read (AhmanBauer24) + foundational; floor still
  agent-summary from pre-existing 2405.13157 (not mine). No new sub-deep-read debt.

## STANDALONE-NOTE VERDICT (owed Neil since 07-26): YES, eventually.
The entwining earns its own ~4–5pp note — a distributive law is the seed's core composition tool, and the
forced orientation + branching obstruction (Pf, union-of-products≠product-of-unions) is a crisp self-contained
story the book can only subsection. BUT write it AFTER the general-`j` chase is spelled out OR the Lean lands,
so it carries no "mechanical gap." Flagged, not started. In collaborator note + PROGRESSIVE_DISCLOSURE.
