# Write session 2026-08-09 — the identity T^Σ_M = M ◁ − into the book

## Target
`books/category-of-containers.tex`, chapter `ch:moncomon`, section `sec:moncomon-fibration`.
NOT a new standalone paper. Adjoin to existing material; do not duplicate.

## What already exists (checked)
- §predicate-liftings (`sec:predicate-liftings`, ch:comonoids, ~1559): E=◁, A cartesian-only,
  A-module law (object-level, static). Forward-refs sec:moncomon-fibration.
- sec:moncomon-fibration (~2967): container fibration p:Cont→Set; G vertical fibred comonad;
  T lifts M always / fibred-monad iff cartesian; λ = mixed DL not BC; the 4-level stratification
  teachbox; the 2×2 ℤ/2 grading table with T^Σ DEFINED (line ~3177) and stated a monad for
  Reader/State; Orestis witness. **Has an "Open" flag (3208–3212): reverse-total ⟹ Σ-monad OPEN.**

## What is NEW (08-08, to write)
Source: proofs/2026-08-08-sigma-monad-is-triangle-monoid.md + connections file.
The crown identity that RESOLVES the open flag:
1. Prop: T^Σ_M(C) = M ◁ C (identity of endofunctors); η^Σ=η_M◁−, μ^Σ=μ_M◁−.
2. Thm (corollary): T^Σ_M monad ⟺ M is a ◁-monoid (container monad) ⟺ ⟦M⟧ Set-monad w/
   polynomial structure maps. Mechanism: Cont≃Poly↪[Set,Set] ff strong monoidal ⟹
   "A⊗− monad ⟺ A monoid". σ = μ_M's backward map; (U1)(U2)(A) = ◁-monoid laws on positions.
   ⟹ 08-07 Reader/State proof is now an EXAMPLE of a theorem (Reader=diagonal comonoid on E,
   State=store monad).
3. Bag refutation: reverse-total ⟹ Σ-monad FALSE. Bag reverse-total (μ=union leaf-bijection,
   σ=id) but T^Σ_Bag not even a functor on Cont — Bag∉Cont (fails connected pullback A→1←B;
   |Bag(2×2)|_2=10≠9; {(0,0),(1,1)} vs {(0,1),(1,0)} collide). Discriminator: polynomial vs
   analytic. Slogan: reverse-total : ◁-monoid :: analytic : polynomial :: forgets-provenance :
   tracks-provenance.
4. Both-legs contrast box: ∏=T_M lifts ⟺ M cartesian; Σ=T^Σ lifts ⟺ M ◁-monoid.
   Note Orestis oplax Λ-join ⊆ = base-monad-join layer (type-valued caveat), separate from
   strict object law.

## Placement decision
New subsection `\subsection{Which monads lift: the $\Sigma$-lifting is $M\triangleleft-$}`
inserted AFTER the contravariant-caution Remark (line ~3241), BEFORE "The section closes with
two forward-glances". It is culminating core content, not a forward glance.
ALSO: rewrite the "Open" flag in the big teachbox (3208–3212) → now RESOLVED, forward-ref new subsec.

## Citations used (all deep-read / in bib)
- NiuSpivak23 (Cont≃Poly strong monoidal, ◁↦∘)
- AhmanBauer24 (∏-lift)
- HermidaJacobs98 (two fibrations)
- Beck69 (entwining) — only if mentioning oplax layer
- OrestisAgda (oplax Λ-join caveat)
Bag-not-polynomial: cite as MacBeth computed; MO/Lumsdaine is community corrob (memory), NOT a bib
source here — keep it as prov note "computed", do not add a bibitem for MO.

## Voice
Book voice: teaches by revealing structure; plain words + Neil's vocab; Reader/State running
witnesses; one astonishment (the Σ-lift you painstakingly verified is JUST M◁−). Retire
"proof-relevant" (already logged once in the section). Provenance tags mandatory.

## DONE (end of session)
- Inserted `\subsection{Which monads lift: the Σ-lifting is M◁(−)}` (`sec:sigma-is-comp`) into
  `sec:moncomon-fibration`, after the contravariant-caution remark. Prop `prop:sigma-is-comp`,
  Cor `cor:sigma-monad`, Thm `thm:bag-refutes`, both-legs teachbox.
- Rewrote the 2×2-teachbox "Open" flag → "Resolved" (reverse-total⟹Σ-monad FALSE), forward-ref.
- Cited SigmaLift.lean as Lean-verified (checked: real, sorry/axiom-free, sigmaLift_eq_seq=rfl).
- Softened Prop hypothesis to candidate η/μ (not presupposing monad); capitalized Reader/State.
- Compiles clean: 77pp, 0 undefined refs, 0 new bibitems (all 4 cites pre-existing deep-read).
- Updated PROGRESSIVE_DISCLOSURE.md; collaborator note
  `memory/for-collaborator/2026-08-09-sigma-is-comp-book-section.md`; memory
  `book-sigma-is-comp-section-done.md` + MEMORY.md pointer (also compacted MEMORY.md to <17.1KB).
- No GitHub push: this is a section addition to an existing chapter Robin reads from host directly;
  no established book git repo. No email (write-session rule).
- Ch7 lead-ordering (open Neil decision) written to stand as a theorem either way; asked in note.

## Honesty flags to preserve
- Action-law/module coherence 2-cells still object-level only (already flagged in predicate-lift §).
- LEAN rung for T^Σ=M◁− general: memory says DONE (lean-sigma-lifting-general-done, 08-09
  SigmaLift.lean, 0 axioms). So I CAN cite it Lean-verified! Check the lean file exists.
