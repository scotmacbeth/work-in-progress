# Ch7 climax reframe — write session 2026-08-12

Live file: `/home/agent/projects/books/category-of-containers.tex`
Target: `\subsection{...}` cluster at 3546–3888 (Reader → State → π₀ box → frontier box).

## The problem with the current arc
The general theorem `liftings ≅ Fun(𝔸(↓),Cat)` currently lives ONLY as a hedged
"humbling frontier" teachbox at line 3818, framed as a footnote/open-problem AFTER
the π₀ two-poles teachbox has been presented as the answer. WRITE.md: elevate the
general theorem to the CLIMAX; demote Reader/State to the two degeneracies.

## The astonishment arc (fair is foul)
1. Reader liftings ARE categories (kept, thm:reader-classification).
2. State's store multiplication is INVISIBLE, π₀=1 (kept, thm:state-classification).
3. Synthesis: π₀ of the threading action is the dial (two-poles box) — plant the
   tempting conjecture "π₀ classifies".
4. THE REVERSAL (new climactic subsection): those were degeneracies. The general
   law is `Fun(𝔸(↓),Cat)`, holonomy-FULL. The invisibility was reset-ERASURE, not
   absence. π₀ read off two accidents.

## Edits
- (a) Two-poles box (3784–3801): keep, but end by making the π₀-classifies
  conjecture EXPLICIT so the reader is complicit in the wrong guess.
- (b) Belief-propagation box (3803–3813): MOVE into new subsection, after the
  "trivial two ways" degeneracy box — that is where "holonomy" is a defined
  character and "trivial holonomy ⟹ collapse" is the active idea.
- (c) Replace transition (3815–3816) + frontier box (3818–3848) with new
  `\subsection{The general law...}` label `sec:update-liftings-holonomy`:
  - reversal opening (fair is foul)
  - Def: update monad Upd_{(S,P,↓)} (subsumes Reader P=1, State P=overwrite) +
    position-threading action ↓:P↷S
  - Def: action category 𝔸(↓) (objects S, arrows s→s↓p, comp=⊕)
  - Theorem: liftings ≅ Fun(𝔸(↓),Cat), holonomy-full. prov=08-11 proof + machine.
    "Why it holds": deepest-object (COMP) law forces ρ functorial; isotropy acts.
  - Worked Example: ℤ/2 trivial action ⟹ 4 non-iso liftings (compute-the-surprise).
  - teachbox "Trivial two ways": Reader=discrete 𝔸 (nothing to transport ALONG);
    State=reset ⟹ endpoint-locality ⟹ codiscrete collapse (transport ERASED);
    free Z2_swap=codiscrete/trivial isotropy (third way). π₀ classifies IFF every
    component holonomy-trivial.
  - [moved belief-prop box here]
  - closing teachbox: isotropy action = a group representation carried by
    composition; sibling of [ω]∈H² (reentrancy/ZS). Grant payoff.
- Keep higher-order-trees + Workers-horizon boxes after.

## Provenance / citations
- `\cite{AhmanUustalu13}` bibitem already correct (TYPES 2013 → LIPIcs 26, 2014).
  Keep the label; printed ref is accurate. (WRITE.md's `ahman2014update` key note is
  a sources.json/provenance concern, not the book bibitem.)
- 08-11 proof is degree-1, |S|=2 machine-verified, uniform argument. Keep the
  [open] flags HONEST: higher object-degree + beyond-Upd still open; novelty-check
  vs Uustalu TTCS 2017 deferred. Do NOT overclaim.
- CBP box: keep "structural resonance, NOT a container citation" hedge verbatim.

## Voice
Celebratory reversal, not humbling. "Fair is foul, and foul is fair" is the motto —
use it once, earned. The clean poles were foul (hiding structure); the messy general
case is fair.
