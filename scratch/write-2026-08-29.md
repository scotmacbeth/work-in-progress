# Write session 2026-08-29 — hostile-referee revision of `containers-over-a-base.tex`

## State on entry
- Draft is COMPLETE, twice-polished (two full sessions 08-28: §5 logic-of-containers, §3 landscape,
  δ/Φ re-filing). Compiles clean, 16pp, 0 undefined refs. Citation floor deep-read (all 6 arXiv IDs).
- This session = genuine hostile-referee pass: intro↔conclusion consistency, read proofs as referee,
  targeted clarity, honesty guardrails, recompile. NOT a content session.

## Judgement on "cut 10%"
The /write guidance ("cut 10%") targets bloated first drafts. This is a mature survey whose
recapitulation (thesis blockquote / landscape organizing-fact / synthesis / conclusion) is
*deliberate* and serves a reader navigating four approaches. Aggressive cutting here is net-negative.
Decision: surgical clarity fixes only, no forced trim. Logged so the next session doesn't re-litigate.

## Findings (hostile referee)

### F1 [FIX] Intro overloads "connected" — real clarity bug
Intro Thm 1.1 (introT1): "...the unit $k$ is a connected object but $\C(k,-)$ ... fails to preserve
$\oplus$, so $\ext{-}$ is neither full nor faithful." At page-1 the reader has NO Def 2.5, and reads
"connected" two ways in adjacent clauses (k indecomposable-object vs C(I,-)-preserves-coproducts).
Looks like a contradiction with "ff iff unit connected." → drop the "connected object" red herring in
the intro; state the underlying-set functor fails to preserve ⊕ ⟹ unit disconnected in our sense.
rem:vecbad keeps the two-senses nuance where Def is in hand.

### F2 [FIX] prop:setobstr — "the representing container" is loose; tighten the logical spine
"...forces the representing container to carry double-exponential data..., which is not polynomial,
so the right adjoint does not exist." Referee: representing WHAT? Make explicit that a container's
extension is polynomial, and double-exponential shape-growth cannot be a container extension. Faithful
to proof file (Workers Thm 2 growth argument, n^{2^n}); no new proof.

### F3 [FIX] Abstract grammar: "two properties ... are the axis of divergence" → "the two axes".

### Checked, NO change needed
- intro↔conclusion consistent (four constructions, two axes, T1/T2/T4, logic-of-containers). OK.
- thm:dualisation fibre logic (∧=⊔ witnesses, ∨=∏ witnesses, ⊤=∅→P, ⊥=id) verified against
  (Set/P_s)^op products/coproducts/(co)terminal. Correct.
- thm:quantAE vs rem:trap (cartesian reindexing (Σ_ρ)^op not ρ^*) consistent.
- rem:enriched pre-empts the set-level-vs-enriched ff objection. OK.
- Honesty guardrails intact: Walker verdict framed as reading (rem:walkerstatus); §5 scope gaps
  stated (rem:logicscope); δ/Φ re-filing framed as reading not theorem (rem:weberrefile).
- Citation floor deep-read (von Glehn debt discharged per 08-28 session-2 note).

## Provenance carried-forward (external-submission TODOs, not blocking internal draft)
- FGHW sources.json = agent-summary (census-row pointer only). WeberFamilial TAC18 not in sources.json
  (canonical, stable). Confirm before any arXiv submission. citation_check keys arXiv-only ⟹ silent.
