# Write session 2026-09-04 — reentrancy note + admissibility Xarez cite (VERIFICATION PASS)

## Situation on entry
WRITE.md (Sep 3 14:16) requested two revision tasks:
- PRIMARY: apply Rick's referee revisions to `reentrancy-machine-checked-obstruction.tex`
- SECONDARY: add Xarez [1112.4277, Thm 5.1] prior-art paragraph to `admissibility-composition-product.tex`

**Both were ALREADY applied and committed in the prior write session (Sep 3, 21:27–21:33),
pushed as `20502ea`** ("papers: apply Rick's referee revisions to reentrancy note; add Xarez
prior-art to admissibility note"). WRITE.md was never retired, so the loop re-triggered a
write session with the same stale trigger.

## What I did this session (verification + one polish)

### 1. Verified PRIMARY (reentrancy) — all referee items present in current .tex
- ASK: Prop 3.1 → 3.1(a)/(b)/(c), individually `\label`led (`prop:red-hyp/branch/complex`). ✓
- NIT: C³=0 sentence in the boundary declaration (§Discussion): "the Lean model takes Z²=C²
  as a definition, reflecting the pen-and-paper fact C³=0." ✓
- SUGGESTION: Appendix A derives ω_T=(0,ε), kept explicitly pen-and-paper (Lean takes (0,ε)
  as data — does NOT join the Lean-verified mile). ✓
- HOUSEKEEPING (i): Reentrancy.lean SHA-256 `e28da311…dc2c` on p.1, Lean unchanged since fad5f3c. ✓
- HOUSEKEEPING (ii): Gerstenhaber reframed — hostile-referee reread confirms it asserts only
  standard textbook Gerstenhaber deformation theory (HH² deformations / HH³ obstructions, safe
  without page precision) and hedges "absolute/classified" as an *informal* label, NOT attributed
  to §1. No unsourced claim remains. Consistent with memory `gerstenhaber-1964-cite-unverified`. ✓

### 2. Verified SECONDARY (admissibility) — Xarez remark present (lines 697–717)
- Exact required framing: same π₀-multiplicativity governs Xarez's STABLE-UNITS (Thm 5.1);
  co-extensive over infinitary-lextensive base; our proof via external law (S)/(D) not Galois descent. ✓
- PHRASING GUARD honored: distinguishes his semi-left-exact "admissible" (Thm 4.1, weaker) from
  stable units (Thm 5.1); does NOT write "admissibility ⟺ π₀ multiplicative". ✓
- Failures are ours: Set×Set + Gl((−)²) new, Weichsel kept as failure anchor. ✓
- CHK (J. Austral. Math. Soc. 38A, 1985) + CJKP (Appl. Categ. Struct. 5, 1997) refs added. ✓

### 3. Compilation — both clean
- reentrancy: 8 pp, no undefined refs/citations.
- admissibility: 22 pp, no undefined refs/citations.

### 4. Citation provenance gate (code/citation_check.py --report footprint)
- reentrancy: floor = deep-read. admissibility: floor = deep-read. **Gate passes** (≥ deep-read).
- Flagged "corrections" on 1604.01187 (Ahman–Uustalu) and 2312.00990 (Niu–Spivak) are historical
  *source-entry* metadata fixes (title / author) — my cites already use the corrected forms. No action.
- Xarez 1112.4277 = deep-read, no corrections. Good — this was the secondary task's key source.

### 5. Polish (the only edit I made this session)
- reentrancy had 3 overfull hboxes; worst = 27pt at the "proofs are as short as they look"
  paragraph (three unbreakable `\texttt` identifiers). Fixed with a local `{\sloppy …\par}`
  group — **content-neutral, no prose change**. Overfull cleared; remaining two are sub-8pt
  (3.4pt, 7.8pt), invisible, left alone.
- Propagated to the repo copy (`tmp-wip/work-in-progress/papers/`), rebuilt PDF, committed +
  pushed as **`b8462d1`** ("typography only, no content change"). projects/ and repo now in sync.

## State of the two papers
- `reentrancy-machine-checked-obstruction`: peer-cleared (Rick), revisions applied (20502ea),
  typography polished (b8462d1). **Writing complete.**
- `admissibility-composition-product`: last citation gate (Xarez) applied (20502ea). Writing
  complete; publishable-result is a later separate call per WRITE.md.

## What remains — NON-write-session actions (email prohibited this session)
Per the disposition in `proofs/reviews/2026-09-03-reentrancy-standalone-note.md`:
1. Email Robin (§4.3): commit hash, PDF link, title, one-para what/why-ready, least-sure point —
   to move reentrancy note to publishable-result.
2. Reply to Rick (§2.3 header, new commit hash, revised PDF attached; CC Robin) — covering note
   already drafted at `memory/for-collaborator/2026-09-03-reentrancy-revision-reply-to-rick.md`.
   NOTE: that draft cites commit 20502ea; update to b8462d1 when sending.
3. Deferred BROWSE: pull Gerstenhaber Ann. Math. 79 (1964), verify whether absolute/classified is
   genuinely his §1; if yes restore sharper attribution.
4. Deferred LEAN: formalise the first mile (orbit category / (L)-freeness / transversal defect
   ω_T=(0,ε), Appendix A) — the marked next Lean target.

## Trigger disposition
Renamed WRITE.md → WRITE.md.done-2026-09-04-referee-revisions-verified. The *writing* is done;
all remaining items require email/browse/lean sessions, not write.
