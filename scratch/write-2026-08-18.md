# WRITE scratch — Containers over Vec (expository note)

**Date:** 2026-08-18. Source: `proofs/2026-08-18-linear-containers-vec.md` (all 3 parts PROVED/computed),
`scratch/vec-containers-orientation.md`. Target: `expository/containers-over-vec.tex`.

## One-sentence seed
Base-changing containers from Set to Vec trades the coproduct/product distinction for a biproduct, and
the single failure of extensivity (∐ ⊊ ⊕) is what collapses shapes (objects) and breaks fullness
(morphisms) at once.

## Audience decision
Bright-and-curious reader who KNOWS the Set container story (⟦S,P⟧X = Σ_s X^{P_s}, Cont≅Fam(Set^op),
F(1)=S). So I recall Set facts briefly as the "before" picture and spend the ink on what changes.
Neil + Robin are primary readers; this is grant new-front groundwork, not a results paper.

## Structural decisions
- amsart. Expository, so: preface/intro states the question + the headline before any lemma.
- Order (dependency-sorted):
  1. Intro: the base change, the one-line answer, roadmap. State collapse by "page 2".
  2. Defs: linear container, morphism, extension ⟦−⟧, corepresentable h_P. Set/Vec side-by-side.
  3. Three lemmas (co-Yoneda linear, h additive+ff, finite positions distribute over ⊕). These are
     the toolbox — everything downstream uses them. Prove all three (short).
  4. Part 1 — biproduct collapse: terminal-recovery-fails box; finite collapse Thm; survival regimes.
  5. Part 2 — hom formula, container-hom, non-fullness = extensivity crux. The ∐⊊⊕ punchline.
  6. Part 3 — composition ◁ = (S×T, P⊗Q); comonoid = family of k-algebras (algebroid guess fails).
  7. Carry-over table (the orientation table, now with proved/computed/open status).
  8. Neighbours, honestly (strict poly functors own objects; framing is the delta).
  9. Open questions (general rep thm; full comonoid law; ∞-dim composition).

## Teachbox surprises (expository "astonishments")
- "Always ask what the terminal object is." Vec's is 0, so F(1)=S dies. → homological replacement.
- The Example: ({∗},k²) vs ({∗₁,∗₂},(k,k)) — same Id², different shape sets. Over Set they SEPARATE
  at X=1 (X² vs 2X). The collapse is exactly the loss of that separator.
- ∐ ⊊ ⊕: one symbol carries the whole difference. Object-collapse and morphism-collapse are ONE thing.

## Honesty ledger (do NOT over-claim)
- Objects (⊕_s h_{P_s}) = degree-1 corner of strict poly functors (Friedlander–Suslin). NOT mine.
- Day/analytic ⊗ = vector species / TCA (Sam–Snowden). NOT mine.
- "coproduct of representables" + extensivity = Diers familial rep / Carboni–Johnstone. NOT mine.
- "algebroid" = Mitchell. NOT mine.
- MY delta = the assembly: Fam(Vec^op)-container framing + the shape index + the identification of
  shape-collapse and fullness-gap as ONE phenomenon = failure of extensivity ∐⊊⊕. Say this plainly.
- Prop 4.2 (comonoid=family of algebras) is COMPUTED not fully proved — mark as such.

## Citations to verify (run citation_check footprint before finalising)
Friedlander–Suslin 1997; Krause 1203.0311; Sam–Snowden 1209.5122; Carboni–Johnstone 1995;
Mitchell 1972 (Rings with several objects); Eilenberg–Watts; Diers/multirepresentable (nLab);
Altenkirch–Ghani–... for Set container rep thm; Krull–Schmidt–Azumaya (standard).
Only cite deep-read+ sources as references. Downgrade rest to prose mention or general attribution.

## Revision lenses (later pass)
- Intro↔conclusion consistency. Cut 10%. Is headline clear from intro alone?
- Hostile referee on Prop 4.2 sketch and the "extensivity is the whole story" claim.

## DONE — draft complete (2026-08-18)
`expository/containers-over-vec.tex`, 13pp, compiles clean (0 errors, worst overfull 10.6pt).

### Structural decisions that survived
- Front-loaded the one-line answer (∐⊊⊕ / extensivity) in §1 with theorem refs — headline is clear
  from intro alone. ✓ page-2 rule.
- The side-by-side hom-formula display (∏∐ → ∏⊕) is the visual crux; renders beautifully (p.7).
- Redesigned the choice-vs-combination diagram as two minipage TikZ subpictures (original single
  tikzpicture had overlapping title nodes + 106pt overfull). Clean now (p.8).
- Kept ALL three parts (collapse / crux / composition+comonoid) — an expository landscape note SHOULD
  be complete; did not force a 10% cut that would harm the "one phenomenon, three strikes" arc.
- Honesty: Prop 6.3 explicitly marked `computed`; boxed provenance caveat in §8 flags that neighbour
  cites are general-knowledge attributions not deep-read this cycle (no browsing in write session).

### LaTeX gotchas logged (for next write session)
- `\Vec` is predefined → `\renewcommand`. `\k` is the ogonek accent → renamed field to `\fld`.
- `\llbracket` needs `stmaryrd`; stmaryrd's bitmap font breaks microtype expansion →
  `\usepackage[expansion=false]{microtype}`.
- `>{\raggedright\arraybackslash}p{}` columns need `\usepackage{array}`.
- `\emergencystretch=2.5em` absorbed the residual prose overfulls from unbreakable `$..$`-hyphen
  compounds (e.g. `$\fld$-algebras`, `$\Fam(\Vec^\op)$`).

### Referee/consistency check performed
- Intro ↔ §9 conclusion: both anchor ∐⊊⊕ + grant-spine deformation. Consistent.
- Did NOT overclaim: abstract/Thm 5.3 say `⟦−⟧` stops being FULL, never that it loses faithfulness
  (it stays injective-away-from-zero). Correct per companion "faithful-on-nonzero but not full".
- Running example arithmetic re-checked: Set ({∗},2)→X², ({∗₁,∗₂},(1,1))→2·X separate at X=1 (1≠2);
  Vec versions both → Id². ✓

### Still owed (NOT this session — flagged in note)
- Prop 6.3 full comonoid-law verification → PROVE.
- Deep-read the §8 neighbour primary sources into sources.json → BROWSE.
- Possible graduation of finite-collapse + crux core into a book section once comonoid check lands.
