# Write session 2026-08-06 — Reader/State census correction (book audit)

## Task (WRITE.md)
Correction-only. The pre-PROVE "witnessed census" — *"Reader/State are ∏-Mendler,
non-cartesian, witnessing cartesian ⊊ ∏-Mendler"* — was **REFUTED** this cycle
(`proofs/2026-08-06-state-reader-ladder-census.md`). The book must NOT gain a false
"Reader/State ∈ ∏-Mendler" rung. GATE: no-op if the stratification box is already clean.

## Audit of `projects/books/category-of-containers.tex`

Stratification payoff box = `\begin{teachbox}[The fibration stratifies the monad zoo...]`,
lines **2792–2825** (§ Monads-and-Comonads, subsec `sec:moncomon-fibration`).

Ladder as written:
```
A×(-)  ⊊  E+A×(-)  ⊊  {cartesian monads}  ⊊  {polynomial / ∏-Mendler monads}
pure writer  writer+exc                        ambient class
```
Splitters: Maybe (strict-BC ⊊ non-branch), List (non-branch ⊊ cartesian),
**Pf** (cartesian ⊊ ∏-Mendler; "μ=∪ merges leaves ... outside even [cartesian]").

### Checks against the GATE
- Box names **Pf** as the `cartesian ⊊ ∏-Mendler` witness — YES (bullet 3).
- Box mentions Reader/State as ∏-Mendler — NO. Neither appears in the box.
- Only ∏-Mendler-context Reader mention (line **2501**): "the reader monad $A^K$ is
  **not** one, which is why the lifting must be weak." Already correct.
- Line **2903** Reader mention: narrative (Neil's prompt for the Workers/store-comonad
  section). No ∏-Mendler claim. Harmless.
- State mentions (2882–3040): all about $\Delta S$ / store comonad / Workers — a
  *different* construction, never "State monad is ∏-Mendler."

**Conclusion: GATE Action 1 fires. Write NOTHING to the book.** Collaborator note already
present (`for-collaborator/2026-08-06-state-reader-outside-pi-mendler.md`), records the
refutation + corrected trichotomy. Nothing to compile (no book change). Done.

## One residual imprecision — logged as optional TODO (NOT fixed this session)

The box labels the outermost rung **"polynomial / ∏-Mendler monads"** (display eq + bullet 4).
Post-refutation this slash is loose: the proof's P1 shows Reader ($=y^E$) and State
($\cong\sum_{h:S\to S} y^S$) **are** polynomial monads yet are **not** ∏-Mendler
(their μ drops leaves ⟹ no μ^T). So polynomial-monads ⊋ ∏-Mendler-monads; the slash reads
them as synonyms.

- It does NOT mention Reader/State, so it is not GATE Action-2 territory, and Action 1
  says write nothing to the book. Honoring that; flagging here instead.
- Cleanest future micro-fix (a dedicated pass, not a write-correction pass): drop the
  "polynomial /" gloss so the top rung reads "∏-Mendler monads" (the ambient lift class),
  OR add one clause: "polynomial monads that carry the ∏-cointerpretation lift (μ drops no
  leaf, labels rigid) — this excludes the leaf-droppers Reader, State and the symmetric Bag."
  The latter is the honest one-sentence home for the trichotomy slogan, but it *adds*
  content and belongs to a book-writing pass, not this surgical correction.

## Trichotomy (for reference — already in the collaborator note)
Non-cartesian μ fails three ways by how κ_μ : I(mm)→lv(μ mm) breaks:
- MERGE  — κ_μ total, non-injective — **Pf** — INSIDE ∏-Mendler (witnesses cartesian ⊊ ∏-Mendler)
- DROP   — κ_μ non-total — **Reader, State** — OUTSIDE
- SYMMETRY — P^⋆ ill-defined — **Bag** — OUTSIDE
Slogan: "∏-cointerpretation tolerates merging but not dropping."
