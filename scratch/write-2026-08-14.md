# WRITE 2026-08-14 — sharpen gap-#3 two-[ω]-sites teachbox (book §sec:emergent-holonomy)

## Task
Replace the hand-wave teachbox (book lines 4824–4838, "Two H² classes that rhyme")
with the sharpened relationship proved 2026-08-14
(`proofs/2026-08-14-two-omega-sites-isotropy-restriction.md`).

## Source result (what to write up)
- **Part 1 (refutation):** isotropy-restriction i_x*: H²_BW(Sk_C;𝒟)→H²(Aut(x);𝒟(x)) is
  the ONLY natural candidate (deg 2, right variance). Sk_C is automorphism-rigid ⟹ i_x*
  is the ZERO map at every object ⟹ kills [ω_h]. Handoff generator supported on length-2
  chain Sup→Wk→Rt through 3 distinct objects = off-diagonal/nerve class, exactly what
  isotropy restriction discards. The two 𝔽₂'s coincide numerically, not by any restriction.
- **Part 2 (no functor rescues):** for every F: 𝔸(↓_⋈)→Sk_C, i_s*F*[ω_h]=0 (functor sends
  loops to loops; rigid target has none). Different base categories (roles vs states); a
  common refinement is what's needed. Obstructed for BG-cospans + both directions checked;
  general many-object cospan = the one honest open edge (gap #1).
- **Part 1.5 (collapse from the other side):** aligned + A normal ⟹ E=A⋊B split ⟹
  [ω_st]=0 for every action; (ℤ/2,ℤ/2,ℤ/4) geometrically impossible for exact
  factorisations. So the ε/ℤ4 row of the table is an ABSTRACT extension, not realised by
  an aligned ZS point-stabiliser. STRENGTHENS the two-sites thesis (stabiliser class forced
  0 exactly where naive identification wants it nonzero).

## Structural decisions
1. **Primary:** rewrite the teachbox → 4 paragraphs:
   (a) astonishment (same ε, two sites, "surely one class?");
   (b) refutation — introduce BW-generalises-group-coh gently (one object = a group),
       the restriction map, compute → 0 (rigid, off-diagonal support);
   (c) no functor rescues + Part 1.5 collapse from the other side (compressed);
   (d) discipline "check the support", rhyme-not-identity close.
2. **Secondary honesty (Part 1.5 forces this):** the table above (line ~4805) currently
   labels the middle column "composite isotropy E" and shows ε ⟹ ℤ/4 as a live composite
   isotropy. Part 1.5 says aligned+normal never realises ℤ/4. Minimal honest fixes:
   - table column header: "composite isotropy $E$" → "extension $E$ classified by $[\omega]$"
     (makes table = the abstract classification, which is TRUE);
   - line 4794 sentence: reframe nonzero as the *abstract* non-split extension + forward-ref box;
   - thm:emergent-h2 Scope note: add one clause (aligned+normal ⟹ split, so [ω]=0 within the
     hypothesis; nonzero is realised outside aligned geometry — see box).
3. No new bibitems (BW85 already cited). No new proofs — all from the PROVE file.

## Cross-refs to use
- thm:h2 (line 4353): [ω_h]∈H²(Sk_C;𝒟), "handoff category".
- thm:emergent-h2 (4783): [ω_st]∈H²(B;A).
- The book's prior conflation-correction (λ / Beck–Chevalley, ~line 3195) = "the kind of
  error this book has elsewhere had to correct" — keep that spirit for the discipline beat.

## TODO / notes (not this session)
- thm:emergent-h2's nonzero branch is vacuous under its own aligned+normal hypotheses
  (Part 1.5). I keep the theorem as the ABSTRACT extension classification + realisability
  caveat. A fuller reconciliation of the theorem's scope (state it for abstract extensions,
  separate the geometric-realisability corollary) may deserve a future prove/write pass.
  Flag to Robin/Neil.
